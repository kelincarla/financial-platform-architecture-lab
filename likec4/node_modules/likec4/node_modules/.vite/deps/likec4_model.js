import { t as __commonJSMin } from "./rolldown-runtime-FDOR9p9I.js";
import { F as Z, J as dynamicViewFlow, dn as nonexhaustive, gt as invariant, mn as parentFlow, un as nonNullable, vt as isAncestor } from "./types-Dcw6-bwQ.js";
import { A as isDeployedInstanceModel, B as isNodeModel, C as findDescendantConnections, D as hasSameSourceTarget, E as hasSameSource, F as isElementModel, H as isRelationModel, I as isIncoming, J as ve, K as sortConnectionsByBoundaryHierarchy, L as isLikeC4ViewModel, M as isDeploymentNodeModel, N as isDeploymentRelationModel, O as hasSameTarget, P as isEdgeModel, R as isNestedConnection, S as findDeepestNestedConnection, U as mergeConnections, V as isOutgoing, _ as differenceConnections, a as EdgeModel, c as LikeC4DeploymentModel, d as Q, f as RelationshipModel, g as _e, i as DeploymentRelationModel, j as isDeploymentElementModel, k as isAnyInOut, l as LikeC4ViewModel, m as Y, n as DeployedInstanceModel, o as ElementModel, p as X, q as sortDeepestFirst, r as DeploymentNodeModel, t as $, u as NodeModel, y as findAscendingConnections, z as isNestedElementOfDeployedInstanceModel } from "./model-DBSABTjR.js";
import "./@likec4_core_model.js";
import { DefaultMap as se, Stack as fe } from "./@likec4_core_utils.js";
//#endregion
//#region node_modules/likec4/dist/chunks/rolldown-runtime.mjs
var import_browser_external_node_module = (/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Object.create(new Proxy({}, { get(_, key) {
		if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") console.warn(`Module "node:module" has been externalized for browser compatibility. Cannot access "node:module.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
	} }));
})))();
var n$6 = Object.defineProperty;
var __name = (e, t) => n$6(e, `name`, {
	value: t,
	configurable: !0
});
(0, import_browser_external_node_module.createRequire)(import.meta.url);
//#endregion
//#region node_modules/likec4/dist/chunks/libs/remeda.mjs
function e$20(s, c, l) {
	let r = (l) => s(l, ...c);
	return l === void 0 ? r : Object.assign(r, {
		lazy: l,
		lazyArgs: c
	});
}
__name(e$20, `e`);
function t$37(s, c, l) {
	let u = s.length - c.length;
	if (u === 0) return s(...c);
	if (u === 1) return e$20(s, c, l);
	throw Error(`Wrong number of arguments`);
}
__name(t$37, `t`);
function t$36(...s) {
	return t$37(c$1, s);
}
__name(t$36, `t`);
var c$1 = __name((s, c) => c.some((c) => c(s)), `n`);
function t$35(...s) {
	return t$37(n$35, s);
}
__name(t$35, `t`);
function n$35(s, c) {
	if (c < 1) throw RangeError(`chunk: A chunk size of '${c.toString()}' would result in an infinite array`);
	if (s.length === 0) return [];
	if (c >= s.length) return [[...s]];
	let l = Math.ceil(s.length / c), u = Array(l);
	if (c === 1) for (let [c, l] of s.entries()) u[c] = [l];
	else for (let d = 0; d < l; d += 1) {
		let l = d * c;
		u[d] = s.slice(l, l + c);
	}
	return u;
}
__name(n$35, `n`);
function t$34(...s) {
	return t$37(l$1, s);
}
__name(t$34, `t`);
var l$1 = __name((s, { min: c, max: l }) => c !== void 0 && s < c ? c : l !== void 0 && s > l ? l : s, `n`);
function t$33(...s) {
	return t$37(u, s);
}
__name(t$33, `t`);
var u = __name((s, c) => [...s, ...c], `n`);
var d = {
	done: !0,
	hasNext: !1
};
var h = {
	done: !1,
	hasNext: !1
};
var g = __name(() => d, `n`);
var _ = __name((s) => ({
	hasNext: !0,
	next: s,
	done: !1
}), `r`);
function t$31(s, ...c) {
	let l = s, u = c.map((s) => `lazy` in s ? r$13(s) : void 0), d = 0;
	for (; d < c.length;) {
		if (u[d] === void 0 || !i$7(l)) {
			let s = c[d];
			l = s(l), d += 1;
			continue;
		}
		let s = [];
		for (let l = d; l < c.length; l++) {
			let c = u[l];
			if (c === void 0 || (s.push(c), c.isSingle)) break;
		}
		let h = [];
		for (let c of l) if (n$31(c, h, s)) break;
		let { isSingle: g } = s.at(-1);
		l = g ? h[0] : h, d += s.length;
	}
	return l;
}
__name(t$31, `t`);
function n$31(s, c, l) {
	if (l.length === 0) return c.push(s), !1;
	let u = s, d = h, g = !1;
	for (let [s, h] of l.entries()) {
		let { index: _, items: v } = h;
		if (v.push(u), d = h(u, _, v), h.index += 1, d.hasNext) {
			if (d.hasMany ?? !1) {
				for (let u of d.next) if (n$31(u, c, l.slice(s + 1))) return !0;
				return g;
			}
			u = d.next;
		}
		if (!d.hasNext) break;
		d.done && (g = !0);
	}
	return d.hasNext && c.push(u), g;
}
__name(n$31, `n`);
function r$13(s) {
	let { lazy: c, lazyArgs: l } = s, u = c(...l);
	return Object.assign(u, {
		isSingle: c.single ?? !1,
		index: 0,
		items: []
	});
}
__name(r$13, `r`);
function i$7(s) {
	return typeof s == `string` || typeof s == `object` && !!s && Symbol.iterator in s;
}
__name(i$7, `i`);
function t$30(s, c) {
	let l = c.length - s.length;
	if (l === 1) {
		let [l, ...u] = c;
		return t$31(l, {
			lazy: s,
			lazyArgs: u
		});
	}
	if (l === 0) {
		let l = {
			lazy: s,
			lazyArgs: c
		};
		return Object.assign((s) => t$31(s, l), l);
	}
	throw Error(`Wrong number of arguments`);
}
__name(t$30, `t`);
function r$12(...s) {
	return t$30(i$6, s);
}
__name(r$12, `r`);
function i$6(s) {
	if (s.length === 0) return _;
	let c = /* @__PURE__ */ new Map();
	for (let l of s) c.set(l, (c.get(l) ?? 0) + 1);
	return (s) => {
		let l = c.get(s);
		return l === void 0 || l === 0 ? {
			done: !1,
			hasNext: !0,
			next: s
		} : (c.set(s, l - 1), h);
	};
}
__name(i$6, `i`);
function t$29(...s) {
	return t$37(v, s);
}
__name(t$29, `t`);
var v = __name((s, c) => s.length >= c, `n`);
function t$28(...s) {
	return t$37(Object.entries, s);
}
__name(t$28, `t`);
function n$29(...s) {
	return t$37(y, s, b);
}
__name(n$29, `n`);
var y = __name((s, c) => s.filter(c), `r`);
var b = __name((s) => (c, l, u) => s(c, l, u) ? {
	done: !1,
	hasNext: !0,
	next: c
} : h, `i`);
var x = __name((s) => Object.assign(s, { single: !0 }), `e`);
function r$10(...s) {
	return t$37(S, s, x(C));
}
__name(r$10, `r`);
var S = __name((s, c) => s.find(c), `i`);
var C = __name((s) => (c, l, u) => s(c, l, u) ? {
	done: !0,
	hasNext: !0,
	next: c
} : h, `a`);
function t$27(...s) {
	return t$37(w, s);
}
__name(t$27, `t`);
var w = __name((s, c) => {
	for (let l = s.length - 1; l >= 0; l--) {
		let u = s[l];
		if (c(u, l, s)) return u;
	}
}, `n`);
function n$27(...s) {
	return t$37(T, s, x(E$1));
}
__name(n$27, `n`);
var T = __name(([s]) => s, `r`);
var E$1 = __name(() => D$1, `i`);
var D$1 = __name((s) => ({
	hasNext: !0,
	next: s,
	done: !0
}), `a`);
function n$26(s, c) {
	return typeof s == `object` ? O(s, c) : e$20(O, s === void 0 ? [] : [s], k);
}
__name(n$26, `n`);
var O = __name((s, c) => c === void 0 ? s.flat() : s.flat(c), `r`);
var k = __name((s) => s === void 0 || s === 1 ? A : s <= 0 ? _ : (c) => Array.isArray(c) ? {
	next: c.flat(s - 1),
	hasNext: !0,
	hasMany: !0,
	done: !1
} : {
	next: c,
	hasNext: !0,
	done: !1
}, `i`);
var A = __name((s) => Array.isArray(s) ? {
	next: s,
	hasNext: !0,
	hasMany: !0,
	done: !1
} : {
	next: s,
	hasNext: !0,
	done: !1
}, `a`);
function t$26(...s) {
	return t$37(j, s, M);
}
__name(t$26, `t`);
var j = __name((s, c) => s.flatMap(c), `n`);
var M = __name((s) => (c, l, u) => {
	let d = s(c, l, u);
	return Array.isArray(d) ? {
		done: !1,
		hasNext: !0,
		hasMany: !0,
		next: d
	} : {
		done: !1,
		hasNext: !0,
		next: d
	};
}, `r`);
function t$25(...s) {
	return t$37(n$24, s, N);
}
__name(t$25, `t`);
function n$24(s, c) {
	return s.forEach(c), s;
}
__name(n$24, `n`);
var N = __name((s) => (c, l, u) => (s(c, l, u), {
	done: !1,
	hasNext: !0,
	next: c
}), `r`);
function t$24(...s) {
	return t$37(n$23, s);
}
__name(t$24, `t`);
function n$23(s, c) {
	for (let [l, u] of Object.entries(s)) c(u, l, s);
	return s;
}
__name(n$23, `n`);
var P = Symbol(`funnel/voidReducer`);
var F = __name(() => P, `t`);
function n$22(s, { triggerAt: c = `end`, minQuietPeriodMs: l, maxBurstDurationMs: u, minGapMs: d, reducer: h = F }) {
	let g, _, v, y, f = () => {
		let c = v;
		c !== void 0 && (v = void 0, c === P ? s() : s(c), d !== void 0 && (_ = setTimeout(p, d)));
	}, p = () => {
		clearTimeout(_), _ = void 0, g === void 0 && f();
	}, m = () => {
		clearTimeout(g), g = void 0, y = void 0, _ === void 0 && f();
	};
	return {
		call: (...s) => {
			let b = g === void 0 && _ === void 0;
			if ((c !== `start` || b) && (v = h(v, ...s)), !(g === void 0 && !b)) {
				if (l !== void 0 || u !== void 0 || d === void 0) {
					clearTimeout(g);
					let s = Date.now();
					y ??= s;
					let c = u === void 0 ? l ?? 0 : Math.min(l ?? u, Math.max(0, u - (s - y)));
					g = setTimeout(m, c);
				}
				c !== `end` && b && f();
			}
		},
		cancel: () => {
			clearTimeout(g), g = void 0, y = void 0, clearTimeout(_), _ = void 0, v = void 0;
		},
		flush: () => {
			m(), p();
		},
		get isIdle() {
			return g === void 0 && _ === void 0;
		}
	};
}
__name(n$22, `n`);
function t$22(...s) {
	return t$37(I, s);
}
__name(t$22, `t`);
var I = __name((s, c) => {
	let l = Object.create(null);
	for (let u = 0; u < s.length; u++) {
		let d = s[u], h = c(d, u, s);
		if (h !== void 0) {
			let s = l[h];
			s === void 0 ? l[h] = [d] : s.push(d);
		}
	}
	return Object.setPrototypeOf(l, Object.prototype), l;
}, `n`);
function t$21(...s) {
	return t$37(n$20, s);
}
__name(t$21, `t`);
function n$20(s, c) {
	let l = Object.create(null);
	for (let u of s) {
		let s = u?.[c];
		if (s !== void 0) {
			let c = l[s];
			c === void 0 ? l[s] = [u] : c.push(u);
		}
	}
	return Object.setPrototypeOf(l, Object.prototype), l;
}
__name(n$20, `n`);
function t$20(...s) {
	return t$37(n$19, s);
}
__name(t$20, `t`);
function n$19(s, c) {
	if (s === c || Object.is(s, c)) return !0;
	if (typeof s != `object` || typeof c != `object` || s === null || c === null || !r$5(s, c)) return !1;
	if (Array.isArray(s)) return i$1(s, c);
	if (s instanceof Map) return a$1(s, c);
	if (s instanceof Set) return o$1(s, c);
	if (s instanceof Date) return s.getTime() === c.getTime();
	if (s instanceof RegExp) return s.toString() === c.toString();
	if (Object.keys(s).length !== Object.keys(c).length) return !1;
	for (let [l, u] of Object.entries(s)) if (!(l in c) || !n$19(u, c[l])) return !1;
	return !0;
}
__name(n$19, `n`);
function r$5(s, c) {
	let l = Object.getPrototypeOf(s), u = Object.getPrototypeOf(c);
	return l === u ? !0 : l === null ? u === Object.prototype : l === Object.prototype && u === null;
}
__name(r$5, `r`);
function i$1(s, c) {
	if (s.length !== c.length) return !1;
	for (let [l, u] of s.entries()) if (!n$19(u, c[l])) return !1;
	return !0;
}
__name(i$1, `i`);
function a$1(s, c) {
	if (s.size !== c.size) return !1;
	for (let [l, u] of s.entries()) if (!c.has(l) || !n$19(u, c.get(l))) return !1;
	return !0;
}
function o$1(s, c) {
	if (s.size !== c.size) return !1;
	let l = [...c];
	for (let c of s) {
		let s = !1;
		for (let [u, d] of l.entries()) if (n$19(c, d)) {
			s = !0, l.splice(u, 1);
			break;
		}
		if (!s) return !1;
	}
	return !0;
}
function e$16() {
	return L;
}
__name(e$16, `e`);
var L = __name((s) => s, `t`);
function t$18(...s) {
	return t$37(n$18, s);
}
__name(t$18, `t`);
function n$18(s, c) {
	let l = {};
	for (let [u, d] of s.entries()) {
		let h = c(d, u, s);
		l[h] = d;
	}
	return l;
}
__name(n$18, `n`);
function e$15(s) {
	return Array.isArray(s);
}
__name(e$15, `e`);
function e$14(s) {
	return typeof s == `boolean`;
}
__name(e$14, `e`);
function e$13(s) {
	return s !== void 0;
}
__name(e$13, `e`);
function e$12(s) {
	return s === `` || s === void 0 ? !0 : Array.isArray(s) ? s.length === 0 : Object.keys(s).length === 0;
}
__name(e$12, `e`);
function e$11(s) {
	if (s == null || s === ``) return !0;
	if (typeof s != `object`) return !1;
	if (`length` in s && typeof s.length == `number`) return s.length === 0;
	if (`size` in s && typeof s.size == `number`) return s.size === 0;
	for (let c in s) return !1;
	return Object.getOwnPropertySymbols(s).length === 0;
}
__name(e$11, `e`);
__name((s) => typeof s == `function`, `e`);
function e$9(s) {
	return s != null;
}
__name(e$9, `e`);
function e$8(s) {
	return (c) => !s(c);
}
__name(e$8, `e`);
function e$7(s) {
	return s == null;
}
__name(e$7, `e`);
function e$6(s) {
	return typeof s == `number` && !Number.isNaN(s);
}
__name(e$6, `e`);
function e$5(s) {
	return typeof s == `object` && !!s;
}
__name(e$5, `e`);
function e$4(s) {
	return s instanceof Promise;
}
__name(e$4, `e`);
function e$3(s) {
	return typeof s == `string`;
}
__name(e$3, `e`);
function e$2(s) {
	return !!s;
}
__name(e$2, `e`);
function t$17(...s) {
	return t$37(z, s);
}
__name(t$17, `t`);
var z = __name((s, c) => s.join(c), `n`);
function t$16(...s) {
	return t$37(Object.keys, s);
}
__name(t$16, `t`);
function t$15(...s) {
	return t$37(B, s);
}
__name(t$15, `t`);
var B = __name((s) => s.at(-1), `n`);
function t$14(...s) {
	return t$37(V, s, H);
}
__name(t$14, `t`);
var V = __name((s, c) => s.map(c), `n`);
var H = __name((s) => (c, l, u) => ({
	done: !1,
	hasNext: !0,
	next: s(c, l, u)
}), `r`);
function t$13(...s) {
	return t$37(n$14, s);
}
__name(t$13, `t`);
function n$14(s, c) {
	let l = {};
	for (let [u, d] of s.entries()) {
		let [h, g] = c(d, u, s);
		l[h] = g;
	}
	return l;
}
__name(n$14, `n`);
function t$12(...s) {
	return t$37(n$13, s);
}
__name(t$12, `t`);
function n$13(s, c) {
	let l = {};
	for (let [u, d] of Object.entries(s)) l[u] = c(d, u, s);
	return l;
}
__name(n$13, `n`);
function n$12(...s) {
	return t$37(r$3, s);
}
__name(n$12, `n`);
function r$3(s, c) {
	if (!t$29(c, 1)) return { ...s };
	if (!t$29(c, 2)) {
		let { [c[0]]: l, ...u } = s;
		return u;
	}
	let l = { ...s };
	for (let s of c) delete l[s];
	return l;
}
__name(r$3, `r`);
function t$11(...s) {
	return t$37(n$11, s);
}
__name(t$11, `t`);
function n$11(s, c) {
	let l = { ...s };
	for (let [u, d] of Object.entries(l)) c(d, u, s) && delete l[u];
	return l;
}
__name(n$11, `n`);
function e$1(s) {
	let c = !1, l;
	return () => (c ||= (l = s(), !0), l);
}
__name(e$1, `e`);
function t$10(...s) {
	return t$37(U, s);
}
__name(t$10, `t`);
var U = __name((s) => s.length === 1 ? s[0] : void 0, `n`);
function t$9(...s) {
	return t$37(W, s);
}
__name(t$9, `t`);
var W = __name((s, c) => {
	let l = [[], []];
	for (let [u, d] of s.entries()) c(d, u, s) ? l[0].push(d) : l[1].push(d);
	return l;
}, `n`);
function t$8(...s) {
	return t$37(n$8, s);
}
__name(t$8, `t`);
function n$8(s, c) {
	let l = {};
	for (let [u, d] of Object.entries(s)) c(d, u, s) && (l[u] = d);
	return l;
}
__name(n$8, `n`);
function t$7(...s) {
	return (c) => t$31(c, ...s);
}
__name(t$7, `t`);
function t$6(s, ...c) {
	let l = s;
	for (let s of c) {
		if (l == null) return;
		l = l[s];
	}
	return l;
}
__name(t$6, `t`);
function t$5(...s) {
	return t$37(n$7, s);
}
__name(t$5, `t`);
function n$7(s) {
	let c = [];
	for (let l = 0; l < s; l++) {
		let s = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789`[Math.floor(Math.random() * 62)];
		c.push(s);
	}
	return c.join(``);
}
__name(n$7, `n`);
function t$4(...s) {
	return t$37(G, s);
}
__name(t$4, `t`);
var G = __name((s, c, l) => s.reduce(c, l), `n`);
function t$3(...s) {
	return t$37(n$5, s);
}
__name(t$3, `t`);
function n$5(s) {
	return [...s].reverse();
}
__name(n$5, `n`);
function t$2(...s) {
	return t$37(n$4, s);
}
__name(t$2, `t`);
function n$4(s, c) {
	let l = [...s];
	return l.sort(c), l;
}
__name(n$4, `n`);
function n$3(...s) {
	return t$37(K, s, i$2);
}
__name(n$3, `n`);
var K = __name((s, c) => c < 0 ? [] : s.slice(0, c), `r`);
function i$2(s) {
	if (s <= 0) return g;
	let c = s;
	return (s) => (--c, {
		done: c <= 0,
		hasNext: !0,
		next: s
	});
}
function t$1(...s) {
	return t$37(n$2, s);
}
__name(t$1, `t`);
function n$2(s, c) {
	return c(s), s;
}
__name(n$2, `n`);
function n$1(...s) {
	return t$30(r$1, s);
}
__name(n$1, `n`);
function r$1() {
	let s = /* @__PURE__ */ new Set();
	return (c) => s.has(c) ? h : (s.add(c), {
		done: !1,
		hasNext: !0,
		next: c
	});
}
__name(r$1, `r`);
function t$19(...s) {
	return t$37(Object.values, s);
}
//#endregion
//#region node_modules/likec4/dist/chunks/libs/@lume/kiwi.mjs
function createMap() {
	return new e();
}
var e = class IndexedMap {
	index = {};
	array = [];
	size() {
		return this.array.length;
	}
	empty() {
		return this.array.length === 0;
	}
	itemAt(e) {
		return this.array[e];
	}
	contains(e) {
		return this.index[e.id()] !== void 0;
	}
	find(e) {
		let t = this.index[e.id()];
		return t === void 0 ? void 0 : this.array[t];
	}
	setDefault(e, n) {
		let r = this.index[e.id()];
		if (r === void 0) {
			let r = new t(e, n());
			return this.index[e.id()] = this.array.length, this.array.push(r), r;
		} else return this.array[r];
	}
	insert(e, n) {
		let r = new t(e, n), i = this.index[e.id()];
		return i === void 0 ? (this.index[e.id()] = this.array.length, this.array.push(r)) : this.array[i] = r, r;
	}
	erase(e) {
		let t = this.index[e.id()];
		if (t === void 0) return;
		this.index[e.id()] = void 0;
		let n = this.array[t], r = this.array.pop();
		return n !== r && (this.array[t] = r, this.index[r.first.id()] = t), n;
	}
	copy() {
		let e = new IndexedMap();
		for (let t = 0; t < this.array.length; t++) {
			let n = this.array[t].copy();
			e.array[t] = n, e.index[n.first.id()] = t;
		}
		return e;
	}
};
var t = class Pair {
	first;
	second;
	constructor(e, t) {
		this.first = e, this.second = t;
	}
	copy() {
		return new Pair(this.first, this.second);
	}
};
var Variable = class {
	constructor(e = ``) {
		this._name = e;
	}
	id() {
		return this._id;
	}
	name() {
		return this._name;
	}
	setName(e) {
		this._name = e;
	}
	context() {
		return this._context;
	}
	setContext(e) {
		this._context = e;
	}
	value() {
		return this._value;
	}
	setValue(e) {
		this._value = e;
	}
	plus(e) {
		return new r(this, e);
	}
	minus(e) {
		return new r(this, typeof e == `number` ? -e : [-1, e]);
	}
	multiply(e) {
		return new r([e, this]);
	}
	divide(e) {
		return new r([1 / e, this]);
	}
	toJSON() {
		return {
			name: this._name,
			value: this._value
		};
	}
	toString() {
		return this._context + `[` + this._name + `:` + this._value + `]`;
	}
	_name;
	_value = 0;
	_context = null;
	_id = n++;
};
var n = 0;
var r = class Expression {
	constructor() {
		let e = parseArgs(arguments);
		this._terms = e.terms, this._constant = e.constant;
	}
	terms() {
		return this._terms;
	}
	constant() {
		return this._constant;
	}
	value() {
		let e = this._constant;
		for (let t = 0, n = this._terms.size(); t < n; t++) {
			let n = this._terms.itemAt(t);
			e += n.first.value() * n.second;
		}
		return e;
	}
	plus(e) {
		return new Expression(this, e);
	}
	minus(e) {
		return new Expression(this, typeof e == `number` ? -e : [-1, e]);
	}
	multiply(e) {
		return new Expression([e, this]);
	}
	divide(e) {
		return new Expression([1 / e, this]);
	}
	isConstant() {
		return this._terms.size() == 0;
	}
	toString() {
		let e = this._terms.array.map(function(e) {
			return e.second + `*` + e.first.toString();
		}).join(` + `);
		return !this.isConstant() && this._constant !== 0 && (e += ` + `), e += this._constant, e;
	}
	_terms;
	_constant;
};
function parseArgs(e) {
	let t = 0, factory = () => 0, n = createMap();
	for (let i = 0, a = e.length; i < a; ++i) {
		let a = e[i];
		if (typeof a == `number`) t += a;
		else if (a instanceof Variable) n.setDefault(a, factory).second += 1;
		else if (a instanceof r) {
			t += a.constant();
			let e = a.terms();
			for (let t = 0, r = e.size(); t < r; t++) {
				let r = e.itemAt(t);
				n.setDefault(r.first, factory).second += r.second;
			}
		} else if (a instanceof Array) {
			if (a.length !== 2) throw Error(`array must have length 2`);
			let e = a[0], i = a[1];
			if (typeof e != `number`) throw Error(`array item 0 must be a number`);
			if (i instanceof Variable) n.setDefault(i, factory).second += e;
			else if (i instanceof r) {
				t += i.constant() * e;
				let r = i.terms();
				for (let t = 0, i = r.size(); t < i; t++) {
					let i = r.itemAt(t);
					n.setDefault(i.first, factory).second += i.second * e;
				}
			} else throw Error(`array item 1 must be a variable or expression`);
		} else throw Error(`invalid Expression argument: ` + a);
	}
	return {
		terms: n,
		constant: t
	};
}
var i = class Strength {
	static create(e, t, n, r = 1) {
		let i = 0;
		return i += Math.max(0, Math.min(1e3, e * r)) * 1e6, i += Math.max(0, Math.min(1e3, t * r)) * 1e3, i += Math.max(0, Math.min(1e3, n * r)), i;
	}
	static required = Strength.create(1e3, 1e3, 1e3);
	static strong = Strength.create(1, 0, 0);
	static medium = Strength.create(0, 1, 0);
	static weak = Strength.create(0, 0, 1);
	static clip(e) {
		return Math.max(0, Math.min(Strength.required, e));
	}
};
var a;
(function(e) {
	e[e.Le = 0] = `Le`, e[e.Ge = 1] = `Ge`, e[e.Eq = 2] = `Eq`;
})(a ||= {});
var Constraint = class {
	constructor(e, t, n, a = i.required) {
		this._operator = t, this._strength = i.clip(a), n === void 0 && e instanceof r ? this._expression = e : this._expression = e.minus(n);
	}
	id() {
		return this._id;
	}
	expression() {
		return this._expression;
	}
	op() {
		return this._operator;
	}
	strength() {
		return this._strength;
	}
	toString() {
		return this._expression.toString() + ` ` + [
			`<=`,
			`>=`,
			`=`
		][this._operator] + ` 0 (` + this._strength.toString() + `)`;
	}
	_expression;
	_operator;
	_strength;
	_id = o++;
};
var o = 0;
var Solver = class {
	maxIterations = 1e3;
	constructor() {}
	createConstraint(e, t, n, r = i.required) {
		let a = new Constraint(e, t, n, r);
		return this.addConstraint(a), a;
	}
	addConstraint(e) {
		if (this._cnMap.find(e) !== void 0) throw Error(`duplicate constraint`);
		let t = this._createRow(e), n = t.row, r = t.tag, i = this._chooseSubject(n, r);
		if (i.type() === s.Invalid && n.allDummies()) if (nearZero(n.constant())) i = r.marker;
		else throw Error(`unsatisfiable constraint`);
		if (i.type() === s.Invalid) {
			if (!this._addWithArtificialVariable(n)) throw Error(`unsatisfiable constraint`);
		} else n.solveFor(i), this._substitute(i, n), this._rowMap.insert(i, n);
		this._cnMap.insert(e, r), this._optimize(this._objective);
	}
	removeConstraint(e) {
		let t = this._cnMap.erase(e);
		if (t === void 0) throw Error(`unknown constraint`);
		this._removeConstraintEffects(e, t.second);
		let n = t.second.marker, r = this._rowMap.erase(n);
		if (r === void 0) {
			let e = this._getMarkerLeavingSymbol(n);
			if (e.type() === s.Invalid) throw Error(`failed to find leaving row`);
			r = this._rowMap.erase(e), r.second.solveForEx(e, n), this._substitute(n, r.second);
		}
		this._optimize(this._objective);
	}
	hasConstraint(e) {
		return this._cnMap.contains(e);
	}
	getConstraints() {
		return this._cnMap.array.map(({ first: e }) => e);
	}
	addEditVariable(e, t) {
		if (this._editMap.find(e) !== void 0) throw Error(`duplicate edit variable`);
		if (t = i.clip(t), t === i.required) throw Error(`bad required strength`);
		let n = new Constraint(new r(e), a.Eq, void 0, t);
		this.addConstraint(n);
		let o = {
			tag: this._cnMap.find(n).second,
			constraint: n,
			constant: 0
		};
		this._editMap.insert(e, o);
	}
	removeEditVariable(e) {
		let t = this._editMap.erase(e);
		if (t === void 0) throw Error(`unknown edit variable`);
		this.removeConstraint(t.second.constraint);
	}
	hasEditVariable(e) {
		return this._editMap.contains(e);
	}
	suggestValue(e, t) {
		let n = this._editMap.find(e);
		if (n === void 0) throw Error(`unknown edit variable`);
		let r = this._rowMap, i = n.second, a = t - i.constant;
		i.constant = t;
		let o = i.tag.marker, c = r.find(o);
		if (c !== void 0) {
			c.second.add(-a) < 0 && this._infeasibleRows.push(o), this._dualOptimize();
			return;
		}
		let l = i.tag.other;
		if (c = r.find(l), c !== void 0) {
			c.second.add(a) < 0 && this._infeasibleRows.push(l), this._dualOptimize();
			return;
		}
		for (let e = 0, t = r.size(); e < t; ++e) {
			let t = r.itemAt(e), n = t.second, i = n.coefficientFor(o);
			i !== 0 && n.add(a * i) < 0 && t.first.type() !== s.External && this._infeasibleRows.push(t.first);
		}
		this._dualOptimize();
	}
	updateVariables() {
		let e = this._varMap, t = this._rowMap;
		for (let n = 0, r = e.size(); n < r; ++n) {
			let r = e.itemAt(n), i = t.find(r.second);
			i === void 0 ? r.first.setValue(0) : r.first.setValue(i.second.constant());
		}
	}
	_getVarSymbol(e) {
		let factory = () => this._makeSymbol(s.External);
		return this._varMap.setDefault(e, factory).second;
	}
	_createRow(e) {
		let t = e.expression(), n = new l(t.constant()), r = t.terms();
		for (let e = 0, t = r.size(); e < t; ++e) {
			let t = r.itemAt(e);
			if (!nearZero(t.second)) {
				let e = this._getVarSymbol(t.first), r = this._rowMap.find(e);
				r === void 0 ? n.insertSymbol(e, t.second) : n.insertRow(r.second, t.second);
			}
		}
		let o = this._objective, u = e.strength(), d = {
			marker: c,
			other: c
		};
		switch (e.op()) {
			case a.Le:
			case a.Ge: {
				let t = e.op() === a.Le ? 1 : -1, r = this._makeSymbol(s.Slack);
				if (d.marker = r, n.insertSymbol(r, t), u < i.required) {
					let e = this._makeSymbol(s.Error);
					d.other = e, n.insertSymbol(e, -t), o.insertSymbol(e, u);
				}
				break;
			}
			case a.Eq:
				if (u < i.required) {
					let e = this._makeSymbol(s.Error), t = this._makeSymbol(s.Error);
					d.marker = e, d.other = t, n.insertSymbol(e, -1), n.insertSymbol(t, 1), o.insertSymbol(e, u), o.insertSymbol(t, u);
				} else {
					let e = this._makeSymbol(s.Dummy);
					d.marker = e, n.insertSymbol(e);
				}
				break;
		}
		return n.constant() < 0 && n.reverseSign(), {
			row: n,
			tag: d
		};
	}
	_chooseSubject(e, t) {
		let n = e.cells();
		for (let e = 0, t = n.size(); e < t; ++e) {
			let t = n.itemAt(e);
			if (t.first.type() === s.External) return t.first;
		}
		let r = t.marker.type();
		return (r === s.Slack || r === s.Error) && e.coefficientFor(t.marker) < 0 ? t.marker : (r = t.other.type(), (r === s.Slack || r === s.Error) && e.coefficientFor(t.other) < 0 ? t.other : c);
	}
	_addWithArtificialVariable(e) {
		let t = this._makeSymbol(s.Slack);
		this._rowMap.insert(t, e.copy()), this._artificial = e.copy(), this._optimize(this._artificial);
		let n = nearZero(this._artificial.constant());
		this._artificial = null;
		let r = this._rowMap.erase(t);
		if (r !== void 0) {
			let e = r.second;
			if (e.isConstant()) return n;
			let i = this._anyPivotableSymbol(e);
			if (i.type() === s.Invalid) return !1;
			e.solveForEx(t, i), this._substitute(i, e), this._rowMap.insert(i, e);
		}
		let i = this._rowMap;
		for (let e = 0, n = i.size(); e < n; ++e) i.itemAt(e).second.removeSymbol(t);
		return this._objective.removeSymbol(t), n;
	}
	_substitute(e, t) {
		let n = this._rowMap;
		for (let r = 0, i = n.size(); r < i; ++r) {
			let i = n.itemAt(r);
			i.second.substitute(e, t), i.second.constant() < 0 && i.first.type() !== s.External && this._infeasibleRows.push(i.first);
		}
		this._objective.substitute(e, t), this._artificial && this._artificial.substitute(e, t);
	}
	_optimize(e) {
		let t = 0;
		for (; t < this.maxIterations;) {
			let n = this._getEnteringSymbol(e);
			if (n.type() === s.Invalid) return;
			let r = this._getLeavingSymbol(n);
			if (r.type() === s.Invalid) throw Error(`the objective is unbounded`);
			let i = this._rowMap.erase(r).second;
			i.solveForEx(r, n), this._substitute(n, i), this._rowMap.insert(n, i), t++;
		}
		throw Error(`solver iterations exceeded`);
	}
	_dualOptimize() {
		let e = this._rowMap, t = this._infeasibleRows;
		for (; t.length !== 0;) {
			let n = t.pop(), r = e.find(n);
			if (r !== void 0 && r.second.constant() < 0) {
				let t = this._getDualEnteringSymbol(r.second);
				if (t.type() === s.Invalid) throw Error(`dual optimize failed`);
				let i = r.second;
				e.erase(n), i.solveForEx(n, t), this._substitute(t, i), e.insert(t, i);
			}
		}
	}
	_getEnteringSymbol(e) {
		let t = e.cells();
		for (let e = 0, n = t.size(); e < n; ++e) {
			let n = t.itemAt(e), r = n.first;
			if (n.second < 0 && r.type() !== s.Dummy) return r;
		}
		return c;
	}
	_getDualEnteringSymbol(e) {
		let t = Number.MAX_VALUE, n = c, r = e.cells();
		for (let e = 0, i = r.size(); e < i; ++e) {
			let i = r.itemAt(e), a = i.first, o = i.second;
			if (o > 0 && a.type() !== s.Dummy) {
				let e = this._objective.coefficientFor(a) / o;
				e < t && (t = e, n = a);
			}
		}
		return n;
	}
	_getLeavingSymbol(e) {
		let t = Number.MAX_VALUE, n = c, r = this._rowMap;
		for (let i = 0, a = r.size(); i < a; ++i) {
			let a = r.itemAt(i), o = a.first;
			if (o.type() !== s.External) {
				let r = a.second, i = r.coefficientFor(e);
				if (i < 0) {
					let e = -r.constant() / i;
					e < t && (t = e, n = o);
				}
			}
		}
		return n;
	}
	_getMarkerLeavingSymbol(e) {
		let t = Number.MAX_VALUE, n = t, r = t, i = c, a = i, o = i, l = i, u = this._rowMap;
		for (let t = 0, i = u.size(); t < i; ++t) {
			let i = u.itemAt(t), c = i.second, d = c.coefficientFor(e);
			if (d === 0) continue;
			let f = i.first;
			if (f.type() === s.External) l = f;
			else if (d < 0) {
				let e = -c.constant() / d;
				e < n && (n = e, a = f);
			} else {
				let e = c.constant() / d;
				e < r && (r = e, o = f);
			}
		}
		return a === i ? o === i ? l : o : a;
	}
	_removeConstraintEffects(e, t) {
		t.marker.type() === s.Error && this._removeMarkerEffects(t.marker, e.strength()), t.other.type() === s.Error && this._removeMarkerEffects(t.other, e.strength());
	}
	_removeMarkerEffects(e, t) {
		let n = this._rowMap.find(e);
		n === void 0 ? this._objective.insertSymbol(e, -t) : this._objective.insertRow(n.second, -t);
	}
	_anyPivotableSymbol(e) {
		let t = e.cells();
		for (let e = 0, n = t.size(); e < n; ++e) {
			let n = t.itemAt(e), r = n.first.type();
			if (r === s.Slack || r === s.Error) return n.first;
		}
		return c;
	}
	_makeSymbol(e) {
		return new Symbol$1(e, this._idTick++);
	}
	_cnMap = createCnMap();
	_rowMap = createRowMap();
	_varMap = createVarMap();
	_editMap = createEditMap();
	_infeasibleRows = [];
	_objective = new l();
	_artificial = null;
	_idTick = 0;
};
function nearZero(e) {
	let t = 1e-8;
	return e < 0 ? -e < t : e < t;
}
function createCnMap() {
	return createMap();
}
function createRowMap() {
	return createMap();
}
function createVarMap() {
	return createMap();
}
function createEditMap() {
	return createMap();
}
var s;
(function(e) {
	e[e.Invalid = 0] = `Invalid`, e[e.External = 1] = `External`, e[e.Slack = 2] = `Slack`, e[e.Error = 3] = `Error`, e[e.Dummy = 4] = `Dummy`;
})(s ||= {});
var Symbol$1 = class {
	constructor(e, t) {
		this._id = t, this._type = e;
	}
	id() {
		return this._id;
	}
	type() {
		return this._type;
	}
	_id;
	_type;
};
var c = new Symbol$1(s.Invalid, -1);
var l = class Row {
	constructor(e = 0) {
		this._constant = e;
	}
	cells() {
		return this._cellMap;
	}
	constant() {
		return this._constant;
	}
	isConstant() {
		return this._cellMap.empty();
	}
	allDummies() {
		let e = this._cellMap;
		for (let t = 0, n = e.size(); t < n; ++t) if (e.itemAt(t).first.type() !== s.Dummy) return !1;
		return !0;
	}
	copy() {
		let e = new Row(this._constant);
		return e._cellMap = this._cellMap.copy(), e;
	}
	add(e) {
		return this._constant += e;
	}
	insertSymbol(e, t = 1) {
		let n = this._cellMap.setDefault(e, () => 0);
		nearZero(n.second += t) && this._cellMap.erase(e);
	}
	insertRow(e, t = 1) {
		this._constant += e._constant * t;
		let n = e._cellMap;
		for (let e = 0, r = n.size(); e < r; ++e) {
			let r = n.itemAt(e);
			this.insertSymbol(r.first, r.second * t);
		}
	}
	removeSymbol(e) {
		this._cellMap.erase(e);
	}
	reverseSign() {
		this._constant = -this._constant;
		let e = this._cellMap;
		for (let t = 0, n = e.size(); t < n; ++t) {
			let n = e.itemAt(t);
			n.second = -n.second;
		}
	}
	solveFor(e) {
		let t = this._cellMap, n = -1 / t.erase(e).second;
		this._constant *= n;
		for (let e = 0, r = t.size(); e < r; ++e) t.itemAt(e).second *= n;
	}
	solveForEx(e, t) {
		this.insertSymbol(e, -1), this.solveFor(t);
	}
	coefficientFor(e) {
		let t = this._cellMap.find(e);
		return t === void 0 ? 0 : t.second;
	}
	substitute(e, t) {
		let n = this._cellMap.erase(e);
		n !== void 0 && this.insertRow(t, n.second);
	}
	_cellMap = createMap();
	_constant;
};
//#endregion
//#region node_modules/likec4/dist/chunks/sequence-view.mjs
function isPlainObject(e) {
	if (typeof e != `object` || !e) return !1;
	let t = Object.getPrototypeOf(e);
	return t !== null && t !== Object.prototype && Object.getPrototypeOf(t) !== null || Symbol.iterator in e ? !1 : Symbol.toStringTag in e ? Object.prototype.toString.call(e) === `[object Module]` : !0;
}
function _defu(e, t, n = `.`, r) {
	if (!isPlainObject(t)) return _defu(e, {}, n, r);
	let i = { ...t };
	for (let t of Object.keys(e)) {
		if (t === `__proto__` || t === `constructor`) continue;
		let a = e[t];
		a != null && (r && r(i, t, a, n) || (Array.isArray(a) && Array.isArray(i[t]) ? i[t] = [...a, ...i[t]] : isPlainObject(a) && isPlainObject(i[t]) ? i[t] = _defu(a, i[t], (n ? `${n}.` : ``) + t.toString(), r) : i[t] = a));
	}
	return i;
}
function createDefu(e) {
	return (...t) => t.reduce((t, n) => _defu(t, n, ``, e), {});
}
var E = createDefu();
var paddingFromNumber = (e) => ({
	left: e,
	right: e,
	top: e,
	bottom: e
});
function normalizeSpacing(e = 16) {
	return typeof e == `number` ? paddingFromNumber(e) : {
		left: e?.left ?? e?.x ?? 0,
		right: e?.right ?? e?.x ?? 0,
		top: e?.top ?? e?.y ?? 0,
		bottom: e?.bottom ?? e?.y ?? 0
	};
}
function rectFromSteps(e) {
	return invariant(e.length > 0), e.reduce((e, t) => {
		let n = [t.from.column, t.to.column], r = [t.from.row, t.to.row];
		return e.min.column = Math.min(e.min.column, ...n), e.min.row = Math.min(e.min.row, ...r), e.max.column = Math.max(e.max.column, ...n), e.max.row = Math.max(e.max.row, ...r), e;
	}, {
		min: {
			column: Infinity,
			row: Infinity
		},
		max: {
			column: -Infinity,
			row: -Infinity
		}
	});
}
function buildCompounds(e, t) {
	if (e.length === 0 || e.length === t.length) return [];
	let getNode = (e) => t.find((t) => t.id === e);
	function parentsLookup(e) {
		let t = e.parent && getNode(e.parent);
		return t ? [t, ...parentsLookup(t)] : [];
	}
	let n = new fe(), r = [];
	return e.forEach((e) => {
		let t = parentsLookup(e);
		if (t.length === 0) {
			n.clear();
			return;
		}
		let i, a = t[0];
		for (; i = n.peek()?.node;) {
			if (!a || i.id === a.id) {
				t.length = 0;
				break;
			}
			if (!isAncestor(i.id, a.id)) {
				n.pop();
				continue;
			}
			break;
		}
		for (; i = t.pop();) {
			let t = n.peek();
			if (t && (isAncestor(i.id, t.node.id) || i.id === t.node.id)) continue;
			let a = {
				node: i,
				from: e,
				to: e,
				nested: []
			};
			t ? t.nested.push(a) : r.push(a), n.push(a);
		}
		n.forEach((t) => t.to = e);
	}), r;
}
var D = {
	required: i.required,
	strong: i.strong,
	medium: i.medium,
	soft: i.create(0, .8, 1),
	weak: i.weak
};
var SequenceViewLayouter = class {
	#e;
	#t = new Solver();
	#n;
	#r = [];
	#i;
	#a;
	#o;
	#s = [];
	#c = /* @__PURE__ */ new Map();
	constructor({ actors: e, steps: t, compounds: n, flow: r, collapsedFlows: i }) {
		this.#t.maxIterations = 2e3, this.#e = r;
		let a = Math.max(...e.map((e) => e.height)) + 30;
		this.#o = this.newVar(a), this.constraint(this.#o, `==`, a, D.soft), this.constraint(this.#o, `>=`, a, D.weak), this.#i = this.newVar(0), this.#a = this.newVar(0), this.#n = this.addActors(e);
		for (let e of n) {
			let t = this.addCompound(e), n = t[0];
			this.constraint(n.y1, `==`, 0, D.strong), this.put(this.#o, D.strong).after(n.y2), this.#r.push(...t);
		}
		let [o, ...s] = this.#n;
		this.constraint(o.offset.left, `==`, 0, D.strong);
		let c = s.reduce((e, t) => (this.put(t.x).after(e.right, 80), this.put(e.offset.right, D.strong).before(t.offset.left, 50), this.constraint(t.centerY, `==`, e.centerY, D.strong), t), o);
		for (let e of t) this.addStep(e);
		this.createSubflowAreas(t, new Set(i)), this.constraint(this.#i, `==`, c.offset.right);
		let l = t$15(this.#s)?.inner.bottom ?? this.#o;
		if (this.put(this.#a, D.strong).after(l, 16), n.length > 0) for (let e of this.#r) {
			let t = e.from.column, n = e.to.column, r = Math.max(e.from.maxRow, e.to.maxRow);
			for (let e = t + 1; e < n; e++) {
				let t = this.actorBox(e);
				r = Math.max(r, t.maxRow);
			}
			let i = nonNullable(this.#s[r], `row ${r} not found`);
			this.put(e.bottom, D.strong).after(i.inner.bottom, 10);
		}
		this.#t.updateVariables();
	}
	getSubflowAreas() {
		return [...this.#c.entries()].map(([e, { x1: t, y1: n, x2: r, y2: i }]) => ({
			id: e,
			x: t.value(),
			y: n.value(),
			width: r.value() - t.value(),
			height: i.value() - n.value()
		}));
	}
	getActorBox(e) {
		let t = this.actorBox(e);
		return {
			x: t.x.value(),
			y: t.y.value(),
			width: t.width,
			height: t.height
		};
	}
	getCompoundBoxes() {
		return this.#r.map(({ node: e, depth: t, x1: n, y1: r, x2: i, bottom: a }) => ({
			node: e,
			depth: t,
			x: n.value(),
			y: r.value(),
			width: i.value() - n.value(),
			height: a.value() - r.value()
		}));
	}
	getPortCenter(e, t) {
		let { column: n, row: r } = t === `source` ? e.from : e.to, i = this.actorBox(n).centerX, { rowTop: a } = nonNullable(this.#s[r]);
		return e.isSelfLoop && t === `target` ? {
			cx: i.value(),
			cy: a.value(),
			height: 24
		} : {
			cx: i.value(),
			cy: a.value() + 40 + e.offset,
			height: t === `source` ? 40 : 24
		};
	}
	isStepCollapsed(e) {
		if (e.from.row === e.to.row) return this.#s[e.from.row]?.collapsed ?? !1;
		let t = Math.min(e.from.row, e.to.row), n = Math.min(e.from.row, e.to.row), r = !0;
		for (let e = t; e <= n && (r &&= this.#s[e]?.collapsed ?? !1, r); e++);
		return r;
	}
	getViewBounds() {
		return {
			x: 0,
			y: 0,
			width: this.#i.value(),
			height: this.#a.value()
		};
	}
	actorBox(e) {
		if (typeof e != `number`) {
			let t = typeof e == `string` ? e : e.id;
			e = this.#n.findIndex((e) => e.actor.id === t), invariant(e >= 0, `actor ${t} not found`);
		}
		return nonNullable(this.#n[e], `actor at index ${e} not found`);
	}
	addActors(e) {
		let t = 0;
		return t$14(e, (e, n) => {
			let r = this.newVar(t);
			t += e.width + 80;
			let i = this.newVar(0), a = {
				column: n,
				actor: e,
				x: r,
				y: i,
				centerX: r.plus(Math.round(e.width / 2)),
				centerY: i.plus(Math.round(e.height / 2)),
				width: e.width,
				height: e.height,
				right: r.plus(e.width),
				bottom: i.plus(e.height),
				minRow: Infinity,
				maxRow: -Infinity
			}, o = this.newVar(0), s = this.newVar(0), c = this.newVar(0), l = this.newVar(0);
			return this.put(i, D.strong).after(o), this.put(r, D.strong).after(s), this.put(a.right, D.strong).before(c), this.put(a.bottom, D.strong).before(l), {
				...a,
				offset: {
					top: o,
					left: s,
					right: c,
					bottom: l
				}
			};
		});
	}
	addStep(e) {
		let t = this.actorBox(e.source), n = this.actorBox(e.target);
		t.minRow = Math.min(t.minRow, e.from.row), t.maxRow = Math.max(t.maxRow, e.from.row), n.minRow = Math.min(n.minRow, e.to.row), n.maxRow = Math.max(n.maxRow, e.to.row);
		let [r, i] = t.column <= n.column ? [t, n] : [n, t], a = (e.label?.width ?? 100) + 50;
		Math.abs(n.column - t.column) <= 1 && (r === i ? this.constraint(r.centerX.plus(a), `<=`, r.offset.right) : this.constraint(r.centerX.plus(a), `<=`, i.centerX));
		let o = e.label?.height ? e.label.height + 50 + 24 : 100;
		return o = Math.max(o, 100) + e.offset, this.ensureRow(e.from.row, o), e.to.row !== e.from.row && this.ensureRow(e.to.row, 24), this;
	}
	addCompound(e) {
		let t = [], n = e.nested.flatMap((e) => {
			let n = this.addCompound(e);
			return t.push(n[0]), n;
		}), r = Math.max(...n.map((e) => e.depth + 1), 0), i = this.actorBox(e.from), a = this.actorBox(e.to), s = i.offset.left.minus(32);
		i.offset.left = s;
		let c = a.offset.right.plus(32);
		a.offset.right = c;
		let l = this.newVar(0), u = t$10(t), d, f;
		switch (!0) {
			case !!u:
				d = u.y1.minus(40), f = u.y2.plus(32), this.put(u.bottom).before(l, 32);
				break;
			case a === i:
				d = this.newVar(0), f = this.newVar(0), this.put(d).before(i.offset.top, 52), this.put(i.offset.bottom).before(f, 32), this.put(f).before(l);
				break;
			case t.length > 0:
				d = this.newVar(0), f = this.newVar(0);
				for (let e of t) this.put(d).before(e.y1, 32), this.put(e.y2).before(f, 32), this.put(e.bottom).before(l, 32);
				break;
			default:
				d = this.newVar(0), f = this.newVar(0);
				for (var p = i.column; p <= a.column; p++) {
					let e = this.actorBox(p).offset;
					this.put(d).before(e.top, 52), this.put(e.bottom).before(f, 32);
				}
				this.put(f).before(l);
				break;
		}
		for (var p = i.column; p <= a.column; p++) {
			let e = this.actorBox(p).offset;
			e.top = d, e.bottom = f;
		}
		return [{
			node: e.node,
			depth: r,
			from: i,
			to: a,
			x1: s,
			y1: d,
			x2: c,
			y2: f,
			bottom: l
		}, ...n];
	}
	ensureRow(e, t) {
		for (; e >= this.#s.length;) {
			let n = t$15(this.#s)?.outer.bottom || this.#o, r = this.newVar(n.value());
			this.put(r).after(n);
			let i = t;
			e !== this.#s.length && (i = 100);
			let a = new Variable();
			a.setValue(i), this.#t.addEditVariable(a, D.strong), this.#t.suggestValue(a, i);
			let o = this.newVar(r.value() + i);
			this.require(o).after(r), this.constraint(o.minus(r), `==`, a, D.strong);
			let s = this.newVar(o.value());
			this.put(s).after(o), this.#s.push({
				inner: {
					top: r,
					bottom: o
				},
				rowTop: r,
				outer: {
					top: n,
					bottom: s
				},
				height: a
			});
		}
		let n = nonNullable(this.#s[e]);
		return t > n.height.value() && (n.height.setValue(t), this.#t.suggestValue(n.height, t)), n;
	}
	collapseRect({ min: e, max: t }) {
		for (let n = e.row; n <= t.row; n++) {
			let e = nonNullable(this.#s[n], `Row ${n} not found`);
			e.collapsed = !0, e.height.setValue(0), this.#t.suggestValue(e.height, 0);
		}
	}
	getRectBounds({ min: e, max: t }) {
		let n = this.#s[e.row], r = this.#s[t.row];
		return invariant(n && r, `Subflow box invalid minRow=${e.row} maxRow=${t.row}`), {
			x1: this.actorBox(e.column).centerX,
			x2: this.actorBox(t.column).centerX,
			get top() {
				return n.inner.top;
			},
			set top(e) {
				n.inner.top = e;
			},
			get bottom() {
				return r.inner.bottom;
			},
			set bottom(e) {
				r.inner.bottom = e;
			},
			minY: n.outer.top,
			maxY: r.outer.bottom
		};
	}
	wrapAroundRect(e, t) {
		let n = normalizeSpacing(t ?? 16), r = this.getRectBounds(e), i = this.newVar(0);
		this.put(i).before(r.x1, n.left);
		let a = this.newVar(0);
		this.put(a).after(r.x2, n.right);
		let o = this.newVar(r.minY.value());
		this.require(o).after(r.minY), this.put(o).before(r.top, n.top);
		let s = this.newVar(r.maxY.value());
		return this.put(s).after(r.bottom, n.bottom), this.require(s).before(r.maxY), r.top = o, r.bottom = s, {
			x1: i,
			y1: o,
			x2: a,
			y2: s
		};
	}
	createSubflowAreas(r, a) {
		let selectSteps = (e) => n$29(r, (t) => t.id.startsWith(e)), o = {
			top: 30,
			right: 40,
			bottom: 10,
			left: 90
		}, fitSubflows = ({ area: r, visited: a, stretch: s = !1, padding: c }) => {
			let u = t$31(a, n$29(Z.isSubFlow), t$26((e) => this.#c.get(e.id) ?? []));
			if (!t$29(u, 1)) return;
			let f = E(c, o), p = n$27(u), m = t$15(u);
			p.min.row == r.min.row && this.require(r.y1).before(p.y1, f.top), m.max.row == r.max.row && this.require(m.y2).before(r.y2, f.bottom);
			for (let e of u) this.require(e.x1).after(r.x1, f.left), this.require(e.x2).before(r.x2, f.right), s && (e.x1 = f.left > 0 ? r.x1.plus(f.left) : r.x1, e.x2 = f.right > 0 ? r.x2.minus(f.right) : r.x2);
		}, createSubflowArea = (e) => {
			let t = rectFromSteps(selectSteps(e.id)), n;
			switch (!0) {
				case Z.isAltOrTry(e):
					n = 0;
					break;
				case a.has(e.id) && Z.isAltOrTryBranch(e):
					n = {
						...o,
						top: 36,
						bottom: 0
					};
					break;
				case a.has(e.id):
					n = {
						...o,
						top: 24,
						bottom: 0
					};
					break;
				default:
					n = o;
					break;
			}
			let r = {
				...this.wrapAroundRect(t, n),
				...t
			};
			return this.#c.set(e.id, r), r;
		};
		this.#e.walk({ subflow: ({ subflow: e, previous: t, parent: n }) => ({
			next: a.has(e.id) ? [] : e.flow,
			onLeave: ({ visited: r }) => {
				let i = createSubflowArea(e), o = Z.isSubFlow(t) && this.#c.get(t.id);
				switch (!Z.isAltOrTryBranch(e) && o && this.put(o.y2).before(i.y1, 32), !n && i.min.column > 0 && this.require(this.actorBox(i.min.column - 1).centerX).before(i.x1, 100), !0) {
					case a.has(e.id):
						this.collapseRect(i);
						return;
					case Z.isAltOrTry(e): return fitSubflows({
						area: i,
						visited: r,
						padding: {
							top: 30,
							left: 0,
							right: 0
						},
						stretch: !0
					});
					case Z.isAltOrTryBranch(e): return fitSubflows({
						area: i,
						visited: r,
						padding: {
							top: 50,
							left: 32,
							bottom: 32,
							right: 32
						}
					});
					default: return fitSubflows({
						area: i,
						visited: r,
						padding: {
							top: 40,
							bottom: 12,
							left: 32,
							right: 16
						}
					});
				}
			}
		}) });
	}
	newVar(e) {
		let t = new Variable();
		return this.#t.addEditVariable(t, D.weak), typeof e == `number` && (t.setValue(e), this.#t.suggestValue(t, e)), t;
	}
	require(...e) {
		if (e.length == 1) {
			let [t] = e;
			return {
				before: (e, n = 0) => {
					let r = n ? e.minus(n) : e;
					return this.constraint(t, `<=`, r, D.required);
				},
				after: (e, n = 0) => {
					let r = n ? e.plus(n) : e;
					return this.constraint(t, `>=`, r, D.required);
				}
			};
		}
		let [t, n, r] = e, i = this.constraint(t, n, r, D.required);
		switch (n) {
			case `<=`:
			case `>=`:
				this.constraint(t, `==`, r, D.weak);
				break;
			case `<= 0`:
			case `>= 0`:
				this.constraint(t, `== 0`, void 0, D.weak);
				break;
		}
		return i;
	}
	constraint(e, t, n = void 0, r = D.medium) {
		let i;
		switch (t) {
			case `==`:
				i = a.Eq;
				break;
			case `>=`:
				i = a.Ge;
				break;
			case `<=`:
				i = a.Le;
				break;
			case `== 0`:
				i = a.Eq, n = 0;
				break;
			case `>= 0`:
				i = a.Ge, n = 0;
				break;
			case `<= 0`:
				i = a.Le, n = 0;
				break;
			default: nonexhaustive(t);
		}
		let a$2 = new Constraint(e, i, n ?? 0, r);
		return this.#t.addConstraint(a$2), a$2;
	}
	put(e, t = D.required) {
		let n = t === D.required ? D.soft : D.weak;
		return {
			before: (r, i) => {
				i && (r = r.minus(i)), this.constraint(e, `<=`, r, t), this.constraint(e, `==`, r, n);
			},
			after: (r, i) => {
				i && (r = r.plus(i)), this.constraint(e, `>=`, r, t), this.constraint(e, `==`, r, n);
			}
		};
	}
};
function calcSequenceLayout(t) {
	let n = dynamicViewFlow(t), i = /* @__PURE__ */ new Set(), getNode = (e) => nonNullable(t.nodes.find((t) => t.id === e)), o = [];
	for (let e of t.edges) {
		let t = getNode(e.source), n = getNode(e.target);
		if (t.children.length || n.children.length) {
			console.error(`Sequence view does not support nested actors`);
			continue;
		}
		i.add(t), i.add(n), o.push({
			edge: e,
			source: t,
			target: n
		});
	}
	let d = t.nodes.filter((e) => i.has(e));
	invariant(t$29(d, 1), `actors array must not be empty`);
	let f = new se(() => []), p = [], m = 0, h;
	for (let { edge: e, source: t, target: n } of o) {
		let r = d.indexOf(t), i = d.indexOf(n), a = t === n, o = r > i, s = parentFlow(e.id), c = !1;
		h && h.target == t && h.parent === s && (c = h.isSelfLoop !== a || h.isBack === o), !c && h && m++;
		let l = {
			id: e.id,
			from: {
				column: r,
				row: m
			},
			to: {
				column: i,
				row: a ? ++m : m
			},
			edge: e,
			isSelfLoop: a,
			isBack: o,
			parent: s,
			offset: c ? (h?.offset ?? 0) + 22 : 0,
			source: t,
			target: n,
			label: e.labelBBox ? {
				height: e.labelBBox.height + 8 + (e.navigateTo ? 20 : 0),
				width: e.labelBBox.width + 16,
				text: e.label
			} : null
		};
		p.push(l), f.get(t).push({
			step: l,
			row: m,
			type: `source`,
			position: o && !a ? `left` : `right`
		}), f.get(n).push({
			step: l,
			row: m,
			type: `target`,
			position: o || a ? `right` : `left`
		}), h = l;
	}
	let g = new SequenceViewLayouter({
		actors: d,
		steps: p,
		compounds: buildCompounds(d, t.nodes),
		flow: n,
		collapsedFlows: []
	}), v = g.getViewBounds(), x = t$31(g.getCompoundBoxes(), t$14(({ node: e, ...t }) => ({
		...t,
		id: e.id,
		origin: e.id
	})), t$21(`id`), t$12((t, n) => t$29(t, 2) ? t$14(t, (e, t) => ({
		...e,
		id: `${n}-${t + 1}`
	})) : t), t$19(), n$26());
	return {
		actors: d.map((e) => toSeqActor({
			actor: e,
			ports: f.get(e),
			layout: g
		})),
		compounds: x,
		steps: t$14(p, (e) => ({
			id: e.id,
			sourceHandle: e.id + `_source`,
			targetHandle: e.id + `_target`,
			...e.label && { labelBBox: {
				width: e.label.width,
				height: e.label.height
			} }
		})),
		parallelAreas: [],
		subflows: g.getSubflowAreas(),
		bounds: v
	};
}
function toSeqActor({ actor: e, ports: t, layout: n }) {
	let { x: r, y: i, width: a, height: o } = n.getActorBox(e);
	return {
		id: e.id,
		x: r,
		y: i,
		width: a,
		height: o,
		ports: t.map((e) => {
			let t = n.getPortCenter(e.step, e.type);
			return {
				id: `${e.step.id}_${e.type}`,
				cx: t.cx - r,
				cy: t.cy - i,
				height: t.height,
				type: e.type,
				position: e.position
			};
		})
	};
}
//#endregion
//#region node_modules/likec4/dist/model/index.mjs
function createLikeC4Model(e) {
	return Y.create(e);
}
//#endregion
export { X as Connection, $ as ConnectionModel, DeployedInstanceModel, Q as DeploymentConnectionModel, DeploymentNodeModel, DeploymentRelationModel, EdgeModel, ElementModel, LikeC4DeploymentModel, Y as LikeC4Model, LikeC4ViewModel, NodeModel, RelationshipModel, calcSequenceLayout, createLikeC4Model, _e as deploymentConnection, differenceConnections, findAscendingConnections, findDeepestNestedConnection, findDescendantConnections, hasSameSource, hasSameSourceTarget, hasSameTarget, isAnyInOut, isDeployedInstanceModel, isDeploymentElementModel, isDeploymentNodeModel, isDeploymentRelationModel, isEdgeModel, isElementModel, isIncoming, isLikeC4ViewModel, isNestedConnection, isNestedElementOfDeployedInstanceModel, isNodeModel, isOutgoing, isRelationModel, mergeConnections, ve as modelConnection, sortConnectionsByBoundaryHierarchy, sortDeepestFirst };
