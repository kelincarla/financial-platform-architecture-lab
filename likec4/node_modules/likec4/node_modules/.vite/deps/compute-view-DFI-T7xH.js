import { $ as flattenMarkdownOrString, $n as e$16, $t as isViewRulePredicate, An as toSet, Ar as t$24, At as isExtendsElementView, Br as t$37, C as ProjectId, Cr as t$17, D as Re, Dn as stringHash, E as R, Er as t$2$1, Et as isDescendantOf, Fn as whereOperatorAsPredicate, Fr as t$3, Gn as l$1, Ht as isSameHierarchy, I as ancestorsFqn, In as z, Ir as t$30, It as isNonEmptyArray, Jn as e$11, Jr as __toESM, Kn as e$2, L as commonAncestor, Lr as t$32, Mr as t$26, Mt as isGroupElementKind, Nr as t$27, Nt as isIterable, Or as t$22, Ot as isDynamicView, Pr as t$28, Q as exact, Qn as e$15, Qt as isViewRuleGroup, R as commonHead, Rn as S, Rr as t$34, S as P, Sr as t$16, Tr as t$19, Tt as isDeploymentView, U as compareRelations, Ur as t$6, Vn as g, Wn as c$1, Wr as t$7, Xn as e$13, Xt as isViewRuleGlobalPredicateRef, Yn as e$12, Yt as isViewRuleAutoLayout, Z as et, Zn as e$14, Zt as isViewRuleGlobalStyle, _n as preferSummary, _r as t$11, an as k, ar as e$7, br as t$14, cr as f$1, ct as ifilter, dn as nonexhaustive, dr as n$14, dt as iflat, en as isViewRuleRank, er as e$20, fn as objectHash, fr as n$25, g as L, gr as t$10, gt as invariant, hn as parentFqn, hr as t$2, ht as intersection, in as j, it as hasIntersection, j as ViewId, jn as union, jr as t$25, k as StepPath, kn as toArray, kr as t$23, kt as isElementView, lr as m$1, mr as n$3, mt as imap, nn as isome, nr as e$4, or as e$8, pr as n$27, q as difference, qr as __name, rn as iunique, rr as e$5, s as F, sr as e$9, tn as isViewRuleStyle, tr as e$3, u as GlobalFqn, un as nonNullable, ur as n$1, v as MarkdownOrString, vr as t$12, vt as isAncestor, wn as sortParentsFirst, wr as t$18, xn as sortByFqnHierarchically, xr as t$15, y as N, yr as t$13, z as compareByFqnHierarchically, zr as t$35 } from "./types-Dcw6-bwQ.js";
import { n as buildElementNotations } from "./calcDriftsFromSnapshot-B8JVpXOO.js";
import { A as isDeployedInstanceModel, C as findDescendantConnections, F as isElementModel, G as q, I as isIncoming, J as ve, K as sortConnectionsByBoundaryHierarchy, M as isDeploymentNodeModel, R as isNestedConnection, S as findDeepestNestedConnection, U as mergeConnections, V as isOutgoing, _ as differenceConnections, b as findConnection, d as Q$1, g as _e, h as Z$1, j as isDeploymentElementModel, k as isAnyInOut, m as Y$1, p as X$1, t as $$1, x as findConnectionsBetween, y as findAscendingConnections, z as isNestedElementOfDeployedInstanceModel } from "./model-DBSABTjR.js";
//#region node_modules/@likec4/core/dist/chunks/libs/graphology.mjs
var e$1;
function EventHandlers() {}
EventHandlers.prototype = Object.create(null);
function EventEmitter() {
	EventEmitter.init.call(this);
}
EventEmitter.EventEmitter = EventEmitter, EventEmitter.usingDomains = !1, EventEmitter.prototype.domain = void 0, EventEmitter.prototype._events = void 0, EventEmitter.prototype._maxListeners = void 0, EventEmitter.defaultMaxListeners = 10, EventEmitter.init = function() {
	this.domain = null, EventEmitter.usingDomains && e$1.active && !(this instanceof e$1.Domain) && (this.domain = e$1.active), (!this._events || this._events === Object.getPrototypeOf(this)._events) && (this._events = new EventHandlers(), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
}, EventEmitter.prototype.setMaxListeners = function setMaxListeners(e) {
	if (typeof e != `number` || e < 0 || isNaN(e)) throw TypeError(`"n" argument must be a positive number`);
	return this._maxListeners = e, this;
};
function $getMaxListeners(e) {
	return e._maxListeners === void 0 ? EventEmitter.defaultMaxListeners : e._maxListeners;
}
EventEmitter.prototype.getMaxListeners = function getMaxListeners() {
	return $getMaxListeners(this);
};
function emitNone(e, t, n) {
	if (t) e.call(n);
	else for (var r = e.length, i = arrayClone(e, r), a = 0; a < r; ++a) i[a].call(n);
}
function emitOne(e, t, n, r) {
	if (t) e.call(n, r);
	else for (var i = e.length, a = arrayClone(e, i), o = 0; o < i; ++o) a[o].call(n, r);
}
function emitTwo(e, t, n, r, i) {
	if (t) e.call(n, r, i);
	else for (var a = e.length, o = arrayClone(e, a), s = 0; s < a; ++s) o[s].call(n, r, i);
}
function emitThree(e, t, n, r, i, a) {
	if (t) e.call(n, r, i, a);
	else for (var o = e.length, s = arrayClone(e, o), c = 0; c < o; ++c) s[c].call(n, r, i, a);
}
function emitMany(e, t, n, r) {
	if (t) e.apply(n, r);
	else for (var i = e.length, a = arrayClone(e, i), o = 0; o < i; ++o) a[o].apply(n, r);
}
EventEmitter.prototype.emit = function emit(e) {
	var t, n, r, i, a, o, s, c = e === `error`;
	if (o = this._events, o) c &&= o.error == null;
	else if (!c) return !1;
	if (s = this.domain, c) {
		if (t = arguments[1], s) t ||= Error(`Uncaught, unspecified "error" event`), t.domainEmitter = this, t.domain = s, t.domainThrown = !1, s.emit(`error`, t);
		else if (t instanceof Error) throw t;
		else {
			var l = Error(`Uncaught, unspecified "error" event. (` + t + `)`);
			throw l.context = t, l;
		}
		return !1;
	}
	if (n = o[e], !n) return !1;
	var u = typeof n == `function`;
	switch (r = arguments.length, r) {
		case 1:
			emitNone(n, u, this);
			break;
		case 2:
			emitOne(n, u, this, arguments[1]);
			break;
		case 3:
			emitTwo(n, u, this, arguments[1], arguments[2]);
			break;
		case 4:
			emitThree(n, u, this, arguments[1], arguments[2], arguments[3]);
			break;
		default:
			for (i = Array(r - 1), a = 1; a < r; a++) i[a - 1] = arguments[a];
			emitMany(n, u, this, i);
	}
	return !0;
};
function _addListener(e, t, n, r) {
	var i, a, o;
	if (typeof n != `function`) throw TypeError(`"listener" argument must be a function`);
	if (a = e._events, a ? (a.newListener && (e.emit(`newListener`, t, n.listener ? n.listener : n), a = e._events), o = a[t]) : (a = e._events = new EventHandlers(), e._eventsCount = 0), !o) o = a[t] = n, ++e._eventsCount;
	else if (typeof o == `function` ? o = a[t] = r ? [n, o] : [o, n] : r ? o.unshift(n) : o.push(n), !o.warned && (i = $getMaxListeners(e), i && i > 0 && o.length > i)) {
		o.warned = !0;
		var s = Error(`Possible EventEmitter memory leak detected. ` + o.length + ` ` + t + ` listeners added. Use emitter.setMaxListeners() to increase limit`);
		s.name = `MaxListenersExceededWarning`, s.emitter = e, s.type = t, s.count = o.length, emitWarning(s);
	}
	return e;
}
function emitWarning(e) {
	typeof console.warn == `function` ? console.warn(e) : console.log(e);
}
EventEmitter.prototype.addListener = function addListener(e, t) {
	return _addListener(this, e, t, !1);
}, EventEmitter.prototype.on = EventEmitter.prototype.addListener, EventEmitter.prototype.prependListener = function prependListener(e, t) {
	return _addListener(this, e, t, !0);
};
function _onceWrap(e, t, n) {
	var r = !1;
	function g() {
		e.removeListener(t, g), r || (r = !0, n.apply(e, arguments));
	}
	return g.listener = n, g;
}
EventEmitter.prototype.once = function once(e, t) {
	if (typeof t != `function`) throw TypeError(`"listener" argument must be a function`);
	return this.on(e, _onceWrap(this, e, t)), this;
}, EventEmitter.prototype.prependOnceListener = function prependOnceListener(e, t) {
	if (typeof t != `function`) throw TypeError(`"listener" argument must be a function`);
	return this.prependListener(e, _onceWrap(this, e, t)), this;
}, EventEmitter.prototype.removeListener = function removeListener(e, t) {
	var n, r, i, a, o;
	if (typeof t != `function`) throw TypeError(`"listener" argument must be a function`);
	if (r = this._events, !r || (n = r[e], !n)) return this;
	if (n === t || n.listener && n.listener === t) --this._eventsCount === 0 ? this._events = new EventHandlers() : (delete r[e], r.removeListener && this.emit(`removeListener`, e, n.listener || t));
	else if (typeof n != `function`) {
		for (i = -1, a = n.length; a-- > 0;) if (n[a] === t || n[a].listener && n[a].listener === t) {
			o = n[a].listener, i = a;
			break;
		}
		if (i < 0) return this;
		if (n.length === 1) {
			if (n[0] = void 0, --this._eventsCount === 0) return this._events = new EventHandlers(), this;
			delete r[e];
		} else spliceOne(n, i);
		r.removeListener && this.emit(`removeListener`, e, o || t);
	}
	return this;
}, EventEmitter.prototype.off = function(e, t) {
	return this.removeListener(e, t);
}, EventEmitter.prototype.removeAllListeners = function removeAllListeners(e) {
	var t, n = this._events;
	if (!n) return this;
	if (!n.removeListener) return arguments.length === 0 ? (this._events = new EventHandlers(), this._eventsCount = 0) : n[e] && (--this._eventsCount === 0 ? this._events = new EventHandlers() : delete n[e]), this;
	if (arguments.length === 0) {
		for (var r = Object.keys(n), i = 0, a; i < r.length; ++i) a = r[i], a !== `removeListener` && this.removeAllListeners(a);
		return this.removeAllListeners(`removeListener`), this._events = new EventHandlers(), this._eventsCount = 0, this;
	}
	if (t = n[e], typeof t == `function`) this.removeListener(e, t);
	else if (t) do
		this.removeListener(e, t[t.length - 1]);
	while (t[0]);
	return this;
}, EventEmitter.prototype.listeners = function listeners(e) {
	var t, n, r = this._events;
	return r ? (t = r[e], n = t ? typeof t == `function` ? [t.listener || t] : unwrapListeners(t) : []) : n = [], n;
}, EventEmitter.listenerCount = function(e, t) {
	return typeof e.listenerCount == `function` ? e.listenerCount(t) : listenerCount.call(e, t);
}, EventEmitter.prototype.listenerCount = listenerCount;
function listenerCount(e) {
	var t = this._events;
	if (t) {
		var n = t[e];
		if (typeof n == `function`) return 1;
		if (n) return n.length;
	}
	return 0;
}
EventEmitter.prototype.eventNames = function eventNames() {
	return this._eventsCount > 0 ? Reflect.ownKeys(this._events) : [];
};
function spliceOne(e, t) {
	for (var n = t, r = n + 1, i = e.length; r < i; n += 1, r += 1) e[n] = e[r];
	e.pop();
}
function arrayClone(e, t) {
	for (var n = Array(t); t--;) n[t] = e[t];
	return n;
}
function unwrapListeners(e) {
	for (var t = Array(e.length), n = 0; n < t.length; ++n) t[n] = e[n].listener || e[n];
	return t;
}
function assignPolyfill() {
	let e = arguments[0];
	for (let t = 1, n = arguments.length; t < n; t++) if (arguments[t]) for (let n in arguments[t]) e[n] = arguments[t][n];
	return e;
}
var t$1 = assignPolyfill;
typeof Object.assign == `function` && (t$1 = Object.assign);
function getMatchingEdge(e, t, n, r) {
	let i = e._nodes.get(t), a = null;
	return i && (a = r === `mixed` ? i.out && i.out[n] || i.undirected && i.undirected[n] : r === `directed` ? i.out && i.out[n] : i.undirected && i.undirected[n]), a;
}
function isPlainObject(e) {
	return typeof e == `object` && !!e;
}
function isEmpty(e) {
	let t;
	for (t in e) return !1;
	return !0;
}
function privateProperty(e, t, n) {
	Object.defineProperty(e, t, {
		enumerable: !1,
		configurable: !1,
		writable: !0,
		value: n
	});
}
function readOnlyProperty(e, t, n) {
	let r = {
		enumerable: !0,
		configurable: !0
	};
	typeof n == `function` ? r.get = n : (r.value = n, r.writable = !1), Object.defineProperty(e, t, r);
}
function validateHints(e) {
	return !(!isPlainObject(e) || e.attributes && !Array.isArray(e.attributes));
}
function incrementalIdStartingFromRandomByte() {
	let e = Math.floor(Math.random() * 256) & 255;
	return () => e++;
}
function chain() {
	let e = arguments, t = null, n = -1;
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			let r = null;
			do {
				if (t === null) {
					if (n++, n >= e.length) return { done: !0 };
					t = e[n][Symbol.iterator]();
				}
				if (r = t.next(), r.done) {
					t = null;
					continue;
				}
				break;
			} while (!0);
			return r;
		}
	};
}
function emptyIterator() {
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			return { done: !0 };
		}
	};
}
var GraphError = class extends Error {
	constructor(e) {
		super(), this.name = `GraphError`, this.message = e;
	}
};
var n = class InvalidArgumentsGraphError extends GraphError {
	constructor(e) {
		super(e), this.name = `InvalidArgumentsGraphError`, typeof Error.captureStackTrace == `function` && Error.captureStackTrace(this, InvalidArgumentsGraphError.prototype.constructor);
	}
};
var r$1 = class NotFoundGraphError extends GraphError {
	constructor(e) {
		super(e), this.name = `NotFoundGraphError`, typeof Error.captureStackTrace == `function` && Error.captureStackTrace(this, NotFoundGraphError.prototype.constructor);
	}
};
var i$2 = class UsageGraphError extends GraphError {
	constructor(e) {
		super(e), this.name = `UsageGraphError`, typeof Error.captureStackTrace == `function` && Error.captureStackTrace(this, UsageGraphError.prototype.constructor);
	}
};
function MixedNodeData(e, t) {
	this.key = e, this.attributes = t, this.clear();
}
MixedNodeData.prototype.clear = function() {
	this.inDegree = 0, this.outDegree = 0, this.undirectedDegree = 0, this.undirectedLoops = 0, this.directedLoops = 0, this.in = {}, this.out = {}, this.undirected = {};
};
function DirectedNodeData(e, t) {
	this.key = e, this.attributes = t, this.clear();
}
DirectedNodeData.prototype.clear = function() {
	this.inDegree = 0, this.outDegree = 0, this.directedLoops = 0, this.in = {}, this.out = {};
};
function UndirectedNodeData(e, t) {
	this.key = e, this.attributes = t, this.clear();
}
UndirectedNodeData.prototype.clear = function() {
	this.undirectedDegree = 0, this.undirectedLoops = 0, this.undirected = {};
};
function EdgeData(e, t, n, r, i) {
	this.key = t, this.attributes = i, this.undirected = e, this.source = n, this.target = r;
}
EdgeData.prototype.attach = function() {
	let e = `out`, t = `in`;
	this.undirected && (e = t = `undirected`);
	let n = this.source.key, r = this.target.key;
	this.source[e][r] = this, !(this.undirected && n === r) && (this.target[t][n] = this);
}, EdgeData.prototype.attachMulti = function() {
	let e = `out`, t = `in`, n = this.source.key, r = this.target.key;
	this.undirected && (e = t = `undirected`);
	let i = this.source[e], a = i[r];
	if (a === void 0) {
		i[r] = this, this.undirected && n === r || (this.target[t][n] = this);
		return;
	}
	a.previous = this, this.next = a, i[r] = this, this.target[t][n] = this;
}, EdgeData.prototype.detach = function() {
	let e = this.source.key, t = this.target.key, n = `out`, r = `in`;
	this.undirected && (n = r = `undirected`), delete this.source[n][t], delete this.target[r][e];
}, EdgeData.prototype.detachMulti = function() {
	let e = this.source.key, t = this.target.key, n = `out`, r = `in`;
	this.undirected && (n = r = `undirected`), this.previous === void 0 ? this.next === void 0 ? (delete this.source[n][t], delete this.target[r][e]) : (this.next.previous = void 0, this.source[n][t] = this.next, this.target[r][e] = this.next) : (this.previous.next = this.next, this.next !== void 0 && (this.next.previous = this.previous));
};
function findRelevantNodeData(e, t, n, i, a, o, s) {
	let c, l, u, d;
	if (i = `` + i, n === 0) {
		if (c = e._nodes.get(i), !c) throw new r$1(`Graph.${t}: could not find the "${i}" node in the graph.`);
		u = a, d = o;
	} else if (n === 3) {
		if (a = `` + a, l = e._edges.get(a), !l) throw new r$1(`Graph.${t}: could not find the "${a}" edge in the graph.`);
		let n = l.source.key, f = l.target.key;
		if (i === n) c = l.target;
		else if (i === f) c = l.source;
		else throw new r$1(`Graph.${t}: the "${i}" node is not attached to the "${a}" edge (${n}, ${f}).`);
		u = o, d = s;
	} else {
		if (l = e._edges.get(i), !l) throw new r$1(`Graph.${t}: could not find the "${i}" edge in the graph.`);
		c = n === 1 ? l.source : l.target, u = a, d = o;
	}
	return [
		c,
		u,
		d
	];
}
function attachNodeAttributeGetter(e, t, n) {
	e.prototype[t] = function(e, r, i) {
		let [a, o] = findRelevantNodeData(this, t, n, e, r, i);
		return a.attributes[o];
	};
}
function attachNodeAttributesGetter(e, t, n) {
	e.prototype[t] = function(e, r) {
		let [i] = findRelevantNodeData(this, t, n, e, r);
		return i.attributes;
	};
}
function attachNodeAttributeChecker(e, t, n) {
	e.prototype[t] = function(e, r, i) {
		let [a, o] = findRelevantNodeData(this, t, n, e, r, i);
		return a.attributes.hasOwnProperty(o);
	};
}
function attachNodeAttributeSetter(e, t, n) {
	e.prototype[t] = function(e, r, i, a) {
		let [o, s, c] = findRelevantNodeData(this, t, n, e, r, i, a);
		return o.attributes[s] = c, this.emit(`nodeAttributesUpdated`, {
			key: o.key,
			type: `set`,
			attributes: o.attributes,
			name: s
		}), this;
	};
}
function attachNodeAttributeUpdater(e, t, r) {
	e.prototype[t] = function(e, i, a, o) {
		let [s, c, l] = findRelevantNodeData(this, t, r, e, i, a, o);
		if (typeof l != `function`) throw new n(`Graph.${t}: updater should be a function.`);
		let u = s.attributes;
		return u[c] = l(u[c]), this.emit(`nodeAttributesUpdated`, {
			key: s.key,
			type: `set`,
			attributes: s.attributes,
			name: c
		}), this;
	};
}
function attachNodeAttributeRemover(e, t, n) {
	e.prototype[t] = function(e, r, i) {
		let [a, o] = findRelevantNodeData(this, t, n, e, r, i);
		return delete a.attributes[o], this.emit(`nodeAttributesUpdated`, {
			key: a.key,
			type: `remove`,
			attributes: a.attributes,
			name: o
		}), this;
	};
}
function attachNodeAttributesReplacer(e, t, r) {
	e.prototype[t] = function(e, i, a) {
		let [o, s] = findRelevantNodeData(this, t, r, e, i, a);
		if (!isPlainObject(s)) throw new n(`Graph.${t}: provided attributes are not a plain object.`);
		return o.attributes = s, this.emit(`nodeAttributesUpdated`, {
			key: o.key,
			type: `replace`,
			attributes: o.attributes
		}), this;
	};
}
function attachNodeAttributesMerger(e, r, i) {
	e.prototype[r] = function(e, a, o) {
		let [s, c] = findRelevantNodeData(this, r, i, e, a, o);
		if (!isPlainObject(c)) throw new n(`Graph.${r}: provided attributes are not a plain object.`);
		return t$1(s.attributes, c), this.emit(`nodeAttributesUpdated`, {
			key: s.key,
			type: `merge`,
			attributes: s.attributes,
			data: c
		}), this;
	};
}
function attachNodeAttributesUpdater(e, t, r) {
	e.prototype[t] = function(e, i, a) {
		let [o, s] = findRelevantNodeData(this, t, r, e, i, a);
		if (typeof s != `function`) throw new n(`Graph.${t}: provided updater is not a function.`);
		return o.attributes = s(o.attributes), this.emit(`nodeAttributesUpdated`, {
			key: o.key,
			type: `update`,
			attributes: o.attributes
		}), this;
	};
}
var a$2 = [
	{
		name: (e) => `get${e}Attribute`,
		attacher: attachNodeAttributeGetter
	},
	{
		name: (e) => `get${e}Attributes`,
		attacher: attachNodeAttributesGetter
	},
	{
		name: (e) => `has${e}Attribute`,
		attacher: attachNodeAttributeChecker
	},
	{
		name: (e) => `set${e}Attribute`,
		attacher: attachNodeAttributeSetter
	},
	{
		name: (e) => `update${e}Attribute`,
		attacher: attachNodeAttributeUpdater
	},
	{
		name: (e) => `remove${e}Attribute`,
		attacher: attachNodeAttributeRemover
	},
	{
		name: (e) => `replace${e}Attributes`,
		attacher: attachNodeAttributesReplacer
	},
	{
		name: (e) => `merge${e}Attributes`,
		attacher: attachNodeAttributesMerger
	},
	{
		name: (e) => `update${e}Attributes`,
		attacher: attachNodeAttributesUpdater
	}
];
function attachNodeAttributesMethods(e) {
	a$2.forEach(function({ name: t, attacher: n }) {
		n(e, t(`Node`), 0), n(e, t(`Source`), 1), n(e, t(`Target`), 2), n(e, t(`Opposite`), 3);
	});
}
function attachEdgeAttributeGetter(e, t, n) {
	e.prototype[t] = function(e, a) {
		let o;
		if (this.type !== `mixed` && n !== `mixed` && n !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let s = `` + e, c = `` + a;
			if (a = arguments[2], o = getMatchingEdge(this, s, c, n), !o) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${s}" - "${c}").`);
		} else {
			if (n !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, o = this._edges.get(e), !o) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		return o.attributes[a];
	};
}
function attachEdgeAttributesGetter(e, t, n) {
	e.prototype[t] = function(e) {
		let a;
		if (this.type !== `mixed` && n !== `mixed` && n !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 1) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let o = `` + e, s = `` + arguments[1];
			if (a = getMatchingEdge(this, o, s, n), !a) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${o}" - "${s}").`);
		} else {
			if (n !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, a = this._edges.get(e), !a) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		return a.attributes;
	};
}
function attachEdgeAttributeChecker(e, t, n) {
	e.prototype[t] = function(e, a) {
		let o;
		if (this.type !== `mixed` && n !== `mixed` && n !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let s = `` + e, c = `` + a;
			if (a = arguments[2], o = getMatchingEdge(this, s, c, n), !o) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${s}" - "${c}").`);
		} else {
			if (n !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, o = this._edges.get(e), !o) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		return o.attributes.hasOwnProperty(a);
	};
}
function attachEdgeAttributeSetter(e, t, n) {
	e.prototype[t] = function(e, a, o) {
		let s;
		if (this.type !== `mixed` && n !== `mixed` && n !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 3) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let c = `` + e, l = `` + a;
			if (a = arguments[2], o = arguments[3], s = getMatchingEdge(this, c, l, n), !s) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${c}" - "${l}").`);
		} else {
			if (n !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, s = this._edges.get(e), !s) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		return s.attributes[a] = o, this.emit(`edgeAttributesUpdated`, {
			key: s.key,
			type: `set`,
			attributes: s.attributes,
			name: a
		}), this;
	};
}
function attachEdgeAttributeUpdater(e, t, a) {
	e.prototype[t] = function(e, o, s) {
		let c;
		if (this.type !== `mixed` && a !== `mixed` && a !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 3) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let n = `` + e, l = `` + o;
			if (o = arguments[2], s = arguments[3], c = getMatchingEdge(this, n, l, a), !c) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${n}" - "${l}").`);
		} else {
			if (a !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, c = this._edges.get(e), !c) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		if (typeof s != `function`) throw new n(`Graph.${t}: updater should be a function.`);
		return c.attributes[o] = s(c.attributes[o]), this.emit(`edgeAttributesUpdated`, {
			key: c.key,
			type: `set`,
			attributes: c.attributes,
			name: o
		}), this;
	};
}
function attachEdgeAttributeRemover(e, t, n) {
	e.prototype[t] = function(e, a) {
		let o;
		if (this.type !== `mixed` && n !== `mixed` && n !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let s = `` + e, c = `` + a;
			if (a = arguments[2], o = getMatchingEdge(this, s, c, n), !o) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${s}" - "${c}").`);
		} else {
			if (n !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, o = this._edges.get(e), !o) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		return delete o.attributes[a], this.emit(`edgeAttributesUpdated`, {
			key: o.key,
			type: `remove`,
			attributes: o.attributes,
			name: a
		}), this;
	};
}
function attachEdgeAttributesReplacer(e, t, a) {
	e.prototype[t] = function(e, o) {
		let s;
		if (this.type !== `mixed` && a !== `mixed` && a !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let n = `` + e, c = `` + o;
			if (o = arguments[2], s = getMatchingEdge(this, n, c, a), !s) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${n}" - "${c}").`);
		} else {
			if (a !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, s = this._edges.get(e), !s) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		if (!isPlainObject(o)) throw new n(`Graph.${t}: provided attributes are not a plain object.`);
		return s.attributes = o, this.emit(`edgeAttributesUpdated`, {
			key: s.key,
			type: `replace`,
			attributes: s.attributes
		}), this;
	};
}
function attachEdgeAttributesMerger(e, a, o) {
	e.prototype[a] = function(e, s) {
		let c;
		if (this.type !== `mixed` && o !== `mixed` && o !== this.type) throw new i$2(`Graph.${a}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${a}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let t = `` + e, n = `` + s;
			if (s = arguments[2], c = getMatchingEdge(this, t, n, o), !c) throw new r$1(`Graph.${a}: could not find an edge for the given path ("${t}" - "${n}").`);
		} else {
			if (o !== `mixed`) throw new i$2(`Graph.${a}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, c = this._edges.get(e), !c) throw new r$1(`Graph.${a}: could not find the "${e}" edge in the graph.`);
		}
		if (!isPlainObject(s)) throw new n(`Graph.${a}: provided attributes are not a plain object.`);
		return t$1(c.attributes, s), this.emit(`edgeAttributesUpdated`, {
			key: c.key,
			type: `merge`,
			attributes: c.attributes,
			data: s
		}), this;
	};
}
function attachEdgeAttributesUpdater(e, t, a) {
	e.prototype[t] = function(e, o) {
		let s;
		if (this.type !== `mixed` && a !== `mixed` && a !== this.type) throw new i$2(`Graph.${t}: cannot find this type of edges in your ${this.type} graph.`);
		if (arguments.length > 2) {
			if (this.multi) throw new i$2(`Graph.${t}: cannot use a {source,target} combo when asking about an edge's attributes in a MultiGraph since we cannot infer the one you want information about.`);
			let n = `` + e, c = `` + o;
			if (o = arguments[2], s = getMatchingEdge(this, n, c, a), !s) throw new r$1(`Graph.${t}: could not find an edge for the given path ("${n}" - "${c}").`);
		} else {
			if (a !== `mixed`) throw new i$2(`Graph.${t}: calling this method with only a key (vs. a source and target) does not make sense since an edge with this key could have the other type.`);
			if (e = `` + e, s = this._edges.get(e), !s) throw new r$1(`Graph.${t}: could not find the "${e}" edge in the graph.`);
		}
		if (typeof o != `function`) throw new n(`Graph.${t}: provided updater is not a function.`);
		return s.attributes = o(s.attributes), this.emit(`edgeAttributesUpdated`, {
			key: s.key,
			type: `update`,
			attributes: s.attributes
		}), this;
	};
}
var o$1 = [
	{
		name: (e) => `get${e}Attribute`,
		attacher: attachEdgeAttributeGetter
	},
	{
		name: (e) => `get${e}Attributes`,
		attacher: attachEdgeAttributesGetter
	},
	{
		name: (e) => `has${e}Attribute`,
		attacher: attachEdgeAttributeChecker
	},
	{
		name: (e) => `set${e}Attribute`,
		attacher: attachEdgeAttributeSetter
	},
	{
		name: (e) => `update${e}Attribute`,
		attacher: attachEdgeAttributeUpdater
	},
	{
		name: (e) => `remove${e}Attribute`,
		attacher: attachEdgeAttributeRemover
	},
	{
		name: (e) => `replace${e}Attributes`,
		attacher: attachEdgeAttributesReplacer
	},
	{
		name: (e) => `merge${e}Attributes`,
		attacher: attachEdgeAttributesMerger
	},
	{
		name: (e) => `update${e}Attributes`,
		attacher: attachEdgeAttributesUpdater
	}
];
function attachEdgeAttributesMethods(e) {
	o$1.forEach(function({ name: t, attacher: n }) {
		n(e, t(`Edge`), `mixed`), n(e, t(`DirectedEdge`), `directed`), n(e, t(`UndirectedEdge`), `undirected`);
	});
}
var s$1 = [
	{
		name: `edges`,
		type: `mixed`
	},
	{
		name: `inEdges`,
		type: `directed`,
		direction: `in`
	},
	{
		name: `outEdges`,
		type: `directed`,
		direction: `out`
	},
	{
		name: `inboundEdges`,
		type: `mixed`,
		direction: `in`
	},
	{
		name: `outboundEdges`,
		type: `mixed`,
		direction: `out`
	},
	{
		name: `directedEdges`,
		type: `directed`
	},
	{
		name: `undirectedEdges`,
		type: `undirected`
	}
];
function forEachSimple(e, t, n, r) {
	let i = !1;
	for (let a in t) {
		if (a === r) continue;
		let o = t[a];
		if (i = n(o.key, o.attributes, o.source.key, o.target.key, o.source.attributes, o.target.attributes, o.undirected), e && i) return o.key;
	}
}
function forEachMulti(e, t, n, r) {
	let i, a, o, s = !1;
	for (let c in t) if (c !== r) {
		i = t[c];
		do {
			if (a = i.source, o = i.target, s = n(i.key, i.attributes, a.key, o.key, a.attributes, o.attributes, i.undirected), e && s) return i.key;
			i = i.next;
		} while (i !== void 0);
	}
}
function createIterator(e, t) {
	let n = Object.keys(e), r = n.length, i, a = 0;
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			do
				if (i) i = i.next;
				else {
					if (a >= r) return { done: !0 };
					let o = n[a++];
					if (o === t) {
						i = void 0;
						continue;
					}
					i = e[o];
				}
			while (!i);
			return {
				done: !1,
				value: {
					edge: i.key,
					attributes: i.attributes,
					source: i.source.key,
					target: i.target.key,
					sourceAttributes: i.source.attributes,
					targetAttributes: i.target.attributes,
					undirected: i.undirected
				}
			};
		}
	};
}
function forEachForKeySimple(e, t, n, r) {
	let i = t[n];
	if (!i) return;
	let a = i.source, o = i.target;
	if (r(i.key, i.attributes, a.key, o.key, a.attributes, o.attributes, i.undirected) && e) return i.key;
}
function forEachForKeyMulti(e, t, n, r) {
	let i = t[n];
	if (!i) return;
	let a = !1;
	do {
		if (a = r(i.key, i.attributes, i.source.key, i.target.key, i.source.attributes, i.target.attributes, i.undirected), e && a) return i.key;
		i = i.next;
	} while (i !== void 0);
}
function createIteratorForKey(e, t) {
	let n = e[t];
	if (n.next !== void 0) return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			if (!n) return { done: !0 };
			let e = {
				edge: n.key,
				attributes: n.attributes,
				source: n.source.key,
				target: n.target.key,
				sourceAttributes: n.source.attributes,
				targetAttributes: n.target.attributes,
				undirected: n.undirected
			};
			return n = n.next, {
				done: !1,
				value: e
			};
		}
	};
	let r = !1;
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			return r === !0 ? { done: !0 } : (r = !0, {
				done: !1,
				value: {
					edge: n.key,
					attributes: n.attributes,
					source: n.source.key,
					target: n.target.key,
					sourceAttributes: n.source.attributes,
					targetAttributes: n.target.attributes,
					undirected: n.undirected
				}
			});
		}
	};
}
function createEdgeArray(e, t) {
	if (e.size === 0) return [];
	if (t === `mixed` || t === e.type) return Array.from(e._edges.keys());
	let n = t === `undirected` ? e.undirectedSize : e.directedSize, r = Array(n), i = t === `undirected`, a = e._edges.values(), o = 0, s, c;
	for (; s = a.next(), s.done !== !0;) c = s.value, c.undirected === i && (r[o++] = c.key);
	return r;
}
function forEachEdge(e, t, n, r) {
	if (t.size === 0) return;
	let i = n !== `mixed` && n !== t.type, a = n === `undirected`, o, s, c = !1, l = t._edges.values();
	for (; o = l.next(), o.done !== !0;) {
		if (s = o.value, i && s.undirected !== a) continue;
		let { key: t, attributes: n, source: l, target: u } = s;
		if (c = r(t, n, l.key, u.key, l.attributes, u.attributes, s.undirected), e && c) return t;
	}
}
function createEdgeIterator(e, t) {
	if (e.size === 0) return emptyIterator();
	let n = t !== `mixed` && t !== e.type, r = t === `undirected`, i = e._edges.values();
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			let e, t;
			for (;;) {
				if (e = i.next(), e.done) return e;
				if (t = e.value, !(n && t.undirected !== r)) break;
			}
			return {
				value: {
					edge: t.key,
					attributes: t.attributes,
					source: t.source.key,
					target: t.target.key,
					sourceAttributes: t.source.attributes,
					targetAttributes: t.target.attributes,
					undirected: t.undirected
				},
				done: !1
			};
		}
	};
}
function forEachEdgeForNode(e, t, n, r, i, a) {
	let o = t ? forEachMulti : forEachSimple, s;
	if (n !== `undirected` && (r !== `out` && (s = o(e, i.in, a), e && s) || r !== `in` && (s = o(e, i.out, a, r ? void 0 : i.key), e && s)) || n !== `directed` && (s = o(e, i.undirected, a), e && s)) return s;
}
function createEdgeArrayForNode(e, t, n, r) {
	let i = [];
	return forEachEdgeForNode(!1, e, t, n, r, function(e) {
		i.push(e);
	}), i;
}
function createEdgeIteratorForNode(e, t, n) {
	let r = emptyIterator();
	return e !== `undirected` && (t !== `out` && n.in !== void 0 && (r = chain(r, createIterator(n.in))), t !== `in` && n.out !== void 0 && (r = chain(r, createIterator(n.out, t ? void 0 : n.key)))), e !== `directed` && n.undirected !== void 0 && (r = chain(r, createIterator(n.undirected))), r;
}
function forEachEdgeForPath(e, t, n, r, i, a, o) {
	let s = n ? forEachForKeyMulti : forEachForKeySimple, c;
	if (t !== `undirected` && (i.in !== void 0 && r !== `out` && (c = s(e, i.in, a, o), e && c) || i.out !== void 0 && r !== `in` && (r || i.key !== a) && (c = s(e, i.out, a, o), e && c)) || t !== `directed` && i.undirected !== void 0 && (c = s(e, i.undirected, a, o), e && c)) return c;
}
function createEdgeArrayForPath(e, t, n, r, i) {
	let a = [];
	return forEachEdgeForPath(!1, e, t, n, r, i, function(e) {
		a.push(e);
	}), a;
}
function createEdgeIteratorForPath(e, t, n, r) {
	let i = emptyIterator();
	return e !== `undirected` && (n.in !== void 0 && t !== `out` && r in n.in && (i = chain(i, createIteratorForKey(n.in, r))), n.out !== void 0 && t !== `in` && r in n.out && (t || n.key !== r) && (i = chain(i, createIteratorForKey(n.out, r)))), e !== `directed` && n.undirected !== void 0 && r in n.undirected && (i = chain(i, createIteratorForKey(n.undirected, r))), i;
}
function attachEdgeArrayCreator(e, t) {
	let { name: i, type: a, direction: o } = t;
	e.prototype[i] = function(e, t) {
		if (a !== `mixed` && this.type !== `mixed` && a !== this.type) return [];
		if (!arguments.length) return createEdgeArray(this, a);
		if (arguments.length === 1) {
			e = `` + e;
			let t = this._nodes.get(e);
			if (t === void 0) throw new r$1(`Graph.${i}: could not find the "${e}" node in the graph.`);
			return createEdgeArrayForNode(this.multi, a === `mixed` ? this.type : a, o, t);
		}
		if (arguments.length === 2) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			if (!n) throw new r$1(`Graph.${i}:  could not find the "${e}" source node in the graph.`);
			if (!this._nodes.has(t)) throw new r$1(`Graph.${i}:  could not find the "${t}" target node in the graph.`);
			return createEdgeArrayForPath(a, this.multi, o, n, t);
		}
		throw new n(`Graph.${i}: too many arguments (expecting 0, 1 or 2 and got ${arguments.length}).`);
	};
}
function attachForEachEdge(e, t) {
	let { name: i, type: a, direction: o } = t, s = `forEach` + i[0].toUpperCase() + i.slice(1, -1);
	e.prototype[s] = function(e, t, i) {
		if (a === `mixed` || this.type === `mixed` || a === this.type) {
			if (arguments.length === 1) return i = e, forEachEdge(!1, this, a, i);
			if (arguments.length === 2) {
				e = `` + e, i = t;
				let n = this._nodes.get(e);
				if (n === void 0) throw new r$1(`Graph.${s}: could not find the "${e}" node in the graph.`);
				return forEachEdgeForNode(!1, this.multi, a === `mixed` ? this.type : a, o, n, i);
			}
			if (arguments.length === 3) {
				e = `` + e, t = `` + t;
				let n = this._nodes.get(e);
				if (!n) throw new r$1(`Graph.${s}:  could not find the "${e}" source node in the graph.`);
				if (!this._nodes.has(t)) throw new r$1(`Graph.${s}:  could not find the "${t}" target node in the graph.`);
				return forEachEdgeForPath(!1, a, this.multi, o, n, t, i);
			}
			throw new n(`Graph.${s}: too many arguments (expecting 1, 2 or 3 and got ${arguments.length}).`);
		}
	};
	let c = `map` + i[0].toUpperCase() + i.slice(1);
	e.prototype[c] = function() {
		let e = Array.prototype.slice.call(arguments), t = e.pop(), n;
		if (e.length === 0) {
			let r = 0;
			a !== `directed` && (r += this.undirectedSize), a !== `undirected` && (r += this.directedSize), n = Array(r);
			let i = 0;
			e.push((e, r, a, o, s, c, l) => {
				n[i++] = t(e, r, a, o, s, c, l);
			});
		} else n = [], e.push((e, r, i, a, o, s, c) => {
			n.push(t(e, r, i, a, o, s, c));
		});
		return this[s].apply(this, e), n;
	};
	let l = `filter` + i[0].toUpperCase() + i.slice(1);
	e.prototype[l] = function() {
		let e = Array.prototype.slice.call(arguments), t = e.pop(), n = [];
		return e.push((e, r, i, a, o, s, c) => {
			t(e, r, i, a, o, s, c) && n.push(e);
		}), this[s].apply(this, e), n;
	};
	let u = `reduce` + i[0].toUpperCase() + i.slice(1);
	e.prototype[u] = function() {
		let e = Array.prototype.slice.call(arguments);
		if (e.length < 2 || e.length > 4) throw new n(`Graph.${u}: invalid number of arguments (expecting 2, 3 or 4 and got ${e.length}).`);
		if (typeof e[e.length - 1] == `function` && typeof e[e.length - 2] != `function`) throw new n(`Graph.${u}: missing initial value. You must provide it because the callback takes more than one argument and we cannot infer the initial value from the first iteration, as you could with a simple array.`);
		let t, r;
		e.length === 2 ? (t = e[0], r = e[1], e = []) : e.length === 3 ? (t = e[1], r = e[2], e = [e[0]]) : e.length === 4 && (t = e[2], r = e[3], e = [e[0], e[1]]);
		let i = r;
		return e.push((e, n, r, a, o, s, c) => {
			i = t(i, e, n, r, a, o, s, c);
		}), this[s].apply(this, e), i;
	};
}
function attachFindEdge(e, t) {
	let { name: i, type: a, direction: o } = t, s = `find` + i[0].toUpperCase() + i.slice(1, -1);
	e.prototype[s] = function(e, t, i) {
		if (a !== `mixed` && this.type !== `mixed` && a !== this.type) return !1;
		if (arguments.length === 1) return i = e, forEachEdge(!0, this, a, i);
		if (arguments.length === 2) {
			e = `` + e, i = t;
			let n = this._nodes.get(e);
			if (n === void 0) throw new r$1(`Graph.${s}: could not find the "${e}" node in the graph.`);
			return forEachEdgeForNode(!0, this.multi, a === `mixed` ? this.type : a, o, n, i);
		}
		if (arguments.length === 3) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			if (!n) throw new r$1(`Graph.${s}:  could not find the "${e}" source node in the graph.`);
			if (!this._nodes.has(t)) throw new r$1(`Graph.${s}:  could not find the "${t}" target node in the graph.`);
			return forEachEdgeForPath(!0, a, this.multi, o, n, t, i);
		}
		throw new n(`Graph.${s}: too many arguments (expecting 1, 2 or 3 and got ${arguments.length}).`);
	};
	let c = `some` + i[0].toUpperCase() + i.slice(1, -1);
	e.prototype[c] = function() {
		let e = Array.prototype.slice.call(arguments), t = e.pop();
		return e.push((e, n, r, i, a, o, s) => t(e, n, r, i, a, o, s)), !!this[s].apply(this, e);
	};
	let l = `every` + i[0].toUpperCase() + i.slice(1, -1);
	e.prototype[l] = function() {
		let e = Array.prototype.slice.call(arguments), t = e.pop();
		return e.push((e, n, r, i, a, o, s) => !t(e, n, r, i, a, o, s)), !this[s].apply(this, e);
	};
}
function attachEdgeIteratorCreator(e, t) {
	let { name: i, type: a, direction: o } = t, s = i.slice(0, -1) + `Entries`;
	e.prototype[s] = function(e, t) {
		if (a !== `mixed` && this.type !== `mixed` && a !== this.type) return emptyIterator();
		if (!arguments.length) return createEdgeIterator(this, a);
		if (arguments.length === 1) {
			e = `` + e;
			let t = this._nodes.get(e);
			if (!t) throw new r$1(`Graph.${s}: could not find the "${e}" node in the graph.`);
			return createEdgeIteratorForNode(a, o, t);
		}
		if (arguments.length === 2) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			if (!n) throw new r$1(`Graph.${s}:  could not find the "${e}" source node in the graph.`);
			if (!this._nodes.has(t)) throw new r$1(`Graph.${s}:  could not find the "${t}" target node in the graph.`);
			return createEdgeIteratorForPath(a, o, n, t);
		}
		throw new n(`Graph.${s}: too many arguments (expecting 0, 1 or 2 and got ${arguments.length}).`);
	};
}
function attachEdgeIterationMethods(e) {
	s$1.forEach((t) => {
		attachEdgeArrayCreator(e, t), attachForEachEdge(e, t), attachFindEdge(e, t), attachEdgeIteratorCreator(e, t);
	});
}
var c = [
	{
		name: `neighbors`,
		type: `mixed`
	},
	{
		name: `inNeighbors`,
		type: `directed`,
		direction: `in`
	},
	{
		name: `outNeighbors`,
		type: `directed`,
		direction: `out`
	},
	{
		name: `inboundNeighbors`,
		type: `mixed`,
		direction: `in`
	},
	{
		name: `outboundNeighbors`,
		type: `mixed`,
		direction: `out`
	},
	{
		name: `directedNeighbors`,
		type: `directed`
	},
	{
		name: `undirectedNeighbors`,
		type: `undirected`
	}
];
function CompositeSetWrapper() {
	this.A = null, this.B = null;
}
CompositeSetWrapper.prototype.wrap = function(e) {
	this.A === null ? this.A = e : this.B === null && (this.B = e);
}, CompositeSetWrapper.prototype.has = function(e) {
	return this.A !== null && e in this.A || this.B !== null && e in this.B;
};
function forEachInObjectOnce(e, t, n, r, i) {
	for (let a in r) {
		let o = r[a], s = o.source, c = o.target, l = s === n ? c : s;
		if (t && t.has(l.key)) continue;
		let u = i(l.key, l.attributes);
		if (e && u) return l.key;
	}
}
function forEachNeighbor(e, t, n, r, i) {
	if (t !== `mixed`) {
		if (t === `undirected`) return forEachInObjectOnce(e, null, r, r.undirected, i);
		if (typeof n == `string`) return forEachInObjectOnce(e, null, r, r[n], i);
	}
	let a = new CompositeSetWrapper(), o;
	if (t !== `undirected`) {
		if (n !== `out`) {
			if (o = forEachInObjectOnce(e, null, r, r.in, i), e && o) return o;
			a.wrap(r.in);
		}
		if (n !== `in`) {
			if (o = forEachInObjectOnce(e, a, r, r.out, i), e && o) return o;
			a.wrap(r.out);
		}
	}
	if (t !== `directed` && (o = forEachInObjectOnce(e, a, r, r.undirected, i), e && o)) return o;
}
function createNeighborArrayForNode(e, t, n) {
	if (e !== `mixed`) {
		if (e === `undirected`) return Object.keys(n.undirected);
		if (typeof t == `string`) return Object.keys(n[t]);
	}
	let r = [];
	return forEachNeighbor(!1, e, t, n, function(e) {
		r.push(e);
	}), r;
}
function createDedupedObjectIterator(e, t, n) {
	let r = Object.keys(n), i = r.length, a = 0;
	return {
		[Symbol.iterator]() {
			return this;
		},
		next() {
			let o = null;
			do {
				if (a >= i) return e && e.wrap(n), { done: !0 };
				let s = n[r[a++]], c = s.source, l = s.target;
				if (o = c === t ? l : c, e && e.has(o.key)) {
					o = null;
					continue;
				}
			} while (o === null);
			return {
				done: !1,
				value: {
					neighbor: o.key,
					attributes: o.attributes
				}
			};
		}
	};
}
function createNeighborIterator(e, t, n) {
	if (e !== `mixed`) {
		if (e === `undirected`) return createDedupedObjectIterator(null, n, n.undirected);
		if (typeof t == `string`) return createDedupedObjectIterator(null, n, n[t]);
	}
	let r = emptyIterator(), i = new CompositeSetWrapper();
	return e !== `undirected` && (t !== `out` && (r = chain(r, createDedupedObjectIterator(i, n, n.in))), t !== `in` && (r = chain(r, createDedupedObjectIterator(i, n, n.out)))), e !== `directed` && (r = chain(r, createDedupedObjectIterator(i, n, n.undirected))), r;
}
function attachNeighborArrayCreator(e, t) {
	let { name: n, type: i, direction: a } = t;
	e.prototype[n] = function(e) {
		if (i !== `mixed` && this.type !== `mixed` && i !== this.type) return [];
		e = `` + e;
		let t = this._nodes.get(e);
		if (t === void 0) throw new r$1(`Graph.${n}: could not find the "${e}" node in the graph.`);
		return createNeighborArrayForNode(i === `mixed` ? this.type : i, a, t);
	};
}
function attachForEachNeighbor(e, t) {
	let { name: i, type: a, direction: o } = t, s = `forEach` + i[0].toUpperCase() + i.slice(1, -1);
	e.prototype[s] = function(e, t) {
		if (a !== `mixed` && this.type !== `mixed` && a !== this.type) return;
		e = `` + e;
		let n = this._nodes.get(e);
		if (n === void 0) throw new r$1(`Graph.${s}: could not find the "${e}" node in the graph.`);
		forEachNeighbor(!1, a === `mixed` ? this.type : a, o, n, t);
	};
	let c = `map` + i[0].toUpperCase() + i.slice(1);
	e.prototype[c] = function(e, t) {
		let n = [];
		return this[s](e, (e, r) => {
			n.push(t(e, r));
		}), n;
	};
	let l = `filter` + i[0].toUpperCase() + i.slice(1);
	e.prototype[l] = function(e, t) {
		let n = [];
		return this[s](e, (e, r) => {
			t(e, r) && n.push(e);
		}), n;
	};
	let u = `reduce` + i[0].toUpperCase() + i.slice(1);
	e.prototype[u] = function(e, t, r) {
		if (arguments.length < 3) throw new n(`Graph.${u}: missing initial value. You must provide it because the callback takes more than one argument and we cannot infer the initial value from the first iteration, as you could with a simple array.`);
		let i = r;
		return this[s](e, (e, n) => {
			i = t(i, e, n);
		}), i;
	};
}
function attachFindNeighbor(e, t) {
	let { name: n, type: i, direction: a } = t, o = n[0].toUpperCase() + n.slice(1, -1), s = `find` + o;
	e.prototype[s] = function(e, t) {
		if (i !== `mixed` && this.type !== `mixed` && i !== this.type) return;
		e = `` + e;
		let n = this._nodes.get(e);
		if (n === void 0) throw new r$1(`Graph.${s}: could not find the "${e}" node in the graph.`);
		return forEachNeighbor(!0, i === `mixed` ? this.type : i, a, n, t);
	};
	let c = `some` + o;
	e.prototype[c] = function(e, t) {
		return !!this[s](e, t);
	};
	let l = `every` + o;
	e.prototype[l] = function(e, t) {
		return !this[s](e, (e, n) => !t(e, n));
	};
}
function attachNeighborIteratorCreator(e, t) {
	let { name: n, type: i, direction: a } = t, o = n.slice(0, -1) + `Entries`;
	e.prototype[o] = function(e) {
		if (i !== `mixed` && this.type !== `mixed` && i !== this.type) return emptyIterator();
		e = `` + e;
		let t = this._nodes.get(e);
		if (t === void 0) throw new r$1(`Graph.${o}: could not find the "${e}" node in the graph.`);
		return createNeighborIterator(i === `mixed` ? this.type : i, a, t);
	};
}
function attachNeighborIterationMethods(e) {
	c.forEach((t) => {
		attachNeighborArrayCreator(e, t), attachForEachNeighbor(e, t), attachFindNeighbor(e, t), attachNeighborIteratorCreator(e, t);
	});
}
function forEachAdjacency(e, t, n, r, i) {
	let a = r._nodes.values(), o = r.type, s, c, l, u, d, f, p;
	for (; s = a.next(), s.done !== !0;) {
		let r = !1;
		if (c = s.value, o !== `undirected`) for (l in u = c.out, u) {
			d = u[l];
			do {
				if (f = d.target, r = !0, p = i(c.key, f.key, c.attributes, f.attributes, d.key, d.attributes, d.undirected), e && p) return d;
				d = d.next;
			} while (d);
		}
		if (o !== `directed`) {
			for (l in u = c.undirected, u) if (!(t && c.key > l)) {
				d = u[l];
				do {
					if (f = d.target, f.key !== l && (f = d.source), r = !0, p = i(c.key, f.key, c.attributes, f.attributes, d.key, d.attributes, d.undirected), e && p) return d;
					d = d.next;
				} while (d);
			}
		}
		if (n && !r && (p = i(c.key, null, c.attributes, null, null, null, null), e && p)) return null;
	}
}
function serializeNode(e, n) {
	let r = { key: e };
	return isEmpty(n.attributes) || (r.attributes = t$1({}, n.attributes)), r;
}
function serializeEdge(e, n, r) {
	let i = {
		key: n,
		source: r.source.key,
		target: r.target.key
	};
	return isEmpty(r.attributes) || (i.attributes = t$1({}, r.attributes)), e === `mixed` && r.undirected && (i.undirected = !0), i;
}
function validateSerializedNode(e) {
	if (!isPlainObject(e)) throw new n(`Graph.import: invalid serialized node. A serialized node should be a plain object with at least a "key" property.`);
	if (!(`key` in e)) throw new n(`Graph.import: serialized node is missing its key.`);
	if (`attributes` in e && (!isPlainObject(e.attributes) || e.attributes === null)) throw new n(`Graph.import: invalid attributes. Attributes should be a plain object, null or omitted.`);
}
function validateSerializedEdge(e) {
	if (!isPlainObject(e)) throw new n(`Graph.import: invalid serialized edge. A serialized edge should be a plain object with at least a "source" & "target" property.`);
	if (!(`source` in e)) throw new n(`Graph.import: serialized edge is missing its source.`);
	if (!(`target` in e)) throw new n(`Graph.import: serialized edge is missing its target.`);
	if (`attributes` in e && (!isPlainObject(e.attributes) || e.attributes === null)) throw new n(`Graph.import: invalid attributes. Attributes should be a plain object, null or omitted.`);
	if (`undirected` in e && typeof e.undirected != `boolean`) throw new n(`Graph.import: invalid undirectedness information. Undirected should be boolean or omitted.`);
}
var l = incrementalIdStartingFromRandomByte();
var u = /* @__PURE__ */ new Set([
	`directed`,
	`undirected`,
	`mixed`
]);
var d = /* @__PURE__ */ new Set([
	`domain`,
	`_events`,
	`_eventsCount`,
	`_maxListeners`
]);
var f = [
	{
		name: (e) => `${e}Edge`,
		generateKey: !0
	},
	{
		name: (e) => `${e}DirectedEdge`,
		generateKey: !0,
		type: `directed`
	},
	{
		name: (e) => `${e}UndirectedEdge`,
		generateKey: !0,
		type: `undirected`
	},
	{ name: (e) => `${e}EdgeWithKey` },
	{
		name: (e) => `${e}DirectedEdgeWithKey`,
		type: `directed`
	},
	{
		name: (e) => `${e}UndirectedEdgeWithKey`,
		type: `undirected`
	}
];
var p = {
	allowSelfLoops: !0,
	multi: !1,
	type: `mixed`
};
function addNode(e, t, r) {
	if (r && !isPlainObject(r)) throw new n(`Graph.addNode: invalid attributes. Expecting an object but got "${r}"`);
	if (t = `` + t, r ||= {}, e._nodes.has(t)) throw new i$2(`Graph.addNode: the "${t}" node already exist in the graph.`);
	let a = new e.NodeDataClass(t, r);
	return e._nodes.set(t, a), e.emit(`nodeAdded`, {
		key: t,
		attributes: r
	}), a;
}
function unsafeAddNode(e, t, n) {
	let r = new e.NodeDataClass(t, n);
	return e._nodes.set(t, r), e.emit(`nodeAdded`, {
		key: t,
		attributes: n
	}), r;
}
function addEdge(e, t, a, o, s, c, l, u) {
	if (!o && e.type === `undirected`) throw new i$2(`Graph.${t}: you cannot add a directed edge to an undirected graph. Use the #.addEdge or #.addUndirectedEdge instead.`);
	if (o && e.type === `directed`) throw new i$2(`Graph.${t}: you cannot add an undirected edge to a directed graph. Use the #.addEdge or #.addDirectedEdge instead.`);
	if (u && !isPlainObject(u)) throw new n(`Graph.${t}: invalid attributes. Expecting an object but got "${u}"`);
	if (c = `` + c, l = `` + l, u ||= {}, !e.allowSelfLoops && c === l) throw new i$2(`Graph.${t}: source & target are the same ("${c}"), thus creating a loop explicitly forbidden by this graph 'allowSelfLoops' option set to false.`);
	let d = e._nodes.get(c), f = e._nodes.get(l);
	if (!d) throw new r$1(`Graph.${t}: source node "${c}" not found.`);
	if (!f) throw new r$1(`Graph.${t}: target node "${l}" not found.`);
	let p = {
		key: null,
		undirected: o,
		source: c,
		target: l,
		attributes: u
	};
	if (a) s = e._edgeKeyGenerator();
	else if (s = `` + s, e._edges.has(s)) throw new i$2(`Graph.${t}: the "${s}" edge already exists in the graph.`);
	if (!e.multi && (o ? d.undirected[l] !== void 0 : d.out[l] !== void 0)) throw new i$2(`Graph.${t}: an edge linking "${c}" to "${l}" already exists. If you really want to add multiple edges linking those nodes, you should create a multi graph by using the 'multi' option.`);
	let m = new EdgeData(o, s, d, f, u);
	e._edges.set(s, m);
	let h = c === l;
	return o ? (d.undirectedDegree++, f.undirectedDegree++, h && (d.undirectedLoops++, e._undirectedSelfLoopCount++)) : (d.outDegree++, f.inDegree++, h && (d.directedLoops++, e._directedSelfLoopCount++)), e.multi ? m.attachMulti() : m.attach(), o ? e._undirectedSize++ : e._directedSize++, p.key = s, e.emit(`edgeAdded`, p), s;
}
function mergeEdge(e, r, a, o, s, c, l, u, d) {
	if (!o && e.type === `undirected`) throw new i$2(`Graph.${r}: you cannot merge/update a directed edge to an undirected graph. Use the #.mergeEdge/#.updateEdge or #.addUndirectedEdge instead.`);
	if (o && e.type === `directed`) throw new i$2(`Graph.${r}: you cannot merge/update an undirected edge to a directed graph. Use the #.mergeEdge/#.updateEdge or #.addDirectedEdge instead.`);
	if (u) {
		if (d) {
			if (typeof u != `function`) throw new n(`Graph.${r}: invalid updater function. Expecting a function but got "${u}"`);
		} else if (!isPlainObject(u)) throw new n(`Graph.${r}: invalid attributes. Expecting an object but got "${u}"`);
	}
	c = `` + c, l = `` + l;
	let f;
	if (d && (f = u, u = void 0), !e.allowSelfLoops && c === l) throw new i$2(`Graph.${r}: source & target are the same ("${c}"), thus creating a loop explicitly forbidden by this graph 'allowSelfLoops' option set to false.`);
	let p = e._nodes.get(c), m = e._nodes.get(l), h, _;
	if (!a && (h = e._edges.get(s), h)) {
		if ((h.source.key !== c || h.target.key !== l) && (!o || h.source.key !== l || h.target.key !== c)) throw new i$2(`Graph.${r}: inconsistency detected when attempting to merge the "${s}" edge with "${c}" source & "${l}" target vs. ("${h.source.key}", "${h.target.key}").`);
		_ = h;
	}
	if (!_ && !e.multi && p && (_ = o ? p.undirected[l] : p.out[l]), _) {
		let n = [
			_.key,
			!1,
			!1,
			!1
		];
		if (d ? !f : !u) return n;
		if (d) {
			let t = _.attributes;
			_.attributes = f(t), e.emit(`edgeAttributesUpdated`, {
				type: `replace`,
				key: _.key,
				attributes: _.attributes
			});
		} else t$1(_.attributes, u), e.emit(`edgeAttributesUpdated`, {
			type: `merge`,
			key: _.key,
			attributes: _.attributes,
			data: u
		});
		return n;
	}
	u ||= {}, d && f && (u = f(u));
	let v = {
		key: null,
		undirected: o,
		source: c,
		target: l,
		attributes: u
	};
	if (a) s = e._edgeKeyGenerator();
	else if (s = `` + s, e._edges.has(s)) throw new i$2(`Graph.${r}: the "${s}" edge already exists in the graph.`);
	let y = !1, b = !1;
	p || (p = unsafeAddNode(e, c, {}), y = !0, c === l && (m = p, b = !0)), m || (m = unsafeAddNode(e, l, {}), b = !0), h = new EdgeData(o, s, p, m, u), e._edges.set(s, h);
	let x = c === l;
	return o ? (p.undirectedDegree++, m.undirectedDegree++, x && (p.undirectedLoops++, e._undirectedSelfLoopCount++)) : (p.outDegree++, m.inDegree++, x && (p.directedLoops++, e._directedSelfLoopCount++)), e.multi ? h.attachMulti() : h.attach(), o ? e._undirectedSize++ : e._directedSize++, v.key = s, e.emit(`edgeAdded`, v), [
		s,
		!0,
		y,
		b
	];
}
function dropEdgeFromData(e, t) {
	e._edges.delete(t.key);
	let { source: n, target: r, attributes: i } = t, a = t.undirected, o = n === r;
	a ? (n.undirectedDegree--, r.undirectedDegree--, o && (n.undirectedLoops--, e._undirectedSelfLoopCount--)) : (n.outDegree--, r.inDegree--, o && (n.directedLoops--, e._directedSelfLoopCount--)), e.multi ? t.detachMulti() : t.detach(), a ? e._undirectedSize-- : e._directedSize--, e.emit(`edgeDropped`, {
		key: t.key,
		attributes: i,
		source: n.key,
		target: r.key,
		undirected: a
	});
}
var m = class Graph extends EventEmitter {
	constructor(e) {
		if (super(), e = t$1({}, p, e), typeof e.multi != `boolean`) throw new n(`Graph.constructor: invalid 'multi' option. Expecting a boolean but got "${e.multi}".`);
		if (!u.has(e.type)) throw new n(`Graph.constructor: invalid 'type' option. Should be one of "mixed", "directed" or "undirected" but got "${e.type}".`);
		if (typeof e.allowSelfLoops != `boolean`) throw new n(`Graph.constructor: invalid 'allowSelfLoops' option. Expecting a boolean but got "${e.allowSelfLoops}".`);
		let r = e.type === `mixed` ? MixedNodeData : e.type === `directed` ? DirectedNodeData : UndirectedNodeData;
		privateProperty(this, `NodeDataClass`, r);
		let i = `geid_` + l() + `_`, a = 0, edgeKeyGenerator = () => {
			let e;
			do
				e = i + a++;
			while (this._edges.has(e));
			return e;
		};
		privateProperty(this, `_attributes`, {}), privateProperty(this, `_nodes`, /* @__PURE__ */ new Map()), privateProperty(this, `_edges`, /* @__PURE__ */ new Map()), privateProperty(this, `_directedSize`, 0), privateProperty(this, `_undirectedSize`, 0), privateProperty(this, `_directedSelfLoopCount`, 0), privateProperty(this, `_undirectedSelfLoopCount`, 0), privateProperty(this, `_edgeKeyGenerator`, edgeKeyGenerator), privateProperty(this, `_options`, e), d.forEach((e) => privateProperty(this, e, this[e])), readOnlyProperty(this, `order`, () => this._nodes.size), readOnlyProperty(this, `size`, () => this._edges.size), readOnlyProperty(this, `directedSize`, () => this._directedSize), readOnlyProperty(this, `undirectedSize`, () => this._undirectedSize), readOnlyProperty(this, `selfLoopCount`, () => this._directedSelfLoopCount + this._undirectedSelfLoopCount), readOnlyProperty(this, `directedSelfLoopCount`, () => this._directedSelfLoopCount), readOnlyProperty(this, `undirectedSelfLoopCount`, () => this._undirectedSelfLoopCount), readOnlyProperty(this, `multi`, this._options.multi), readOnlyProperty(this, `type`, this._options.type), readOnlyProperty(this, `allowSelfLoops`, this._options.allowSelfLoops), readOnlyProperty(this, `implementation`, () => `graphology`);
	}
	_resetInstanceCounters() {
		this._directedSize = 0, this._undirectedSize = 0, this._directedSelfLoopCount = 0, this._undirectedSelfLoopCount = 0;
	}
	hasNode(e) {
		return this._nodes.has(`` + e);
	}
	hasDirectedEdge(e, t) {
		if (this.type === `undirected`) return !1;
		if (arguments.length === 1) {
			let t = `` + e, n = this._edges.get(t);
			return !!n && !n.undirected;
		} else if (arguments.length === 2) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			return n ? n.out.hasOwnProperty(t) : !1;
		}
		throw new n(`Graph.hasDirectedEdge: invalid arity (${arguments.length}, instead of 1 or 2). You can either ask for an edge id or for the existence of an edge between a source & a target.`);
	}
	hasUndirectedEdge(e, t) {
		if (this.type === `directed`) return !1;
		if (arguments.length === 1) {
			let t = `` + e, n = this._edges.get(t);
			return !!n && n.undirected;
		} else if (arguments.length === 2) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			return n ? n.undirected.hasOwnProperty(t) : !1;
		}
		throw new n(`Graph.hasDirectedEdge: invalid arity (${arguments.length}, instead of 1 or 2). You can either ask for an edge id or for the existence of an edge between a source & a target.`);
	}
	hasEdge(e, t) {
		if (arguments.length === 1) {
			let t = `` + e;
			return this._edges.has(t);
		} else if (arguments.length === 2) {
			e = `` + e, t = `` + t;
			let n = this._nodes.get(e);
			return n ? n.out !== void 0 && n.out.hasOwnProperty(t) || n.undirected !== void 0 && n.undirected.hasOwnProperty(t) : !1;
		}
		throw new n(`Graph.hasEdge: invalid arity (${arguments.length}, instead of 1 or 2). You can either ask for an edge id or for the existence of an edge between a source & a target.`);
	}
	directedEdge(e, t) {
		if (this.type === `undirected`) return;
		if (e = `` + e, t = `` + t, this.multi) throw new i$2(`Graph.directedEdge: this method is irrelevant with multigraphs since there might be multiple edges between source & target. See #.directedEdges instead.`);
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.directedEdge: could not find the "${e}" source node in the graph.`);
		if (!this._nodes.has(t)) throw new r$1(`Graph.directedEdge: could not find the "${t}" target node in the graph.`);
		let a = n.out && n.out[t] || void 0;
		if (a) return a.key;
	}
	undirectedEdge(e, t) {
		if (this.type === `directed`) return;
		if (e = `` + e, t = `` + t, this.multi) throw new i$2(`Graph.undirectedEdge: this method is irrelevant with multigraphs since there might be multiple edges between source & target. See #.undirectedEdges instead.`);
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.undirectedEdge: could not find the "${e}" source node in the graph.`);
		if (!this._nodes.has(t)) throw new r$1(`Graph.undirectedEdge: could not find the "${t}" target node in the graph.`);
		let a = n.undirected && n.undirected[t] || void 0;
		if (a) return a.key;
	}
	edge(e, t) {
		if (this.multi) throw new i$2(`Graph.edge: this method is irrelevant with multigraphs since there might be multiple edges between source & target. See #.edges instead.`);
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.edge: could not find the "${e}" source node in the graph.`);
		if (!this._nodes.has(t)) throw new r$1(`Graph.edge: could not find the "${t}" target node in the graph.`);
		let a = n.out && n.out[t] || n.undirected && n.undirected[t] || void 0;
		if (a) return a.key;
	}
	areDirectedNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areDirectedNeighbors: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? !1 : t in n.in || t in n.out;
	}
	areOutNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areOutNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `undirected` && t in n.out;
	}
	areInNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areInNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `undirected` && t in n.in;
	}
	areUndirectedNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areUndirectedNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `directed` && t in n.undirected;
	}
	areNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `undirected` && (t in n.in || t in n.out) || this.type !== `directed` && t in n.undirected;
	}
	areInboundNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areInboundNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `undirected` && t in n.in || this.type !== `directed` && t in n.undirected;
	}
	areOutboundNeighbors(e, t) {
		e = `` + e, t = `` + t;
		let n = this._nodes.get(e);
		if (!n) throw new r$1(`Graph.areOutboundNeighbors: could not find the "${e}" node in the graph.`);
		return this.type !== `undirected` && t in n.out || this.type !== `directed` && t in n.undirected;
	}
	inDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.inDegree: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.inDegree;
	}
	outDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.outDegree: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.outDegree;
	}
	directedDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.directedDegree: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.inDegree + t.outDegree;
	}
	undirectedDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.undirectedDegree: could not find the "${e}" node in the graph.`);
		return this.type === `directed` ? 0 : t.undirectedDegree;
	}
	inboundDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.inboundDegree: could not find the "${e}" node in the graph.`);
		let n = 0;
		return this.type !== `directed` && (n += t.undirectedDegree), this.type !== `undirected` && (n += t.inDegree), n;
	}
	outboundDegree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.outboundDegree: could not find the "${e}" node in the graph.`);
		let n = 0;
		return this.type !== `directed` && (n += t.undirectedDegree), this.type !== `undirected` && (n += t.outDegree), n;
	}
	degree(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.degree: could not find the "${e}" node in the graph.`);
		let n = 0;
		return this.type !== `directed` && (n += t.undirectedDegree), this.type !== `undirected` && (n += t.inDegree + t.outDegree), n;
	}
	inDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.inDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.inDegree - t.directedLoops;
	}
	outDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.outDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.outDegree - t.directedLoops;
	}
	directedDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.directedDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		return this.type === `undirected` ? 0 : t.inDegree + t.outDegree - t.directedLoops * 2;
	}
	undirectedDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.undirectedDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		return this.type === `directed` ? 0 : t.undirectedDegree - t.undirectedLoops * 2;
	}
	inboundDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.inboundDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		let n = 0, i = 0;
		return this.type !== `directed` && (n += t.undirectedDegree, i += t.undirectedLoops * 2), this.type !== `undirected` && (n += t.inDegree, i += t.directedLoops), n - i;
	}
	outboundDegreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.outboundDegreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		let n = 0, i = 0;
		return this.type !== `directed` && (n += t.undirectedDegree, i += t.undirectedLoops * 2), this.type !== `undirected` && (n += t.outDegree, i += t.directedLoops), n - i;
	}
	degreeWithoutSelfLoops(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.degreeWithoutSelfLoops: could not find the "${e}" node in the graph.`);
		let n = 0, i = 0;
		return this.type !== `directed` && (n += t.undirectedDegree, i += t.undirectedLoops * 2), this.type !== `undirected` && (n += t.inDegree + t.outDegree, i += t.directedLoops * 2), n - i;
	}
	source(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.source: could not find the "${e}" edge in the graph.`);
		return t.source.key;
	}
	target(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.target: could not find the "${e}" edge in the graph.`);
		return t.target.key;
	}
	extremities(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.extremities: could not find the "${e}" edge in the graph.`);
		return [t.source.key, t.target.key];
	}
	opposite(e, t) {
		e = `` + e, t = `` + t;
		let n = this._edges.get(t);
		if (!n) throw new r$1(`Graph.opposite: could not find the "${t}" edge in the graph.`);
		let i = n.source.key, a = n.target.key;
		if (e === i) return a;
		if (e === a) return i;
		throw new r$1(`Graph.opposite: the "${e}" node is not attached to the "${t}" edge (${i}, ${a}).`);
	}
	hasExtremity(e, t) {
		e = `` + e, t = `` + t;
		let n = this._edges.get(e);
		if (!n) throw new r$1(`Graph.hasExtremity: could not find the "${e}" edge in the graph.`);
		return n.source.key === t || n.target.key === t;
	}
	isUndirected(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.isUndirected: could not find the "${e}" edge in the graph.`);
		return t.undirected;
	}
	isDirected(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.isDirected: could not find the "${e}" edge in the graph.`);
		return !t.undirected;
	}
	isSelfLoop(e) {
		e = `` + e;
		let t = this._edges.get(e);
		if (!t) throw new r$1(`Graph.isSelfLoop: could not find the "${e}" edge in the graph.`);
		return t.source === t.target;
	}
	addNode(e, t) {
		return addNode(this, e, t).key;
	}
	mergeNode(e, r) {
		if (r && !isPlainObject(r)) throw new n(`Graph.mergeNode: invalid attributes. Expecting an object but got "${r}"`);
		e = `` + e, r ||= {};
		let i = this._nodes.get(e);
		return i ? (r && (t$1(i.attributes, r), this.emit(`nodeAttributesUpdated`, {
			type: `merge`,
			key: e,
			attributes: i.attributes,
			data: r
		})), [e, !1]) : (i = new this.NodeDataClass(e, r), this._nodes.set(e, i), this.emit(`nodeAdded`, {
			key: e,
			attributes: r
		}), [e, !0]);
	}
	updateNode(e, t) {
		if (t && typeof t != `function`) throw new n(`Graph.updateNode: invalid updater function. Expecting a function but got "${t}"`);
		e = `` + e;
		let r = this._nodes.get(e);
		if (r) {
			if (t) {
				let n = r.attributes;
				r.attributes = t(n), this.emit(`nodeAttributesUpdated`, {
					type: `replace`,
					key: e,
					attributes: r.attributes
				});
			}
			return [e, !1];
		}
		let i = t ? t({}) : {};
		return r = new this.NodeDataClass(e, i), this._nodes.set(e, r), this.emit(`nodeAdded`, {
			key: e,
			attributes: i
		}), [e, !0];
	}
	dropNode(e) {
		e = `` + e;
		let t = this._nodes.get(e);
		if (!t) throw new r$1(`Graph.dropNode: could not find the "${e}" node in the graph.`);
		let n;
		if (this.type !== `undirected`) {
			for (let e in t.out) {
				n = t.out[e];
				do
					dropEdgeFromData(this, n), n = n.next;
				while (n);
			}
			for (let e in t.in) {
				n = t.in[e];
				do
					dropEdgeFromData(this, n), n = n.next;
				while (n);
			}
		}
		if (this.type !== `directed`) for (let e in t.undirected) {
			n = t.undirected[e];
			do
				dropEdgeFromData(this, n), n = n.next;
			while (n);
		}
		this._nodes.delete(e), this.emit(`nodeDropped`, {
			key: e,
			attributes: t.attributes
		});
	}
	dropEdge(e) {
		let t;
		if (arguments.length > 1) {
			let e = `` + arguments[0], n = `` + arguments[1];
			if (t = getMatchingEdge(this, e, n, this.type), !t) throw new r$1(`Graph.dropEdge: could not find the "${e}" -> "${n}" edge in the graph.`);
		} else if (e = `` + e, t = this._edges.get(e), !t) throw new r$1(`Graph.dropEdge: could not find the "${e}" edge in the graph.`);
		return dropEdgeFromData(this, t), this;
	}
	dropDirectedEdge(e, t) {
		if (arguments.length < 2) throw new i$2(`Graph.dropDirectedEdge: it does not make sense to try and drop a directed edge by key. What if the edge with this key is undirected? Use #.dropEdge for this purpose instead.`);
		if (this.multi) throw new i$2(`Graph.dropDirectedEdge: cannot use a {source,target} combo when dropping an edge in a MultiGraph since we cannot infer the one you want to delete as there could be multiple ones.`);
		e = `` + e, t = `` + t;
		let n = getMatchingEdge(this, e, t, `directed`);
		if (!n) throw new r$1(`Graph.dropDirectedEdge: could not find a "${e}" -> "${t}" edge in the graph.`);
		return dropEdgeFromData(this, n), this;
	}
	dropUndirectedEdge(e, t) {
		if (arguments.length < 2) throw new i$2(`Graph.dropUndirectedEdge: it does not make sense to drop a directed edge by key. What if the edge with this key is undirected? Use #.dropEdge for this purpose instead.`);
		if (this.multi) throw new i$2(`Graph.dropUndirectedEdge: cannot use a {source,target} combo when dropping an edge in a MultiGraph since we cannot infer the one you want to delete as there could be multiple ones.`);
		let n = getMatchingEdge(this, e, t, `undirected`);
		if (!n) throw new r$1(`Graph.dropUndirectedEdge: could not find a "${e}" -> "${t}" edge in the graph.`);
		return dropEdgeFromData(this, n), this;
	}
	clear() {
		this._edges.clear(), this._nodes.clear(), this._resetInstanceCounters(), this.emit(`cleared`);
	}
	clearEdges() {
		let e = this._nodes.values(), t;
		for (; t = e.next(), t.done !== !0;) t.value.clear();
		this._edges.clear(), this._resetInstanceCounters(), this.emit(`edgesCleared`);
	}
	getAttribute(e) {
		return this._attributes[e];
	}
	getAttributes() {
		return this._attributes;
	}
	hasAttribute(e) {
		return this._attributes.hasOwnProperty(e);
	}
	setAttribute(e, t) {
		return this._attributes[e] = t, this.emit(`attributesUpdated`, {
			type: `set`,
			attributes: this._attributes,
			name: e
		}), this;
	}
	updateAttribute(e, t) {
		if (typeof t != `function`) throw new n(`Graph.updateAttribute: updater should be a function.`);
		let r = this._attributes[e];
		return this._attributes[e] = t(r), this.emit(`attributesUpdated`, {
			type: `set`,
			attributes: this._attributes,
			name: e
		}), this;
	}
	removeAttribute(e) {
		return delete this._attributes[e], this.emit(`attributesUpdated`, {
			type: `remove`,
			attributes: this._attributes,
			name: e
		}), this;
	}
	replaceAttributes(e) {
		if (!isPlainObject(e)) throw new n(`Graph.replaceAttributes: provided attributes are not a plain object.`);
		return this._attributes = e, this.emit(`attributesUpdated`, {
			type: `replace`,
			attributes: this._attributes
		}), this;
	}
	mergeAttributes(e) {
		if (!isPlainObject(e)) throw new n(`Graph.mergeAttributes: provided attributes are not a plain object.`);
		return t$1(this._attributes, e), this.emit(`attributesUpdated`, {
			type: `merge`,
			attributes: this._attributes,
			data: e
		}), this;
	}
	updateAttributes(e) {
		if (typeof e != `function`) throw new n(`Graph.updateAttributes: provided updater is not a function.`);
		return this._attributes = e(this._attributes), this.emit(`attributesUpdated`, {
			type: `update`,
			attributes: this._attributes
		}), this;
	}
	updateEachNodeAttributes(e, t) {
		if (typeof e != `function`) throw new n(`Graph.updateEachNodeAttributes: expecting an updater function.`);
		if (t && !validateHints(t)) throw new n(`Graph.updateEachNodeAttributes: invalid hints. Expecting an object having the following shape: {attributes?: [string]}`);
		let r = this._nodes.values(), i, a;
		for (; i = r.next(), i.done !== !0;) a = i.value, a.attributes = e(a.key, a.attributes);
		this.emit(`eachNodeAttributesUpdated`, { hints: t || null });
	}
	updateEachEdgeAttributes(e, t) {
		if (typeof e != `function`) throw new n(`Graph.updateEachEdgeAttributes: expecting an updater function.`);
		if (t && !validateHints(t)) throw new n(`Graph.updateEachEdgeAttributes: invalid hints. Expecting an object having the following shape: {attributes?: [string]}`);
		let r = this._edges.values(), i, a, o, s;
		for (; i = r.next(), i.done !== !0;) a = i.value, o = a.source, s = a.target, a.attributes = e(a.key, a.attributes, o.key, s.key, o.attributes, s.attributes, a.undirected);
		this.emit(`eachEdgeAttributesUpdated`, { hints: t || null });
	}
	forEachAdjacencyEntry(e) {
		if (typeof e != `function`) throw new n(`Graph.forEachAdjacencyEntry: expecting a callback.`);
		forEachAdjacency(!1, !1, !1, this, e);
	}
	forEachAdjacencyEntryWithOrphans(e) {
		if (typeof e != `function`) throw new n(`Graph.forEachAdjacencyEntryWithOrphans: expecting a callback.`);
		forEachAdjacency(!1, !1, !0, this, e);
	}
	forEachAssymetricAdjacencyEntry(e) {
		if (typeof e != `function`) throw new n(`Graph.forEachAssymetricAdjacencyEntry: expecting a callback.`);
		forEachAdjacency(!1, !0, !1, this, e);
	}
	forEachAssymetricAdjacencyEntryWithOrphans(e) {
		if (typeof e != `function`) throw new n(`Graph.forEachAssymetricAdjacencyEntryWithOrphans: expecting a callback.`);
		forEachAdjacency(!1, !0, !0, this, e);
	}
	nodes() {
		return Array.from(this._nodes.keys());
	}
	forEachNode(e) {
		if (typeof e != `function`) throw new n(`Graph.forEachNode: expecting a callback.`);
		let t = this._nodes.values(), r, i;
		for (; r = t.next(), r.done !== !0;) i = r.value, e(i.key, i.attributes);
	}
	findNode(e) {
		if (typeof e != `function`) throw new n(`Graph.findNode: expecting a callback.`);
		let t = this._nodes.values(), r, i;
		for (; r = t.next(), r.done !== !0;) if (i = r.value, e(i.key, i.attributes)) return i.key;
	}
	mapNodes(e) {
		if (typeof e != `function`) throw new n(`Graph.mapNode: expecting a callback.`);
		let t = this._nodes.values(), r, i, a = Array(this.order), o = 0;
		for (; r = t.next(), r.done !== !0;) i = r.value, a[o++] = e(i.key, i.attributes);
		return a;
	}
	someNode(e) {
		if (typeof e != `function`) throw new n(`Graph.someNode: expecting a callback.`);
		let t = this._nodes.values(), r, i;
		for (; r = t.next(), r.done !== !0;) if (i = r.value, e(i.key, i.attributes)) return !0;
		return !1;
	}
	everyNode(e) {
		if (typeof e != `function`) throw new n(`Graph.everyNode: expecting a callback.`);
		let t = this._nodes.values(), r, i;
		for (; r = t.next(), r.done !== !0;) if (i = r.value, !e(i.key, i.attributes)) return !1;
		return !0;
	}
	filterNodes(e) {
		if (typeof e != `function`) throw new n(`Graph.filterNodes: expecting a callback.`);
		let t = this._nodes.values(), r, i, a = [];
		for (; r = t.next(), r.done !== !0;) i = r.value, e(i.key, i.attributes) && a.push(i.key);
		return a;
	}
	reduceNodes(e, t) {
		if (typeof e != `function`) throw new n(`Graph.reduceNodes: expecting a callback.`);
		if (arguments.length < 2) throw new n(`Graph.reduceNodes: missing initial value. You must provide it because the callback takes more than one argument and we cannot infer the initial value from the first iteration, as you could with a simple array.`);
		let r = t, i = this._nodes.values(), a, o;
		for (; a = i.next(), a.done !== !0;) o = a.value, r = e(r, o.key, o.attributes);
		return r;
	}
	nodeEntries() {
		let e = this._nodes.values();
		return {
			[Symbol.iterator]() {
				return this;
			},
			next() {
				let t = e.next();
				if (t.done) return t;
				let n = t.value;
				return {
					value: {
						node: n.key,
						attributes: n.attributes
					},
					done: !1
				};
			}
		};
	}
	export() {
		let e = Array(this._nodes.size), t = 0;
		this._nodes.forEach((n, r) => {
			e[t++] = serializeNode(r, n);
		});
		let n = Array(this._edges.size);
		return t = 0, this._edges.forEach((e, r) => {
			n[t++] = serializeEdge(this.type, r, e);
		}), {
			options: {
				type: this.type,
				multi: this.multi,
				allowSelfLoops: this.allowSelfLoops
			},
			attributes: this.getAttributes(),
			nodes: e,
			edges: n
		};
	}
	import(e, t = !1) {
		if (e instanceof Graph) return e.forEachNode((e, n) => {
			t ? this.mergeNode(e, n) : this.addNode(e, n);
		}), e.forEachEdge((e, n, r, i, a, o, s) => {
			t ? s ? this.mergeUndirectedEdgeWithKey(e, r, i, n) : this.mergeDirectedEdgeWithKey(e, r, i, n) : s ? this.addUndirectedEdgeWithKey(e, r, i, n) : this.addDirectedEdgeWithKey(e, r, i, n);
		}), this;
		if (!isPlainObject(e)) throw new n(`Graph.import: invalid argument. Expecting a serialized graph or, alternatively, a Graph instance.`);
		if (e.attributes) {
			if (!isPlainObject(e.attributes)) throw new n(`Graph.import: invalid attributes. Expecting a plain object.`);
			t ? this.mergeAttributes(e.attributes) : this.replaceAttributes(e.attributes);
		}
		let r, i, a, o, s;
		if (e.nodes) {
			if (a = e.nodes, !Array.isArray(a)) throw new n(`Graph.import: invalid nodes. Expecting an array.`);
			for (r = 0, i = a.length; r < i; r++) {
				o = a[r], validateSerializedNode(o);
				let { key: e, attributes: n } = o;
				t ? this.mergeNode(e, n) : this.addNode(e, n);
			}
		}
		if (e.edges) {
			let o = !1;
			if (this.type === `undirected` && (o = !0), a = e.edges, !Array.isArray(a)) throw new n(`Graph.import: invalid edges. Expecting an array.`);
			for (r = 0, i = a.length; r < i; r++) {
				s = a[r], validateSerializedEdge(s);
				let { source: e, target: n, attributes: i, undirected: c = o } = s, l;
				`key` in s ? (l = t ? c ? this.mergeUndirectedEdgeWithKey : this.mergeDirectedEdgeWithKey : c ? this.addUndirectedEdgeWithKey : this.addDirectedEdgeWithKey, l.call(this, s.key, e, n, i)) : (l = t ? c ? this.mergeUndirectedEdge : this.mergeDirectedEdge : c ? this.addUndirectedEdge : this.addDirectedEdge, l.call(this, e, n, i));
			}
		}
		return this;
	}
	nullCopy(e) {
		let n = new Graph(t$1({}, this._options, e));
		return n.replaceAttributes(t$1({}, this.getAttributes())), n;
	}
	emptyCopy(e) {
		let n = this.nullCopy(e);
		return this._nodes.forEach((e, r) => {
			let i = t$1({}, e.attributes);
			e = new n.NodeDataClass(r, i), n._nodes.set(r, e);
		}), n;
	}
	copy(e) {
		if (e ||= {}, typeof e.type == `string` && e.type !== this.type && e.type !== `mixed`) throw new i$2(`Graph.copy: cannot create an incompatible copy from "${this.type}" type to "${e.type}" because this would mean losing information about the current graph.`);
		if (typeof e.multi == `boolean` && e.multi !== this.multi && e.multi !== !0) throw new i$2(`Graph.copy: cannot create an incompatible copy by downgrading a multi graph to a simple one because this would mean losing information about the current graph.`);
		if (typeof e.allowSelfLoops == `boolean` && e.allowSelfLoops !== this.allowSelfLoops && e.allowSelfLoops !== !0) throw new i$2(`Graph.copy: cannot create an incompatible copy from a graph allowing self loops to one that does not because this would mean losing information about the current graph.`);
		let n = this.emptyCopy(e), r = this._edges.values(), a, o;
		for (; a = r.next(), a.done !== !0;) o = a.value, addEdge(n, `copy`, !1, o.undirected, o.key, o.source.key, o.target.key, t$1({}, o.attributes));
		return n;
	}
	toJSON() {
		return this.export();
	}
	toString() {
		return `[object Graph]`;
	}
	inspect() {
		let e = {};
		this._nodes.forEach((t, n) => {
			e[n] = t.attributes;
		});
		let t = {}, n = {};
		this._edges.forEach((e, r) => {
			let i = e.undirected ? `--` : `->`, a = ``, o = e.source.key, s = e.target.key, c;
			e.undirected && o > s && (c = o, o = s, s = c);
			let l = `(${o})${i}(${s})`;
			r.startsWith(`geid_`) ? this.multi && (n[l] === void 0 ? n[l] = 0 : n[l]++, a += `${n[l]}. `) : a += `[${r}]: `, a += l, t[a] = e.attributes;
		});
		let r = {};
		for (let e in this) this.hasOwnProperty(e) && !d.has(e) && typeof this[e] != `function` && typeof e != `symbol` && (r[e] = this[e]);
		return r.attributes = this._attributes, r.nodes = e, r.edges = t, privateProperty(r, `constructor`, this.constructor), r;
	}
};
typeof Symbol < `u` && (m.prototype[Symbol.for(`nodejs.util.inspect.custom`)] = m.prototype.inspect), f.forEach((e) => {
	[
		`add`,
		`merge`,
		`update`
	].forEach((t) => {
		let n = e.name(t), r = t === `add` ? addEdge : mergeEdge;
		e.generateKey ? m.prototype[n] = function(i, a, o) {
			return r(this, n, !0, (e.type || this.type) === `undirected`, null, i, a, o, t === `update`);
		} : m.prototype[n] = function(i, a, o, s) {
			return r(this, n, !1, (e.type || this.type) === `undirected`, i, a, o, s, t === `update`);
		};
	});
}), attachNodeAttributesMethods(m), attachEdgeAttributesMethods(m), attachEdgeIterationMethods(m), attachNeighborIterationMethods(m);
var DirectedGraph = class extends m {
	constructor(e) {
		let r = t$1({ type: `directed` }, e);
		if (`multi` in r && r.multi !== !1) throw new n(`DirectedGraph.from: inconsistent indication that the graph should be multi in given options!`);
		if (r.type !== `directed`) throw new n(`DirectedGraph.from: inconsistent "` + r.type + `" type in given options!`);
		super(r);
	}
};
var UndirectedGraph = class extends m {
	constructor(e) {
		let r = t$1({ type: `undirected` }, e);
		if (`multi` in r && r.multi !== !1) throw new n(`UndirectedGraph.from: inconsistent indication that the graph should be multi in given options!`);
		if (r.type !== `undirected`) throw new n(`UndirectedGraph.from: inconsistent "` + r.type + `" type in given options!`);
		super(r);
	}
};
var MultiGraph = class extends m {
	constructor(e) {
		let r = t$1({ multi: !0 }, e);
		if (`multi` in r && r.multi !== !0) throw new n(`MultiGraph.from: inconsistent indication that the graph should be simple in given options!`);
		super(r);
	}
};
var MultiDirectedGraph = class extends m {
	constructor(e) {
		let r = t$1({
			type: `directed`,
			multi: !0
		}, e);
		if (`multi` in r && r.multi !== !0) throw new n(`MultiDirectedGraph.from: inconsistent indication that the graph should be simple in given options!`);
		if (r.type !== `directed`) throw new n(`MultiDirectedGraph.from: inconsistent "` + r.type + `" type in given options!`);
		super(r);
	}
};
var MultiUndirectedGraph = class extends m {
	constructor(e) {
		let r = t$1({
			type: `undirected`,
			multi: !0
		}, e);
		if (`multi` in r && r.multi !== !0) throw new n(`MultiUndirectedGraph.from: inconsistent indication that the graph should be simple in given options!`);
		if (r.type !== `undirected`) throw new n(`MultiUndirectedGraph.from: inconsistent "` + r.type + `" type in given options!`);
		super(r);
	}
};
function attachStaticFromMethod(e) {
	e.from = function(n, r) {
		let i = new e(t$1({}, n.options, r));
		return i.import(n), i;
	};
}
attachStaticFromMethod(m), attachStaticFromMethod(DirectedGraph), attachStaticFromMethod(UndirectedGraph), attachStaticFromMethod(MultiGraph), attachStaticFromMethod(MultiDirectedGraph), attachStaticFromMethod(MultiUndirectedGraph), m.Graph = m, m.DirectedGraph = DirectedGraph, m.UndirectedGraph = UndirectedGraph, m.MultiGraph = MultiGraph, m.MultiDirectedGraph = MultiDirectedGraph, m.MultiUndirectedGraph = MultiUndirectedGraph, m.InvalidArgumentsGraphError = n, m.NotFoundGraphError = r$1, m.UsageGraphError = i$2;
//#endregion
//#region node_modules/@likec4/core/dist/utils/graphology/index.mjs
var i$1 = c$1();
var a$1 = __toESM(l$1(), 1);
i$1.topologicalSort;
a$1.default;
//#endregion
//#region node_modules/@likec4/core/dist/chunks/Builder.view-element.mjs
function ancestorsOfNode(e, t) {
	let n = [], r = e.parent;
	for (; r;) {
		let e = nonNullable(t.get(r), `Parent node ${r} not found`);
		n.push(e), r = e.parent;
	}
	return n;
}
function linkNodesWithEdges(e, t) {
	for (let r of t) {
		let t = e.get(r.source), i = e.get(r.target);
		invariant(t, `Source node ${r.source} not found`), invariant(i, `Target node ${r.target} not found`), t.outEdges.push(r.id), i.inEdges.push(r.id);
		let a = ancestorsOfNode(t, e), s = ancestorsOfNode(i, e), c = a.length > 0 && s.length > 0 ? t$19(commonHead(t$6(a), t$6(s))) : null;
		r.parent = c ? c.id : null;
		for (let e of a) {
			if (e === c) break;
			e.outEdges.push(r.id);
		}
		for (let e of s) {
			if (e === c) break;
			e.inEdges.push(r.id);
		}
	}
}
function elementExprToPredicate(e) {
	if (L.isCustom(e)) return elementExprToPredicate(e.custom.expr);
	if (L.isWhere(e)) {
		let t = elementExprToPredicate(e.where.expr), n = whereOperatorAsPredicate(e.where.condition);
		return (e) => t(e) && n(e);
	}
	if (L.isElementKindExpr(e)) return e.isEqual ? (t) => t.kind === e.elementKind : (t) => t.kind !== e.elementKind;
	if (L.isElementTagExpr(e)) return e.isEqual ? ({ tags: t }) => t.includes(e.elementTag) : ({ tags: t }) => !t.includes(e.elementTag);
	if (L.isWildcard(e)) return () => !0;
	if (L.isModelRef(e)) {
		let t = N.flatten(e.ref);
		if (e.selector === `expanded`) return (e) => e.id === t || parentFqn(e.id) === t;
		if (e.selector === `descendants` || e.selector === `children`) {
			let e = t + `.`;
			return (t) => t.id.startsWith(e);
		}
		return (e) => e.id === t;
	}
	nonexhaustive(e);
}
function createRelationExpressionToPredicates(e) {
	return function relationExpressionToPredicates(t) {
		switch (!0) {
			case R.isCustom(t): return relationExpressionToPredicates(t.customRelation.expr);
			case R.isWhere(t): {
				let e = relationExpressionToPredicates(t.where.expr), n = whereOperatorAsPredicate(t.where.condition);
				return (t) => e(t) && n({
					source: {
						tags: t.source.tags,
						kind: t.source.kind,
						metadata: t.source.metadata
					},
					target: {
						tags: t.target.tags,
						kind: t.target.kind,
						metadata: t.target.metadata
					},
					...t.tags && { tags: t.tags },
					...t.kind && { kind: t.kind },
					...t.metadata && { metadata: t.metadata }
				});
			}
			case R.isDirect(t): {
				let n = e(t.source), r = e(t.target);
				return (e) => n(e.source) && r(e.target) || !!t.isBidirectional && n(e.target) && r(e.source);
			}
			case R.isInOut(t): {
				let n = e(t.inout);
				return (e) => n(e.source) || n(e.target);
			}
			case R.isIncoming(t): {
				let n = e(t.incoming);
				return (e) => n(e.target);
			}
			case R.isOutgoing(t): {
				let n = e(t.outgoing);
				return (e) => n(e.source);
			}
			default: nonexhaustive(t);
		}
	};
}
var kt = createRelationExpressionToPredicates(elementExprToPredicate);
function ensureParentsFirst(e) {
	let t = [], n = [...e], r;
	for (; r = n.shift();) {
		let e = r.parent;
		if (e) {
			let r = [];
			for (; e;) {
				let t = n.findIndex((t) => t.id === e);
				if (t < 0) break;
				let [i] = n.splice(t, 1);
				if (!i) throw Error(`Invalid state, should not happen`);
				r.unshift(i), e = i.parent;
			}
			t.push(...r);
		}
		t.push(r);
	}
	return t;
}
function updateChildren(e) {
	return e.forEach((t) => {
		t.children.length > 0 && (t.children = e.reduce((e, n) => (n.parent === t.id && e.push(n.id), e), []));
	}), e;
}
function topologicalSort(e) {
	let t = ensureParentsFirst([...e.nodes.values()]), r = [...e.edges];
	if (t.length < 2 || r.length === 0) return {
		nodes: t,
		edges: r
	};
	let getNode = (t) => nonNullable(e.nodes.get(t)), i = new m({
		multi: !0,
		allowSelfLoops: !0,
		type: `directed`
	}), [a, o] = t$13(t$32(r, t$18((e) => {
		let t = getNode(e.source), n = getNode(e.target), r = e.parent ? getNode(e.parent) : null;
		return {
			id: e.id,
			edge: e,
			parent: r,
			source: t,
			target: n
		};
	})), ({ source: e, target: t }) => e.children.length === 0 && t.children.length === 0), s = [], addEdgeToGraph = (e) => {
		i.mergeNode(e.source), i.mergeNode(e.target), s.push(e), (0, a$1.default)(i, e.source, e.target) || i.mergeDirectedEdge(e.source, e.target);
	};
	for (let { edge: t, source: n, target: r } of a) if (addEdgeToGraph(t), r.parent && r.parent !== t.parent && t$32(ancestorsOfNode(r, e.nodes), t$3((e) => e.inEdges.includes(t.id)), t$24((e) => {
		i.mergeNode(e.id), (0, a$1.default)(i, t.source, e.id) || i.mergeDirectedEdge(t.source, e.id), (0, a$1.default)(i, e.id, t.target) || i.mergeDirectedEdge(e.id, t.target);
	})), n.parent) {
		let e = getNode(n.parent);
		i.mergeNode(e.id), (0, a$1.default)(i, e.id, n.id) || i.mergeDirectedEdge(e.id, n.id), r.parent && r.parent !== n.parent && ((0, a$1.default)(i, e.id, r.parent) || i.mergeDirectedEdge(e.id, r.parent));
	}
	for (let { edge: e } of o) addEdgeToGraph(e);
	invariant(s.length === r.length, `Not all edges were added to the graph`);
	let c = (0, i$1.topologicalSort)(i), l = [], u = t.slice();
	for (let e of c) {
		let t = u.findIndex((t) => t.id === e);
		invariant(t >= 0, `Node "${e}" not found`), l.push(...u.splice(t, 1));
	}
	return u.length > 0 && l.length > 0 && (l = l.flatMap((e) => {
		if (u.length === 0) return e;
		let n = t.slice(0, t.indexOf(e)).filter((e) => u.includes(e));
		return n.length > 0 ? (u = u.filter((e) => !n.includes(e)), [...n, e]) : e;
	})), l.push(...u), {
		nodes: updateChildren(ensureParentsFirst(l)),
		edges: s
	};
}
function calcViewLayoutHash(e) {
	return e.hash = objectHash({
		id: e.id,
		__: e._type ?? `element`,
		autoLayout: e.autoLayout,
		nodes: t$32(e.nodes, t$18((e) => ({
			id: e.id,
			icon: e$4(e.icon) && e.icon !== `none` ? `Y` : `N`,
			title: e.title,
			description: flattenMarkdownOrString(e.description),
			technology: e.technology ?? null,
			shape: e.shape,
			size: e.style.size ?? null,
			iconSize: e.style.iconSize ?? null,
			iconColor: e.style.iconColor ?? null,
			iconPosition: e.style.iconPosition ?? null,
			border: e.style.border ?? null,
			opacity: e.style.opacity ?? null,
			multiple: e.style.multiple ?? null,
			textSize: e.style.textSize ?? null,
			padding: e.style.padding ?? null,
			children: e.children
		})), t$17(({ id: e, ...t }) => [e, t])),
		edges: t$32(e.edges, t$18((e) => ({
			id: e.id,
			source: e.source,
			target: e.target,
			label: e.label,
			description: flattenMarkdownOrString(e.description),
			technology: e.technology ?? null,
			dir: e.dir,
			head: e.head,
			tail: e.tail,
			line: e.line
		})), t$17((e) => [e.id, { ...e }]))
	}), e;
}
var AbstractMemory = class {
	state;
	get Ctx() {
		throw Error(`Should not be called in runtime`);
	}
	constructor(e) {
		this.state = e;
	}
	get elements() {
		return this.state.elements;
	}
	get explicits() {
		return this.state.explicits;
	}
	get final() {
		return this.state.final;
	}
	get connections() {
		return this.state.connections;
	}
	isEmpty() {
		return this.elements.size === 0 && this.connections.length === 0 && this.explicits.size === 0 && this.final.size === 0;
	}
};
var AbstractStageExclude = class {
	memory;
	expression;
	excluded = {
		elements: /* @__PURE__ */ new Set(),
		connections: []
	};
	constructor(e, t) {
		this.memory = e, this.expression = t;
	}
	markedToMoveExplicitToImplicit = !1;
	_removeElement(e) {
		this.excluded.elements.add(e);
	}
	exclude(e) {
		if (!e) return this;
		if (isIterable(e)) {
			for (let t of e) this._removeElement(t);
			return this;
		}
		return this._removeElement(e), this;
	}
	_removeConnection(e) {
		this.excluded.connections.push(e);
	}
	excludeConnections(e, t) {
		if (e$14(t) && (invariant(!this.markedToMoveExplicitToImplicit, `Already marked to move explicits`), this.markedToMoveExplicitToImplicit = t), isIterable(e)) {
			for (let t of e) this._removeConnection(t);
			return this;
		}
		return this._removeConnection(e), this;
	}
	isDirty() {
		return this.excluded.elements.size > 0 || this.excluded.connections.length > 0;
	}
	isEmpty() {
		return !this.isDirty();
	}
	filterForMoveToImplicits(e) {
		return this.markedToMoveExplicitToImplicit || this.excluded.elements.size > 0 ? e : /* @__PURE__ */ new Set();
	}
	moveDisconnectedExplicitsToImplicits(e) {
		let t = difference(new Set(this.memory.connections.flatMap((e) => [e.source, e.target])), new Set(e.connections.flatMap((e) => [e.source, e.target])));
		return t = intersection(t, e.elements), t = t$32(t, ifilter((t) => e.explicits.has(t) && !isome(e.final, isDescendantOf(t))), toSet()), t.size > 0 && (t = this.filterForMoveToImplicits(t), e.explicits = difference(e.explicits, t), e.final = difference(e.final, t)), e;
	}
	removeElements(e) {
		return e.elements = difference(e.elements, this.excluded.elements), e.explicits = difference(e.explicits, this.excluded.elements), e.final = difference(e.final, this.excluded.elements), e;
	}
	removeConnections(e) {
		let t = this.excluded.connections.reduce((e, t) => {
			let n = e.get(t.id);
			return n ? e.set(t.id, n.mergeWith(t)) : e.set(t.id, t), e;
		}, /* @__PURE__ */ new Map()), n = /* @__PURE__ */ new Set();
		e.connections = e.connections.reduce((e, r) => {
			let i = t.get(r.id);
			if (i) {
				n.add(r.source), n.add(r.target);
				let t = r.difference(i);
				t.nonEmpty() && e.push(t);
			} else e.push(r);
			return e;
		}, []);
		for (let t of e.connections) n.delete(t.source), n.delete(t.target), t.boundary && e.elements.has(t.boundary) && n.delete(t.boundary);
		return n.size === 0 ? e : (n = difference(n, e.explicits), e.final = difference(e.final, n), e);
	}
	precommit(e) {
		return e;
	}
	postcommit(e) {
		return invariant(difference(e.explicits, e.elements).size === 0, `Explicits must be subset of elements`), invariant(difference(e.final, e.elements).size === 0, `Final elements must be subset of elements`), e;
	}
	commit() {
		let e = this.precommit(this.memory.mutableState());
		if (this.excluded.elements.size > 0) {
			let t = e.connections.filter((e) => this.excluded.elements.has(e.source) || this.excluded.elements.has(e.target)).flatMap((t) => [t, ...findAscendingConnections(e.connections, t).map((e) => e.intersect(t))]).filter((e) => e.nonEmpty());
			this.excludeConnections(t);
		}
		return this.excluded.connections.length > 0 && (e = this.removeConnections(e)), this.excluded.elements.size > 0 && (e = this.removeElements(e)), this.memory.update(this.postcommit(e));
	}
};
var AbstractStageInclude = class {
	memory;
	expression;
	explicits = /* @__PURE__ */ new Set();
	implicits = /* @__PURE__ */ new Set();
	_ordered = /* @__PURE__ */ new Set();
	_connections = [];
	constructor(e, t) {
		this.memory = e, this.expression = t;
	}
	get elements() {
		return this.explicits;
	}
	get connections() {
		return this._connections;
	}
	mergedConnections() {
		return mergeConnections([...this.memory.connections, ...this._connections]);
	}
	connectWithExisting(e, t) {
		throw Error(`Method not implements, depends on the model`);
	}
	_addExplicit(e) {
		this._ordered.add(e), this.explicits.add(e), this.implicits.delete(e);
	}
	addExplicit(e) {
		if (!e) return this;
		if (isIterable(e)) {
			for (let t of e) this._addExplicit(t);
			return this;
		}
		return this._addExplicit(e), this;
	}
	_addImplicit(e) {
		this.explicits.has(e) || (this._ordered.add(e), this.implicits.add(e));
	}
	addImplicit(e) {
		if (!e) return this;
		if (isIterable(e)) {
			for (let t of e) this._addImplicit(t);
			return this;
		}
		return this._addImplicit(e), this;
	}
	_addConnection(e) {
		this._connections.push(e), this._addImplicit(e.source), this._addImplicit(e.target);
	}
	addConnections(e) {
		if (isIterable(e)) {
			for (let t of e) this._addConnection(t);
			return this;
		}
		return this._addConnection(e), this;
	}
	isDirty() {
		return this.explicits.size > 0 || this.implicits.size > 0 || this._connections.length > 0;
	}
	isEmpty() {
		return !this.isDirty();
	}
	precommit(e) {
		return e;
	}
	postcommit(e) {
		return e;
	}
	processConnections(e) {
		return e;
	}
	commit() {
		let e = this.precommit(this.memory.mutableState()), t = /* @__PURE__ */ new Set();
		if (this._connections.length > 0) {
			let [n, r] = t$13(this._connections, (t) => e.final.has(t.source));
			e.connections = this.processConnections(mergeConnections([
				...e.connections,
				...n,
				...r
			])), t = new Set(e.connections.flatMap((e) => [e.source, e.target]));
		}
		return e.elements = union(e.elements, this._ordered, this.explicits, t, this.implicits), e.explicits = intersection(e.elements, union(e.explicits, this.explicits)), e.final = intersection(e.elements, union(e.final, this.explicits, t)), this.memory.update(this.postcommit(e));
	}
};
function treeFromMemoryState(e, t = `final`) {
	let n = sortParentsFirst(toArray(t === `final` ? e.final : e.elements)), r = new Set(e.connections.flatMap((e) => [e.source, e.target])), a = new Set(n), o = new g.default(() => null), s = n.reduce((e, t, n, r) => (e.set(t, r.slice(n + 1).filter((e) => isAncestor(t, e)).map((e) => (a.delete(e), e)).reduce((e, n) => (e.some((e) => isAncestor(e, n)) || (e.push(n), o.set(n, t)), e), [])), e), new g.default(() => []));
	return {
		root: a,
		connected: r,
		hasInOut: (t) => e.connections.some(X$1.isAnyInOut(t.id)),
		parent: (e) => o.get(e),
		children: (e) => s.get(e)
	};
}
var StageExclude$1 = class extends AbstractStageExclude {
	static {
		__name(this, `StageExclude`);
	}
};
function applyViewRuleStyle(e, t, n) {
	let { shape: r, color: i, icon: a, ...o } = e.style, s = !e$12(o);
	t$32(n, n$27(e$8(isGroupElementKind)), n$27(t), t$24((t) => {
		t.shape = r ?? t.shape, t.color = i ?? t.color, e$13(a) && (t.icon = a), e$13(e.notation) && (t.notation = e.notation), s && (t.style = {
			...t.style,
			...o
		});
	}));
}
function applyViewRuleStyles(e, t) {
	for (let n of e) !isViewRuleStyle(n) || n.targets.length === 0 || applyViewRuleStyle(n, t$35(n.targets.map(elementExprToPredicate)), t);
	return t;
}
var At = class NodesGroup {
	id;
	viewRule;
	parent;
	elements;
	static kind = j;
	constructor(e, t, n = null, r = /* @__PURE__ */ new Set()) {
		this.id = e, this.viewRule = t, this.parent = n, this.elements = r;
	}
	isEmpty() {
		return this.elements.size === 0;
	}
	update(e) {
		return new NodesGroup(this.id, this.viewRule, this.parent, e);
	}
	clone() {
		return new NodesGroup(this.id, this.viewRule, this.parent, new Set(this.elements));
	}
};
var StageExclude = class extends AbstractStageExclude {
	excludeRelations(e) {
		return t$32(this.memory.connections, n$27((t) => hasIntersection(t.relations, e)), t$24((t) => {
			this.excludeConnections(t.update(intersection(t.relations, e)));
		})), this;
	}
	precommit(e) {
		if (this.excluded.elements.size > 0) {
			let e = new Set([...this.excluded.elements].flatMap((e) => [...e.incoming(`direct`), ...e.outgoing(`direct`)]));
			this.excludeRelations(e);
		}
		return e;
	}
	postcommit(e) {
		let t = difference(this.memory.explicits, e.explicits);
		for (let n of t) e.explicitFirstSeenIn.delete(n);
		let n = difference(this.memory.elements, e.elements);
		for (let t of n) e.lastSeenIn.delete(t);
		return e;
	}
};
var ActiveGroupStageExclude = class extends StageExclude {
	memory;
	expression;
	constructor(e, t) {
		super(e, t), this.memory = e, this.expression = t;
	}
};
var { findConnection: jt, findConnectionsBetween: Mt } = ve;
var StageInclude$1 = class extends AbstractStageInclude {
	static {
		__name(this, `StageInclude`);
	}
	connectWithExisting(e, t = `both`) {
		let n = this._connections.length, hasChanged = () => this._connections.length > n;
		if (!isIterable(e)) {
			if (t === `in` || t === `both`) for (let t of this.memory.elements) this.addConnections(jt(t, e, `directed`));
			return (t === `out` || t === `both`) && this.addConnections(Mt(e, this.memory.elements, `directed`)), hasChanged();
		}
		let r = [...e];
		if (t === `in` || t === `both`) for (let e of this.memory.elements) this.addConnections(Mt(e, r, `directed`));
		if (t === `out` || t === `both`) for (let e of r) this.addConnections(Mt(e, this.memory.elements, `directed`));
		return hasChanged();
	}
	addImplicitWithinScope(e) {
		e && (!this.memory.scope || isAncestor(this.memory.scope, e)) && this.addImplicit(e);
	}
	processConnections(e) {
		return z.isRelationExpr(this.expression) || t$32(e, t$24(({ source: e, target: t, boundary: n }) => {
			t$32(t$2([...toArray(e.ancestors()).reverse(), e], [...toArray(t.ancestors()).reverse(), t]), t$28(([e, t]) => e === t), n$3(1), t$24(([r, i]) => {
				if (r === e && i === t) {
					this.addImplicitWithinScope(n);
					return;
				}
				r !== e && this.addImplicitWithinScope(r), i !== t && this.addImplicitWithinScope(i);
			}));
		})), e;
	}
	postcommit(e) {
		let t = difference(e.explicits, this.memory.explicits);
		for (let n of t) e.explicitFirstSeenIn.set(n, `@root`);
		return e;
	}
};
var ActiveGroupStageInclude = class extends StageInclude$1 {
	memory;
	expression;
	constructor(e, t) {
		super(e, t), this.memory = e, this.expression = t;
	}
	postcommit(e) {
		let t = difference(e.explicits, this.memory.explicits);
		for (let n of t) e.explicitFirstSeenIn.set(n, this.memory.activeGroupId);
		for (let t of [...this.explicits, ...this.implicits]) e.lastSeenIn.set(t, this.memory.activeGroupId);
		return e;
	}
};
var Nt = class Memory$1 extends AbstractMemory {
	static {
		__name(this, `Memory`);
	}
	state;
	scope;
	static empty(e) {
		return new Memory$1({
			elements: /* @__PURE__ */ new Set(),
			explicits: /* @__PURE__ */ new Set(),
			final: /* @__PURE__ */ new Set(),
			connections: [],
			groups: [],
			explicitFirstSeenIn: /* @__PURE__ */ new Map(),
			lastSeenIn: /* @__PURE__ */ new Map()
		}, e);
	}
	constructor(e, t) {
		super(e), this.state = e, this.scope = t;
	}
	get groups() {
		return this.state.groups;
	}
	get explicitFirstSeenIn() {
		return this.state.explicitFirstSeenIn;
	}
	get lastSeenIn() {
		return this.state.lastSeenIn;
	}
	stageInclude(e) {
		return new StageInclude$1(this, e);
	}
	stageExclude(e) {
		return new StageExclude(this, e);
	}
	mutableState() {
		return {
			elements: new Set(this.state.elements),
			explicits: new Set(this.state.explicits),
			final: new Set(this.state.final),
			connections: [...this.state.connections],
			groups: this.state.groups.map((e) => e.clone()),
			explicitFirstSeenIn: new Map(this.state.explicitFirstSeenIn),
			lastSeenIn: new Map(this.state.lastSeenIn)
		};
	}
	update(e) {
		return new Memory$1({
			...this.state,
			...e
		}, this.scope);
	}
};
var Pt = class ActiveGroupMemory extends Nt {
	state;
	scope;
	stack;
	static enter(e, t) {
		let n = `@gr${e.groups.length + 1}`;
		if (e instanceof ActiveGroupMemory) {
			let r = S.default.from([...e.stack].reverse()), i = e.mutableState();
			return i.groups.push(new At(n, t, e.activeGroupId)), r.push(n), new ActiveGroupMemory(i, e.scope, r);
		}
		let r = e.mutableState();
		r.groups.push(new At(n, t, null));
		let i = S.default.of(n);
		return new ActiveGroupMemory(r, e.scope, i);
	}
	constructor(e, t, n) {
		super(e, t), this.state = e, this.scope = t, this.stack = n;
	}
	get activeGroupId() {
		return nonNullable(this.stack.peek(), `Stack must not be empty`);
	}
	mutableState() {
		return { ...super.mutableState() };
	}
	update(e) {
		let t = {
			...this.state,
			...e
		};
		return new ActiveGroupMemory(t, this.scope, this.stack);
	}
	stageInclude(e) {
		return new ActiveGroupStageInclude(this, e);
	}
	stageExclude(e) {
		return new ActiveGroupStageExclude(this, e);
	}
	leave() {
		let e = this.mutableState();
		return this.stack.pop(), this.stack.peek() ? new ActiveGroupMemory(e, this.scope, this.stack) : new Nt(e, this.scope);
	}
};
function updateDepthOfAncestors(e, t) {
	let n;
	for (; e.parent && (n = t.get(e.parent));) {
		let t = n.depth ?? 1;
		if (n.depth = Math.max(t, (e.depth ?? 0) + 1), n.depth === t) break;
		e = n;
	}
}
function elementModelToNodeSource(e) {
	let { id: t, title: n, ...r } = n$14(e.$element, [
		`summary`,
		`description`,
		`metadata`,
		`style`,
		`tags`
	]), { color: i, icon: a, shape: o, ...s } = e.style;
	return exact({
		id: t,
		title: n,
		modelRef: t,
		shape: o,
		color: i,
		icon: a,
		style: s,
		description: preferSummary(e.$element),
		tags: [...e.tags],
		metadata: e.$element.metadata,
		...r
	});
}
function buildComputedNodes({ defaults: e }, t, n) {
	let r = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
	n?.forEach(({ id: t, parent: n, viewRule: a, elements: o }) => {
		n && nonNullable(r.get(n), `Parent group node ${n} not found`).children.push(t), r.set(t, {
			id: t,
			parent: n,
			kind: j,
			title: a.title ?? ``,
			color: a.color ?? e.group.color ?? e.color,
			shape: `rectangle`,
			children: [],
			inEdges: [],
			outEdges: [],
			level: 0,
			depth: 0,
			tags: [],
			style: exact({
				border: a.border ?? e.group.border,
				opacity: a.opacity ?? e.group.opacity,
				size: a.size,
				multiple: a.multiple,
				padding: a.padding,
				textSize: a.textSize
			})
		});
		for (let e of o) i.set(e.id, t);
	}), Array.from(t).sort(compareByFqnHierarchically).forEach(({ id: e, ...t }) => {
		let n = parentFqn(e), a = 0, o;
		for (; n && (o = r.get(n), !o);) n = parentFqn(n);
		let s = t.modelRef ?? e;
		if (!o && i.has(s)) {
			let e = nonNullable(i.get(s));
			o = r.get(e), n = e;
		}
		o && (o.children.length == 0 && (o.depth = 1, updateDepthOfAncestors(o, r)), o.children.push(e), a = o.level + 1);
		let c = exact({
			id: e,
			parent: n,
			level: a,
			children: [],
			inEdges: [],
			outEdges: [],
			...t
		});
		r.set(e, c);
	});
	let a = /* @__PURE__ */ new Map();
	return n?.forEach(({ id: e }) => {
		a.set(e, nonNullable(r.get(e)));
	}), t.forEach(({ id: e }) => {
		a.set(e, nonNullable(r.get(e)));
	}), a;
}
function pickRelationshipProps(e) {
	let { title: t, description: n = null } = e;
	return {
		...t && {
			title: t,
			description: n
		},
		...t$12(e, [
			`color`,
			`technology`,
			`head`,
			`line`,
			`tail`,
			`kind`,
			`navigateTo`
		])
	};
}
function mergePropsFromRelationships(e, t) {
	let n = t$32(e, t$7((e, t) => (e$4(t.title) && !e.title.includes(t.title) && e.title.push(t.title), e$4(t.description) && !e.description.some(t$22(t.description)) && e.description.push(t.description), e$4(t.technology) && !e.technology.includes(t.technology) && e.technology.push(t.technology), e$4(t.kind) && !e.kind.includes(t.kind) && e.kind.push(t.kind), e$4(t.color) && !e.color.includes(t.color) && e.color.push(t.color), e$4(t.line) && !e.line.includes(t.line) && e.line.push(t.line), e$4(t.head) && !e.head.includes(t.head) && e.head.push(t.head), e$4(t.tail) && !e.tail.includes(t.tail) && e.tail.push(t.tail), e$4(t.navigateTo) && !e.navigateTo.includes(t.navigateTo) && e.navigateTo.push(t.navigateTo), t.tags && e.tags.push(...t.tags), e), {
		title: [],
		description: [],
		technology: [],
		kind: [],
		head: [],
		tail: [],
		color: [],
		tags: [],
		line: [],
		navigateTo: []
	})), r = t$14(n.title) ?? (n.title.length > 1 ? `[...]` : void 0), i = n$1(n.tags), a = exact({
		title: r,
		description: t$14(n.description),
		technology: t$14(n.technology),
		kind: t$14(n.kind),
		head: t$14(n.head),
		tail: t$14(n.tail),
		color: t$14(n.color),
		line: t$14(n.line),
		navigateTo: t$14(n.navigateTo),
		...isNonEmptyArray(i) && { tags: i }
	});
	if (t) {
		let e = pickRelationshipProps(t);
		a = t$11({
			...a,
			...e
		}, e$4);
	}
	return e$7(a.title) && e$4(a.technology) && (a.title = `[${a.technology}]`, delete a.technology), a;
}
var { findConnection: Ft, findConnectionsBetween: Y, findConnectionsWithin: X } = _e;
function resolveElements$1(e, t) {
	let n = e.element(t.ref.deployment);
	if (n.isDeploymentNode()) {
		if (t.selector === `children`) return [...n.children()];
		if (t.selector === `expanded`) return [n, ...n.children()];
		if (t.selector === `descendants`) return [...n.descendants()];
	}
	return [n];
}
__name(resolveElements$1, `resolveElements`);
function resolveModelElements(e, t) {
	let n = e.$model.element(t.ref.model);
	return t.selector === `children` ? [...n.children()] : t.selector === `expanded` ? [n, ...n.children()] : t.selector === `descendants` ? [...n.descendants()] : [n];
}
function deploymentExpressionToPredicate(e) {
	if (P.isWildcard(e)) return () => !0;
	if (P.isElementTagExpr(e) || P.isElementKindExpr(e)) throw Error(`element kind and tag expressions are not supported in deployment view rules`);
	if (P.isDeploymentRef(e)) {
		let t = e.ref.deployment;
		if (e.selector === `expanded`) {
			let e = t + `.`;
			return (n) => n.id === t || n.id.startsWith(e);
		}
		if (e.selector === `descendants`) {
			let e = t + `.`;
			return (t) => t.id.startsWith(e);
		}
		return e.selector === `children` ? (e) => parentFqn(e.id) === t : (e) => e.id === t;
	}
	if (P.isModelRef(e)) {
		let t = e.ref.model;
		if (e.selector === `expanded`) {
			let e = t + `.`;
			return (n) => {
				let r = n.modelRef ?? null;
				return !r || r === t || r.startsWith(e);
			};
		}
		if (e.selector === `descendants`) {
			let e = t + `.`;
			return (t) => {
				let n = t.modelRef ?? null;
				return !n || n.startsWith(e);
			};
		}
		return e.selector === `children` ? (e) => {
			let n = e.modelRef ?? null;
			return !n || parentFqn(n) === t;
		} : (e) => {
			let n = e.modelRef ?? null;
			return !n || n === t;
		};
	}
	nonexhaustive(e);
}
function instanceSummary(e) {
	return preferSummary(e.$instance) ?? preferSummary(e.element.$element);
}
function deploymentNodeToNodeSource(e) {
	let t = e.id, n = e.onlyOneInstance();
	return exact({
		id: t,
		deploymentRef: t,
		title: e.title,
		kind: e.kind,
		technology: e.technology ?? void 0,
		links: t$30(e.links, 1) ? [...e.links] : void 0,
		notation: e.$node.notation ?? void 0,
		color: e.color,
		shape: e.shape,
		modelRef: n?.element.id,
		icon: e.style.icon,
		description: preferSummary(e.$node) ?? void 0,
		tags: [...e.tags],
		style: n$14(e.style, [
			`icon`,
			`shape`,
			`color`
		])
	});
}
function instanceToNodeSource(e) {
	let t = e.$instance, n = e.element, { icon: r, color: i, shape: a, ...o } = e.style, s = [...n.links, ...t.links ?? []];
	return exact({
		id: e.id,
		kind: `instance`,
		title: e.title,
		description: instanceSummary(e) ?? void 0,
		technology: e.technology ?? void 0,
		tags: [...e.tags],
		links: t$30(s, 1) ? s : void 0,
		icon: r,
		color: i,
		shape: a,
		style: o,
		deploymentRef: t.id,
		modelRef: n.id,
		notation: t.notation
	});
}
function toNodeSource(e) {
	return e.isInstance() ? instanceToNodeSource(e) : deploymentNodeToNodeSource(e);
}
var It = __name((e, t, n) => stringHash(`model:${e}:${t}:${n}`), `hashEdgeId`);
function mergeBidirectionalEdges(e) {
	let t = /* @__PURE__ */ new Set();
	for (let n = 0; n < e.length; n++) {
		if (t.has(n)) continue;
		let r = e[n];
		for (let i = n + 1; i < e.length; i++) {
			if (t.has(i)) continue;
			let n = e[i];
			if (r.source === n.target && r.target === n.source && r.label === n.label) {
				r.dir = `both`;
				let e = r.head ?? n.head;
				e && (r.head ||= e, r.tail ||= e), n.color && (r.color ||= n.color), n.line && (r.line ||= n.line), t.add(i);
				break;
			}
		}
	}
	return t.size > 0 ? e.filter((e, n) => !t.has(n)) : e;
}
function toComputedEdges$1(e, t) {
	let r = [], i = [];
	for (let a of e) {
		let e = [...a.relations.model, ...a.relations.deployment];
		invariant(t$30(e, 1), `Edge must have at least one relation`);
		let o = a.source.$model.$styles.defaults, s = a.source.id, c = a.target.id;
		if (t && e.length > 1) {
			let [n, l] = t$32(e, t$13((e) => t(e, a)));
			if (n.length > 0) {
				for (let e of n) {
					let { title: t, color: n = o.relationship.color, line: i = o.relationship.line, head: l = o.relationship.arrow, ...u } = mergePropsFromRelationships([e.$relationship], e.$relationship);
					r.push({
						id: It(s, c, e.id),
						parent: a.boundary?.id ?? null,
						source: s,
						target: c,
						label: t ?? null,
						relations: [e.id],
						color: n,
						line: i,
						head: l,
						...u
					});
				}
				if (l.length > 0) {
					let e = t$14(n$27(l, (e) => e.source.id === s && e.target.id === c)), { title: t, color: n = o.relationship.color, line: r = o.relationship.line, head: u = o.relationship.arrow, ...d } = mergePropsFromRelationships(l.map((e) => e.$relationship), e?.$relationship);
					i.push({
						id: a.id,
						parent: a.boundary?.id ?? null,
						source: s,
						target: c,
						label: t ?? null,
						relations: l.map((e) => e.id),
						color: n,
						line: r,
						head: u,
						...d
					});
				}
				continue;
			}
		}
		let { title: l, color: u = o.relationship.color, line: d = o.relationship.line, head: p = o.relationship.arrow, ...m } = mergePropsFromRelationships(e.map((e) => e.$relationship)), h = exact({
			id: a.id,
			parent: a.boundary?.id ?? null,
			source: s,
			target: c,
			label: l ?? null,
			relations: e.map((e) => e.id),
			color: u,
			line: d,
			head: p,
			...m
		});
		i.push(h);
	}
	return [...r, ...mergeBidirectionalEdges(i)];
}
__name(toComputedEdges$1, `toComputedEdges`);
function buildNodes$1(e, t) {
	let n = buildComputedNodes(e.$styles, [...t.final].map(toNodeSource));
	for (let t of n.values()) {
		if (!t.deploymentRef || !t.modelRef || t.children.length > 0) continue;
		let n = e.deployment.element(t.deploymentRef), r = n.isDeploymentNode() && n.onlyOneInstance();
		if (!r) continue;
		t.title === n.name && (t.title = r.title), t.description ??= instanceSummary(r) ?? null, t.technology ??= r.technology, e$12(t.tags) && (t.tags = [...r.tags]), (!t.links || e$12(t.links)) && t$30(r.links, 1) && (t.links = [...r.links]);
		let i = e.$styles.defaults;
		t.shape === i.shape && t.shape !== r.shape && (t.shape = r.shape, t.notation = null), t.color === i.color && t.color !== r.color && (t.color = r.color, t.notation = null), !t.icon && r.icon && (t.icon = r.icon);
	}
	return n;
}
__name(buildNodes$1, `buildNodes`);
function applyDeploymentViewRuleStyles(e, t) {
	for (let n of e) !isViewRuleStyle(n) || n.targets.length === 0 || applyViewRuleStyle(n, t$35(n.targets.map(deploymentExpressionToPredicate)), t);
	return t;
}
function findCrossBoundarySameSourceOrTarget(e) {
	let t = new g.default(() => []);
	for (let n of e) for (let e of n.relations.model) t.get(e).push(n);
	let n = new g.default(() => /* @__PURE__ */ new Set());
	for (let [e, r] of t) t$30(r, 2) && t$32(r, t$25((e) => [{
		group: `$source-${e.source.id}`,
		conn: e
	}, {
		group: `$target-${e.target.id}`,
		conn: e
	}]), t$23(e$3(`group`)), t$2$1(), n$27(t$30(2)), t$24(t$10(t$18(e$3(`conn`)), sortConnectionsByBoundaryHierarchy(`desc`), t$28((e, t, n) => t === 0 || e.boundary === n[t - 1].boundary), t$24((t) => {
		n.get(t).add(e);
	}))));
	return n;
}
function findCrossBoundaryConnections(e) {
	e = mergeConnections(e);
	let t = findCrossBoundarySameSourceOrTarget(e);
	for (let r of e) {
		let { source: e, target: i } = r, a = r.relations.model;
		if (e.isDeploymentNode() !== i.isDeploymentNode()) {
			let i = e.isDeploymentNode() ? e : r.target;
			invariant(i.isDeploymentNode());
			let o = intersection(a, i.internalModelRelationships());
			for (let e of o) t.get(r).add(e);
			continue;
		}
		if (e.isDeploymentNode() && i.isDeploymentNode()) {
			let n = union(intersection(r.relations.model, e.internalModelRelationships()), intersection(r.relations.model, i.internalModelRelationships()));
			for (let e of n) t.get(r).add(e);
		}
	}
	return t$32(t.entries(), imap(([e, t]) => e.update({
		model: t,
		deployment: null
	})), toArray());
}
function cleanCrossBoundary(e) {
	return differenceConnections(e, findCrossBoundaryConnections(e));
}
function findRedundantConnections$1(e) {
	return t$32(e, mergeConnections, t$7((t, n) => {
		let { source: r, target: i, relations: a } = n;
		if (r.isInstance() && i.isInstance()) return t;
		let o = q.empty();
		if (r.isDeploymentNode() && i.isDeploymentNode()) {
			let [e] = Ft(i, r, `directed`);
			e && (o = a.intersect(e.relations));
		}
		for (let t of e) isNestedConnection(t, n) && (o = o.union(a.intersect(t.relations)));
		return o.nonEmpty && t.push(new Q$1(r, i, o)), t;
	}, []));
}
__name(findRedundantConnections$1, `findRedundantConnections`);
function cleanRedundantRelationships(e) {
	return differenceConnections(e, findRedundantConnections$1(e));
}
var StageInclude = class extends AbstractStageInclude {
	connectWithExisting(e, t = `both`) {
		let n = this._connections.length, hasChanged = () => this._connections.length > n;
		if (!isIterable(e)) {
			if (t === `in` || t === `both`) for (let t of this.memory.elements) this.addConnections(Ft(t, e, `directed`));
			return (t === `out` || t === `both`) && this.addConnections(Y(e, this.memory.elements, `directed`)), hasChanged();
		}
		let r = [...e];
		if (t === `in` || t === `both`) for (let e of this.memory.elements) this.addConnections(Y(e, r, `directed`));
		if (t === `out` || t === `both`) for (let e of r) this.addConnections(Y(e, this.memory.elements, `directed`));
		return hasChanged();
	}
	processConnections(e) {
		let t = t$32(e, cleanCrossBoundary, cleanRedundantRelationships);
		return t$32(t, t$24(({ source: e, target: t, boundary: n }) => {
			t$32(t$2([...toArray(e.ancestors()).reverse(), e], [...toArray(t.ancestors()).reverse(), t]), t$28(([e, t]) => e === t), n$3(1), t$24(([r, i]) => {
				if (e === r && t === i) {
					this.addImplicit(n);
					return;
				}
				r !== e && r.isDeploymentNode() && !r.onlyOneInstance() && this.addImplicit(r), i !== t && i.isDeploymentNode() && !i.onlyOneInstance() && this.addImplicit(i);
			}));
		})), t;
	}
};
var Lt = class Memory extends AbstractMemory {
	static empty() {
		return new Memory({
			elements: /* @__PURE__ */ new Set(),
			explicits: /* @__PURE__ */ new Set(),
			final: /* @__PURE__ */ new Set(),
			connections: []
		});
	}
	stageInclude(e) {
		return new StageInclude(this, e);
	}
	stageExclude(e) {
		return new StageExclude$1(this, e);
	}
	mutableState() {
		return {
			elements: new Set(this.state.elements),
			explicits: new Set(this.state.explicits),
			final: new Set(this.state.final),
			connections: [...this.state.connections]
		};
	}
	update(e) {
		return new Memory({
			...this.state,
			...e
		});
	}
	equals(e) {
		return e instanceof Memory && t$22(this.state, e.state);
	}
	diff(e) {
		return {
			added: {
				elements: toArray(difference(e.elements, this.elements)),
				explicits: toArray(difference(e.explicits, this.explicits)),
				final: toArray(difference(e.final, this.final)),
				connections: toArray(differenceConnections(e.connections, this.connections))
			},
			removed: {
				elements: toArray(difference(this.elements, e.elements)),
				explicits: toArray(difference(this.explicits, e.explicits)),
				final: toArray(difference(this.final, e.final)),
				connections: differenceConnections(this.connections, e.connections)
			}
		};
	}
	toString() {
		return [
			`final:`,
			...[...this.final].map((e) => `  ` + e.id),
			`connections:`,
			...this.connections.map((e) => `  ` + e.expression)
		].join(`
`);
	}
	[Z$1](e, t, n) {
		return this.toString();
	}
};
var Rt = {
	include: ({ expr: e, model: t, memory: n, stage: r }) => {
		let i = whereOperatorAsPredicate(e.where.condition);
		return nonNullable(predicateToPatch(`include`, {
			expr: e.where.expr,
			model: t,
			stage: r,
			memory: n,
			where: i
		}));
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r }) => {
		let i = whereOperatorAsPredicate(e.where.condition);
		return nonNullable(predicateToPatch(`exclude`, {
			expr: e.where.expr,
			model: t,
			stage: r,
			memory: n,
			where: i
		}));
	}
};
var zt = {
	include: ({ expr: e, model: t, memory: r, stage: i, where: a }) => {
		let o = [...r.elements];
		if (P.isWildcard(e.incoming)) {
			for (let e of o) {
				if (e.allOutgoing.isEmpty) continue;
				let t = applyPredicate(Y(e, [...resolveAscendingSiblings(e)], `directed`), a);
				i.addConnections(t);
			}
			return i;
		}
		invariant(P.isDeploymentRef(e.incoming), `Only deployment refs are supported in include`);
		let s = resolveElements$1(t, e.incoming);
		for (let e of o) {
			let t = applyPredicate(Y(e, s, `directed`), a);
			i.addConnections(t);
		}
		return i;
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r, where: i }) => {
		if (P.isElementTagExpr(e.incoming) || P.isElementKindExpr(e.incoming)) throw Error(`element kind and tag expressions are not supported in exclude`);
		if (P.isModelRef(e.incoming)) return excludeModelRelations(resolveAllIncomingRelations(t, e.incoming), {
			stage: r,
			memory: n
		}, i);
		if (P.isWildcard(e.incoming)) return r;
		let a = filterIncomingConnections(resolveElements$1(t, e.incoming)), o = t$32(n.connections, n$27(a), applyPredicate(i));
		return r.excludeConnections(o), r;
	}
};
function filterIncomingConnections(e) {
	return t$35(e.map((e) => {
		let satisfies = (t) => t === e || isAncestor(e, t);
		return (e) => !satisfies(e.source) && satisfies(e.target);
	}));
}
function resolveAllIncomingRelations(e, t) {
	let n = resolveModelElements(e, t);
	return new Set(n.flatMap((e) => [...e.allIncoming]));
}
var Bt = {
	include: ({ expr: e, model: t, memory: r, stage: i, where: a }) => {
		let o = [...r.elements];
		if (P.isWildcard(e.outgoing)) {
			for (let e of o) if (!e.allIncoming.isEmpty) for (let t of resolveAscendingSiblings(e)) {
				let n = applyPredicate(Ft(t, e, `directed`), a);
				i.addConnections(n);
			}
			return i;
		}
		invariant(P.isDeploymentRef(e.outgoing), `Only deployment refs are supported in include`);
		let s = resolveElements$1(t, e.outgoing);
		for (let e of s) {
			let t = applyPredicate(Y(e, o, `directed`), a);
			i.addConnections(t);
		}
		return i;
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r, where: i }) => {
		if (P.isElementTagExpr(e.outgoing) || P.isElementKindExpr(e.outgoing)) throw Error(`element kind and tag expressions are not supported in exclude`);
		if (P.isModelRef(e.outgoing)) return excludeModelRelations(resolveAllOutgoingRelations(t, e.outgoing), {
			stage: r,
			memory: n
		}, i);
		if (P.isWildcard(e.outgoing)) return r;
		let a = filterOutgoingConnections(resolveElements$1(t, e.outgoing)), o = t$32(n.connections, n$27(a), applyPredicate(i));
		return r.excludeConnections(o), r;
	}
};
function filterOutgoingConnections(e) {
	return t$35(e.map((e) => {
		let satisfies = (t) => t === e || isAncestor(e, t);
		return (e) => satisfies(e.source) && !satisfies(e.target);
	}));
}
function resolveAllOutgoingRelations(e, t) {
	let n = resolveModelElements(e, t);
	return new Set(n.flatMap((e) => [...e.allOutgoing]));
}
var Vt = {
	include: ({ expr: { source: e, target: t, isBidirectional: r = !1 }, model: i, stage: a, where: o }) => {
		if (P.isElementTagExpr(e) || P.isElementKindExpr(e) || P.isElementTagExpr(t) || P.isElementKindExpr(t)) throw Error(`element kind and tag expressions are not supported in include`);
		invariant(!P.isModelRef(e), `Invalid source model ref in direct relation`), invariant(!P.isModelRef(t), `Invalid target model ref in direct relation`);
		let s = P.isWildcard(e), c = P.isWildcard(t), l = r ? `both` : `directed`, u;
		switch (!0) {
			case s && c:
				u = t$32(X(i.instances()), applyPredicate(o), t$18((e) => (a.addImplicit(e.boundary), e)));
				break;
			case !s && c: {
				let t = resolveElements$1(i, e), n = filterOutgoingConnections(t), postFilter = n;
				if (r) {
					let e = filterIncomingConnections(t);
					postFilter = (t) => n(t) !== e(t);
				}
				u = t$32(t, t$25((e) => Y(e, resolveAscendingSiblings(e), l)), n$27(postFilter), applyPredicate(o));
				break;
			}
			case s && !c: {
				let e = resolveElements$1(i, t), n = filterIncomingConnections(e), postFilter = n;
				if (r) {
					let t = filterOutgoingConnections(e);
					postFilter = (e) => t(e) !== n(e);
				}
				u = t$32(e, t$25((e) => [...resolveAscendingSiblings(e)].flatMap((t) => Ft(t, e, l))), n$27(postFilter), applyPredicate(o));
				break;
			}
			default: {
				invariant(!s, `Inference failed - source should be a deployment ref`), invariant(!c, `Inference failed - target should be a deployment ref`);
				let r = resolveElements$1(i, e), a = resolveElements$1(i, t), d = filterOutgoingConnections(r), f = filterIncomingConnections(a);
				u = t$32(r, t$25((e) => Y(e, a, l)), n$27((e) => d(e) && f(e)), applyPredicate(o));
			}
		}
		return a.addConnections(u), P.isDeploymentRef(e) && e$9(e.selector) && a.addImplicit(i.element(e.ref.deployment)), P.isDeploymentRef(t) && e$9(t.selector) && a.addImplicit(i.element(t.ref.deployment)), a;
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r, where: i }) => {
		let a = deploymentExpressionToPredicate(e.target), o = deploymentExpressionToPredicate(e.source), s;
		switch (!0) {
			case P.isWildcard(e.source) && P.isWildcard(e.target): return r.excludeConnections(applyPredicate(n.connections, i)), r;
			case P.isModelRef(e.source) && P.isModelRef(e.target): return s = resolveRelationsBetweenModelElements({
				source: e.source,
				target: e.target,
				expr: e,
				model: t
			}), excludeModelRelations(s, {
				stage: r,
				memory: n
			}, i);
			case P.isModelRef(e.source) && P.isWildcard(e.target): return s = resolveAllOutgoingRelations(t, e.source), excludeModelRelations(s, {
				stage: r,
				memory: n
			}, i);
			case P.isModelRef(e.source): return s = resolveAllOutgoingRelations(t, e.source), excludeModelRelations(s, {
				stage: r,
				memory: n
			}, i, (e) => a(e.target));
			case P.isModelRef(e.target): return s = resolveAllIncomingRelations(t, e.target), excludeModelRelations(s, {
				stage: r,
				memory: n
			}, i, (e) => o(e.source));
			default:
				let satisfies = (t) => o(t.source) && a(t.target) || e.isBidirectional === !0 && o(t.target) && a(t.source), c = t$32(n.connections, n$27(satisfies), applyPredicate(i));
				return c.length === 0 || r.excludeConnections(c), r;
		}
	}
};
function resolveRelationsBetweenModelElements({ source: e, target: t, expr: n, model: r }) {
	let i = resolveModelElements(r, e), a = resolveModelElements(r, t), o = n.isBidirectional ? `both` : `directed`, s = [];
	for (let e of i) s.push(...findConnectionsBetween(e, a, o));
	return new Set(s.flatMap((e) => [...e.relations]));
}
var Ht = {
	include: ({ expr: e, model: t, memory: r, stage: i, where: a }) => {
		let o = [...r.elements];
		if (P.isWildcard(e.inout)) {
			for (let e of o) {
				let t = matchConnections(Y(e, [...resolveAscendingSiblings(e)], `both`), a);
				i.addConnections(t);
			}
			return i;
		}
		invariant(P.isDeploymentRef(e.inout), `Only deployment refs are supported in include`);
		let s = resolveElements$1(t, e.inout);
		for (let e of o) {
			let t = matchConnections(Y(e, s, `both`), a);
			i.addConnections(t);
		}
		return i;
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r, where: i }) => {
		if (P.isElementTagExpr(e.inout) || P.isElementKindExpr(e.inout)) throw Error(`element kind and tag expressions are not supported in exclude`);
		if (P.isModelRef(e.inout)) {
			let a = resolveModelElements(t, e.inout);
			return a.length === 0 ? r : excludeModelRelations(union(/* @__PURE__ */ new Set(), ...a.flatMap((e) => [e.allIncoming, e.allOutgoing])), {
				stage: r,
				memory: n
			}, i);
		}
		if (P.isWildcard(e.inout)) return r;
		let a = resolveElements$1(t, e.inout), o = filterIncomingConnections(a), s = filterOutgoingConnections(a), c = t$32(n.connections, n$27((e) => o(e) !== s(e)), applyPredicate(i));
		return r.excludeConnections(c), r;
	}
};
var Ut = {
	include: ({ expr: e, model: t, memory: n, stage: r }) => {
		let i = whereOperatorAsPredicate(e.where.condition);
		return predicateToPatch(`include`, {
			expr: e.where.expr,
			model: t,
			stage: r,
			memory: n,
			where: i
		});
	},
	exclude: ({ expr: e, model: t, memory: n, stage: r }) => {
		let i = whereOperatorAsPredicate(e.where.condition);
		return predicateToPatch(`exclude`, {
			expr: e.where.expr,
			model: t,
			stage: r,
			memory: n,
			where: i
		});
	}
};
var Wt = {
	include: ({ model: e, stage: t, where: n }) => {
		let r = [], i;
		return i = n ? t$32([...e.elements()], applyElementPredicate(n)) : toArray(imap(e.roots(), (e) => (e.onlyOneInstance() || r.push(...e.children()), e))), t.addExplicit(i), r.length > 1 ? (t.addConnections(X([...i, ...r])), t.connectWithExisting(r)) : t.addConnections(X(i)), t.connectWithExisting(i), t;
	},
	exclude: ({ stage: e, memory: t, where: n }) => {
		let r = t$32([...t.elements], applyElementPredicate(n));
		return e.exclude(r), e;
	}
};
function predicateToPatch(e, { expr: t, where: n, ...r }) {
	switch (!0) {
		case P.isElementTagExpr(t):
		case P.isElementKindExpr(t): throw Error(`element kind and tag expressions are not supported in deployment view rules`);
		case F.isCustom(t):
		case P.isCustom(t):
		case P.isModelRef(t): return;
		case P.isWhere(t): return Rt[e]({
			...r,
			expr: t,
			where: n
		});
		case F.isWhere(t): return Ut[e]({
			...r,
			expr: t,
			where: n
		});
		case P.isDeploymentRef(t): return Gt[e]({
			...r,
			expr: t,
			where: n
		});
		case P.isWildcard(t): return Wt[e]({
			...r,
			expr: t,
			where: n
		});
		case F.isDirect(t): return Vt[e]({
			...r,
			expr: t,
			where: n
		});
		case F.isInOut(t): return Ht[e]({
			...r,
			expr: t,
			where: n
		});
		case F.isOutgoing(t): return Bt[e]({
			...r,
			expr: t,
			where: n
		});
		case F.isIncoming(t): return zt[e]({
			...r,
			expr: t,
			where: n
		});
		default: nonexhaustive(t);
	}
}
function excludeModelRelations(e, { stage: t, memory: n }, r, i = () => !0) {
	if (e.size === 0) return t;
	let a = t$32(n.connections, n$27((e) => i(e)), n$27((t) => hasIntersection(t.relations.model, e)), t$18((t) => t.update({
		deployment: null,
		model: intersection(t.relations.model, e)
	})), applyPredicate(r), n$27((e) => e.nonEmpty()));
	return a.length === 0 ? t : t.excludeConnections(a);
}
function matchConnection(e, t) {
	return applyPredicate(e, t).nonEmpty();
}
function applyPredicate(...e) {
	if (e.length === 1) return (t) => applyPredicate(t, e[0]);
	let [t, n] = e;
	return n === null ? t : e$15(t) ? t.map((e) => applyPredicate(e, n)).filter((e) => e.nonEmpty()) : t.update({
		model: new Set([...t.relations.model.values()].filter((e) => n(toFilterableRelation(t)(e)))),
		deployment: new Set([...t.relations.deployment.values()].filter((e) => n(toFilterableRelation(t)(e))))
	});
}
function applyElementPredicate(...e) {
	if (e.length === 1) {
		let t = e[0];
		return t == null ? (e) => e : (e) => e.filter((e) => t(toFilterable(e, e)));
	}
	let [t, n] = e;
	return e$15(t) ? n == null ? t : t.filter((e) => n(toFilterable(e, e))) : n?.(toFilterable(t, t)) ?? !0;
}
function matchConnections(e, t) {
	return t ? t$32(e, n$27((e) => matchConnection(e, t))) : e;
}
function toFilterable(e, t) {
	if (isElementModel(e)) {
		let n = isDeploymentElementModel(t) && isDeployedInstanceModel(t) ? t : null;
		return {
			kind: e.kind,
			tags: [...e.tags, ...n?.tags ?? []],
			metadata: n?.metadata ?? e.metadata
		};
	}
	if (isNestedElementOfDeployedInstanceModel(e)) return t$12(e.element, [
		`tags`,
		`kind`,
		`metadata`
	]);
	if (isDeployedInstanceModel(e)) return {
		kind: e.element.kind,
		tags: [...e.tags, ...e.element.tags],
		metadata: e.metadata
	};
	if (isDeploymentNodeModel(e)) return t$12(e, [
		`tags`,
		`kind`,
		`metadata`
	]);
	nonexhaustive(e);
}
function toFilterableRelation(e) {
	return (t) => ({
		tags: t.tags,
		kind: t.kind,
		metadata: t.metadata,
		source: toFilterable(t.source, e.source),
		target: toFilterable(t.target, e.target)
	});
}
function resolveAscendingSiblings(e) {
	let t = /* @__PURE__ */ new Set();
	for (let n of e.descendingSiblings()) t.add(n);
	return t;
}
var Gt = {
	include: (e) => {
		let { expr: t, where: n } = e, r = e.model.element(t.ref.deployment);
		if (isDeployedInstanceModel(r)) return applyElementPredicate(r, n) && (e.stage.addExplicit(r), e.stage.connectWithExisting(r)), e.stage;
		switch (!0) {
			case t.selector === `expanded`:
				includeDeployedNodeWithExpanded(r, e);
				break;
			case t.selector === `children`:
				includeDeployedNodeChildren(r, e);
				break;
			case t.selector === `descendants`:
				includeDeployedNodeDescendants(r, e);
				break;
			default: applyElementPredicate(r, n) && (e.stage.addExplicit(r), e.stage.connectWithExisting(r));
		}
		return e.stage;
	},
	exclude: ({ expr: e, stage: t, memory: n, where: r }) => {
		let i = deploymentExpressionToPredicate(e), a = t$32([...n.elements], n$27(i), applyElementPredicate(r));
		return t.exclude(a), t;
	}
};
function includeDeployedNodeChildren(e, { stage: t, where: n }) {
	let r = applyElementPredicate([...e.children()], n);
	r.length !== 0 && (t.addImplicit(e), t.addConnections(X(r)), t.connectWithExisting(r), t.addExplicit(r));
}
function includeDeployedNodeWithExpanded(e, { memory: t, stage: n, where: r }) {
	n.addImplicit(e), n.connectWithExisting(e);
	let i = applyElementPredicate([...e.children()], r), a = !1;
	for (let e of i) if (Y(e, t.elements).length > 0) {
		a = !0;
		break;
	}
	a && (n.connectWithExisting(i, `in`), n.addConnections(X(i)), n.connectWithExisting(i, `out`)), n.addImplicit(i), n.connections.length > 0 && n.addExplicit(e);
}
function includeDeployedNodeDescendants(e, { stage: t, where: n }) {
	let dfs = (e) => {
		let r = [];
		for (let t of e.children()) t.isDeploymentNode() && r.push(...dfs(t)), applyElementPredicate(t, n) && r.push(t);
		return t.connectWithExisting(r, `in`), t.addConnections(X(r)), t.addImplicit(r), r;
	}, r = dfs(e);
	if (r.length === 0) return;
	t.connectWithExisting(r, `out`);
	let i = findConnectedElements(t);
	t$32(r, n$27((e) => i.has(e)), t$24((e) => t.addExplicit(e)));
}
function findConnectedElements(e) {
	return t$32(e.mergedConnections(), cleanCrossBoundary, cleanRedundantRelationships, t$7((e, t) => (e.add(t.source), e.add(t.target), e), /* @__PURE__ */ new Set()));
}
var Kt = class StageFinal$1 {
	static {
		__name(this, `StageFinal`);
	}
	memory;
	includeAncestors;
	static for(e, t) {
		return new StageFinal$1(e, t);
	}
	constructor(e, t) {
		this.memory = e, this.includeAncestors = t;
	}
	step1CleanConnections(e) {
		if (e.connections.length < 2) return e;
		let t = t$32(e.connections, cleanCrossBoundary, cleanRedundantRelationships), n = differenceConnections(e.connections, t);
		if (n.length === 0) return e;
		let r = e.stageExclude({});
		return r.excludeConnections(n, !0), r.commit();
	}
	step2ProcessImplicits(e) {
		let t = union(this.memory.elements, e.elements), n = union(e.final, this.memory.explicits), r = new g.default(() => /* @__PURE__ */ new Set());
		for (let t of e.connections) if (t.boundary) for (let e of t.relations.model) r.get(e).add(t.boundary);
		for (let [e, i] of r) if (!(i.size < 2)) for (let e of i) t.delete(e) && n.add(e);
		let i = new Set(n), isFinalOrHasIncludedDescendant = (e) => {
			for (let t of i) if (e === t || isAncestor(e, t)) return !0;
			return !1;
		};
		return t$32(difference(t, i), ifilter((e) => e.isDeploymentNode()), toArray(), sortByFqnHierarchically, t$24((e) => {
			let t = [...e.children()].filter(isFinalOrHasIncludedDescendant).length;
			if (t >= 2) {
				n.add(e);
				return;
			}
			t === 1 && isome(e.siblings(), isFinalOrHasIncludedDescendant) && n.add(e);
		})), e.update({ final: n });
	}
	step3ProcessBoundaries(e) {
		let t = /* @__PURE__ */ new Set();
		for (let n of e.connections) n.boundary && t.add(n.boundary);
		let n = treeFromMemoryState(e, `final`), r = e.stageExclude({}), isRemovable = (r) => !(t.has(r) || e.explicits.has(r) || n.hasInOut(r) || n.root.has(r)), i = t$14([...n.root]);
		i && !e.explicits.has(i) && r.exclude(i);
		for (let t of e.final) {
			let e = t$14(n.children(t));
			e && !n.hasInOut(e) && isRemovable(t) && r.exclude(t);
		}
		return r.isDirty() ? r.commit() : e;
	}
	step4AddAncestor(e) {
		let t = /* @__PURE__ */ new Set();
		for (let n of e.final) for (let e of n.ancestors()) t.add(e);
		if (t.size > 0) {
			let n = union(e.final, t);
			return e.update({ final: n });
		}
		return e;
	}
	commit() {
		let e = this.step1CleanConnections(this.memory), t = this.step2ProcessImplicits(e), n = this.step3ProcessBoundaries(t);
		return this.includeAncestors === !0 ? this.step4AddAncestor(n) : n;
	}
};
var qt = createRelationExpressionToPredicates(deploymentExpressionToPredicate);
function buildExpandPredicate$1(e, t) {
	let n = [], r = [];
	for (let e of t) if (isViewRulePredicate(e) && `include` in e) for (let t of e.include) F.isCustom(t) && (t.customRelation.multiple === !0 ? n.push(t) : t.customRelation.multiple === !1 && r.push(t));
	let i = /* @__PURE__ */ new Set(), a = e.specification.relationships;
	for (let e of Object.keys(a)) {
		let t = a[e];
		(t?.style ?? t)?.multiple && i.add(e);
	}
	if (n.length === 0 && r.length === 0 && i.size === 0) return;
	let o = n.map((e) => ({ pred: qt(e.customRelation.expr) })), s = r.map((e) => ({ pred: qt(e.customRelation.expr) }));
	return (e, t) => {
		let resolveEndpoint = (e) => {
			if (e && typeof e == `object`) {
				if (`tags` in e && `kind` in e && `metadata` in e) return e;
				if (e.instance && e.element) return {
					id: e.id,
					tags: e.instance.tags,
					kind: e.element.kind,
					metadata: e.instance.metadata
				};
			}
			return e ?? {};
		}, n = resolveEndpoint(t?.source ?? e.source), r = resolveEndpoint(t?.target ?? e.target);
		if (s.length > 0) {
			for (let { pred: t } of s) if (t({
				source: n,
				target: r,
				kind: e.kind,
				tags: e.tags,
				metadata: e.metadata
			})) return !1;
		}
		if (i.size > 0) {
			let t = e.kind;
			if (t && i.has(t)) return !0;
		}
		if (o.length > 0) {
			for (let { pred: t } of o) if (t({
				source: n,
				target: r,
				kind: e.kind,
				tags: e.tags,
				metadata: e.metadata
			})) return !0;
		}
		return !1;
	};
}
__name(buildExpandPredicate$1, `buildExpandPredicate`);
function processPredicates$1(e, t) {
	let n = Lt.empty(), r = !1;
	for (let i of t) {
		if (isViewRulePredicate(i)) {
			let t = `include` in i ? `include` : `exclude`, r = i.include ?? i.exclude;
			for (let i of r) {
				let r = t === `include` ? n.stageInclude(i) : n.stageExclude(i);
				r = predicateToPatch(t, {
					expr: i,
					model: e,
					stage: r,
					memory: n,
					where: null
				}) ?? r, n = r.commit();
			}
		}
		`includeAncestors` in i && (r = i.includeAncestors);
	}
	return Kt.for(n, r).commit();
}
__name(processPredicates$1, `processPredicates`);
function computeDeploymentView(e, { docUri: t, rules: n, ...r }) {
	let i = processPredicates$1(e.deployment, n), o = buildNodes$1(e, i), s = buildExpandPredicate$1(e, n), c = toComputedEdges$1(i.connections, s);
	linkNodesWithEdges(o, c);
	let l = topologicalSort({
		nodes: o,
		edges: c
	}), u = applyDeploymentViewRuleStyles(n, l.nodes), d = t$26(n, isViewRuleAutoLayout), f = buildElementNotations(u);
	return calcViewLayoutHash({
		...r,
		[k]: `computed`,
		[Re]: `deployment`,
		autoLayout: {
			direction: d?.direction ?? `TB`,
			...d?.nodeSep && { nodeSep: d.nodeSep },
			...d?.rankSep && { rankSep: d.rankSep }
		},
		edges: l.edges,
		nodes: t$18(u, (e) => (e.icon === `none` && delete e.icon, e)),
		...f.length > 0 && { notation: { nodes: f } }
	});
}
function flattenGroupRules(e) {
	return (t) => isViewRuleGroup(t) ? t.groupRules.flatMap(flattenGroupRules(e)) : isViewRulePredicate(t) && `include` in t ? t.include.filter(e) : [];
}
function applyCustomElementProperties(e, t) {
	let n = e.flatMap(flattenGroupRules(L.isCustom));
	if (n.length === 0) return t;
	let r = [...t];
	for (let { custom: { expr: e, ...t } } of n) {
		let { border: n, opacity: i, multiple: a, iconColor: o, iconSize: s, iconPosition: c, padding: l, size: u, textSize: d, ...p } = t$15(t, e$7), m = exact({
			border: n,
			opacity: i,
			multiple: a,
			iconColor: o,
			iconSize: s,
			iconPosition: c,
			padding: l,
			size: u,
			textSize: d
		}), h = !e$11(m), g = !e$11(p), _ = elementExprToPredicate(e);
		r.forEach((e, t) => {
			isGroupElementKind(e) || !_(e) || (g && (e = {
				...e,
				...p,
				isCustomized: !0
			}), h && (e = {
				...e,
				isCustomized: !0,
				style: {
					...e.style,
					...m
				}
			}), r[t] = e);
		});
	}
	return r;
}
function resolveGlobalRulesInElementView(e, t) {
	return e.reduce((e, n) => {
		if (isViewRuleGlobalPredicateRef(n)) {
			let r = t.predicates[n.predicateId];
			return e$7(r) ? e : e.concat(r);
		}
		if (isViewRuleGlobalStyle(n)) {
			let r = t.styles[n.styleId];
			return e$7(r) ? e : e.concat(r);
		}
		return e.push(n), e;
	}, []);
}
function resolveGlobalRulesInDynamicView(e, t) {
	return e.reduce((e, n) => {
		if (isViewRuleGlobalPredicateRef(n)) {
			let r = t.dynamicPredicates[n.predicateId];
			return e$7(r) ? e : e.concat(r);
		}
		if (isViewRuleGlobalStyle(n)) {
			let r = t.styles[n.styleId];
			return e$7(r) ? e : e.concat(r);
		}
		return e.push(n), e;
	}, []);
}
function elementsFromIncludeProperties(e, t) {
	let n = /* @__PURE__ */ new Set();
	for (let r of t) if (isViewRulePredicate(r)) for (let t of r.include) {
		let r = elementExprToPredicate(t);
		for (let t of e.elements()) r(t) && n.add(t);
	}
	return n;
}
var flattenSteps = (e) => {
	switch (!0) {
		case et.isStep(e): return [e];
		case et.isParallel(e): {
			let t = [], n = [];
			for (let r of e.steps) {
				let [e, ...i] = flattenSteps(r);
				e && t.push(e), n.push(...i);
			}
			return [...t, ...n];
		}
		case et.isSeries(e): return [...e.steps];
		case et.isBreak(e):
		case et.isLoop(e):
		case et.isOpt(e): return t$25(e.steps, flattenSteps);
		case et.isAlt(e):
		case et.isTry(e): {
			let t = [], n = [], r = e._type === `try` ? [
				e.try,
				e.catch,
				e.finally
			] : [...e.branches];
			for (let e of r) {
				if (!e) continue;
				let [r, ...i] = t$25(e.steps, flattenSteps);
				r && (t.push(r), n.push(...i));
			}
			return [...t, ...n];
		}
		default: nonexhaustive(e);
	}
};
function elementsFromSteps(e, t) {
	let n = [], addActor = (...[e, t]) => {
		if (!n.includes(e)) {
			let r = n.indexOf(t);
			if (r > 0) {
				n.splice(r, 0, e);
				return;
			} else n.push(e);
		}
		n.includes(t) || n.push(t);
	};
	for (let r of t$25(t, flattenSteps)) {
		let t = e.element(r.source), i = e.element(r.target), a = n.indexOf(t), o = n.indexOf(i);
		a >= 0 && o >= 0 || (r.isBackward ? addActor(i, t) : addActor(t, i));
	}
	return new Set(n);
}
function findRelations(e, t, n) {
	let i = findConnection(e, t, `directed`).flatMap((e) => [...e.relations]).sort(compareRelations);
	if (!isNonEmptyArray(i)) return {};
	let specificationOf = (t) => t ? e.$model.specification.relationships[t] : void 0;
	if (i.length === 1) {
		let e = i[0], t = specificationOf(e.kind);
		return exact({
			title: e.title ?? void 0,
			kind: e.kind ?? void 0,
			tags: e.tags,
			relations: [e.id],
			navigateTo: e.$relationship.navigateTo,
			color: e.$relationship.color ?? t?.color,
			line: e.$relationship.line ?? t?.line,
			head: e.$relationship.head ?? t?.head,
			tail: e.$relationship.tail ?? t?.tail,
			technology: e.technology ?? void 0,
			description: e.$relationship.description ?? void 0
		});
	}
	let a = t$32(i, t$25((e) => e.tags), n$1()), o = t$30(a, 1) ? a : void 0, s = t$18(i, (e) => e.id), c = n$25(i).$relationship.navigateTo;
	c === n && (c = void 0), c ||= t$32(i, t$25((e) => e.$relationship.navigateTo && e.$relationship.navigateTo !== n ? e.$relationship.navigateTo : []), n$1(), t$14());
	let l = t$32(i, t$7((e, { title: t, technology: n, kind: r, $relationship: i }) => {
		let a = specificationOf(r), o = i.color ?? a?.color, s = i.line ?? a?.line, c = i.head ?? a?.head, l = i.tail ?? a?.tail;
		return e$4(t) && e.title.add(t), e$4(o) && e.color.add(o), e$4(s) && e.line.add(s), e$4(c) && e.head.add(c), e$4(l) && e.tail.add(l), e$4(r) && e.kind.add(r), e$4(n) && e.technology.add(n), e$4(i.description) && !e.description.some(t$22(i.description)) && e.description.push(i.description), e;
	}, {
		kind: /* @__PURE__ */ new Set(),
		color: /* @__PURE__ */ new Set(),
		line: /* @__PURE__ */ new Set(),
		head: /* @__PURE__ */ new Set(),
		tail: /* @__PURE__ */ new Set(),
		title: /* @__PURE__ */ new Set(),
		technology: /* @__PURE__ */ new Set(),
		description: []
	}));
	return exact({
		tags: o ?? void 0,
		relations: s,
		navigateTo: c,
		kind: t$14([...l.kind]),
		title: t$14([...l.title]),
		color: t$14([...l.color]),
		line: t$14([...l.line]),
		head: t$14([...l.head]),
		tail: t$14([...l.tail]),
		technology: t$14([...l.technology]),
		description: t$14(l.description)
	});
}
var DynamicViewCompute = class {
	model;
	view;
	steps = [];
	constructor(e, t) {
		this.model = e, this.view = t;
	}
	compute() {
		let { docUri: e, rules: t, steps: r, ...o } = this.view, s = resolveGlobalRulesInDynamicView(t, this.model.globals), c = elementsFromIncludeProperties(this.model, s), l = elementsFromSteps(this.model, r), u = t$32(union(intersection(c, l), l, c), toArray(), sortParentsFirst), h = {
			actors: /* @__PURE__ */ new Set(),
			flow: [],
			id: null,
			_type: null
		}, g = new S.default();
		g.push(h);
		let flowStackHead = () => g.peek() ?? h, addToFlow = (e) => (flowStackHead().flow.push(e), e), addActorToFlow = (e) => {
			flowStackHead().actors.add(e);
		}, newFlow = (e) => ({
			actors: /* @__PURE__ */ new Set(),
			flow: [],
			...e
		}), pushFlow = (e) => (g.push(e), e), popFlow = () => {
			let e = g.pop();
			return e && e !== h && e.flow.length > 0 && addToFlow(e), e;
		}, processSteps = ({ steps: e, newflow: t, prefix: n }) => {
			pushFlow(t), n ??= t.id, t$7(e, (e, t) => processStep(t, e, n), 1), popFlow();
		}, flowId = (e, t, n) => StepPath(e, [t, n]), processTryStep = (e, t) => {
			let n = 1;
			processSteps({
				steps: e.try.steps,
				newflow: newFlow({
					_type: `try-block`,
					id: flowId(t, n++, `block`),
					title: e.try.title
				})
			}), e.catch && processSteps({
				steps: e.catch.steps,
				newflow: newFlow({
					_type: `try-catch`,
					id: flowId(t, n++, `catch`),
					title: e.catch.title
				})
			}), e.finally && processSteps({
				steps: e.finally.steps,
				newflow: newFlow({
					_type: `try-finally`,
					id: flowId(t, n, `finally`),
					title: e.finally.title
				})
			});
		}, processStep = (e, t, r) => {
			switch (!0) {
				case et.isSeries(e):
					for (let n of e.steps) t = processStep(n, t, r);
					return t;
				case et.isLoop(e):
				case et.isOpt(e):
				case et.isBreak(e):
				case et.isParallel(e): return processSteps({
					steps: e.steps,
					newflow: newFlow({
						_type: e._type,
						id: flowId(r, t, e._type),
						title: e.title
					})
				}), t + 1;
				case et.isTry(e): {
					let { id: n } = pushFlow(newFlow({
						_type: `try`,
						id: flowId(r, t, `try`)
					}));
					return processTryStep(e, n), popFlow(), t + 1;
				}
				case et.isAlt(e): {
					let { id: n } = pushFlow(newFlow({
						_type: `alt`,
						id: flowId(r, t, `alt`),
						title: e.title
					}));
					return e.branches.forEach((e, t) => {
						processSteps({
							steps: e.steps,
							newflow: newFlow({
								_type: `alt-${e._type}`,
								id: flowId(n, t + 1, e._type),
								title: e.title
							})
						});
					}), popFlow(), t + 1;
				}
				case et.isStep(e): {
					let i = addToFlow(StepPath(r, t)), { source: a, target: o, title: s, isBackward: c, navigateTo: l, notation: d, ...p } = e, m = this.model.element(a), h = u.indexOf(m);
					invariant(h >= 0, `Source ${a} not found`);
					let g = this.model.element(o), _ = u.indexOf(g);
					invariant(_ >= 0, `Target ${o} not found`), addActorToFlow(m), addActorToFlow(g);
					let { title: v, relations: y, navigateTo: ee, ...b } = findRelations(m, g, this.view.id), te = e$4(l) && l !== this.view.id ? l : ee, x = e.kind ? this.model.specification.relationships[e.kind] : void 0;
					return this.steps.push(exact({
						...b,
						...!e.technology && !b.technology && x?.technology && { technology: x.technology },
						...!e.color && !b.color && x?.color && { color: x.color },
						...!e.line && !b.line && x?.line && { line: x.line },
						...!e.head && x?.head && { head: x.head },
						...!e.tail && x?.tail && { tail: x.tail },
						...p,
						id: i,
						source: m,
						target: g,
						navigateTo: te,
						title: s ?? v,
						relations: y ?? [],
						isBackward: _ < h
					})), t + 1;
				}
				default: nonexhaustive(e);
			}
		};
		t$7(r, (e, t) => processStep(t, e), 1);
		let _ = buildComputedNodes(this.model.$styles, u.map(elementModelToNodeSource)), v = this.model.$styles.defaults.relationship, y = this.steps.map(({ id: e, source: t, target: n, relations: r, title: i, isBackward: a, tags: o, ...s }) => {
			let c = nonNullable(_.get(t.id), `Source node ${t.id} not found`), l = nonNullable(_.get(n.id), `Target node ${n.id} not found`), u = {
				id: e,
				parent: commonAncestor(t.id, n.id),
				source: c.id,
				target: l.id,
				label: i ?? null,
				relations: r,
				color: v.color,
				line: v.line,
				head: v.arrow,
				tags: o ?? [],
				...s
			};
			for (a && (u.dir = `back`); u.parent && !_.has(u.parent);) u.parent = parentFqn(u.parent);
			c.outEdges.push(u.id), l.inEdges.push(u.id);
			for (let e of ancestorsFqn(u.source)) {
				if (e === u.parent) break;
				_.get(e)?.outEdges.push(u.id);
			}
			for (let e of ancestorsFqn(u.target)) {
				if (e === u.parent) break;
				_.get(e)?.inEdges.push(u.id);
			}
			return u;
		}), ee = applyCustomElementProperties(s, applyViewRuleStyles(s, u.map((e) => nonNullable(_.get(e.id))))), b = t$26(s, isViewRuleAutoLayout), te = buildElementNotations(ee);
		return calcViewLayoutHash({
			...o,
			[Re]: `dynamic`,
			[k]: `computed`,
			variant: o.variant ?? `diagram`,
			flow: toFlowSteps(h),
			autoLayout: {
				direction: b?.direction ?? `LR`,
				...b?.nodeSep && { nodeSep: b.nodeSep },
				...b?.rankSep && { rankSep: b.rankSep }
			},
			nodes: t$18(ee, (e) => (e.icon === `none` && delete e.icon, e)),
			edges: y,
			...te.length > 0 && { notation: { nodes: te } }
		});
	}
};
function computeDynamicView(e, t) {
	return new DynamicViewCompute(e, t).compute();
}
var Jt = t$10(e$16(), t$18((e) => e$5(e) ? e : subflow(e)));
function subflow(e) {
	let t = {
		id: e.id,
		...e.title && { title: e.title }
	};
	if (e._type === `try`) {
		let [r, i, a] = Jt(e.flow);
		if (e$7(r) || e$5(r) || e$5(i) || e$5(a)) throw Error(`Try block, catch block, and finally block must be subflows`);
		invariant(r._type === `try-block`, `Try block is required`);
		let o = {
			_type: e._type,
			flow: [r],
			...t
		};
		return a ? (invariant(a._type === `try-finally`, `3rd party must be a finally block`), invariant(i && i._type === `try-catch`, `2nd must be a catch block`), o.flow = [
			r,
			i,
			a
		], o) : i ? (invariant(i._type === `try-catch` || i._type === `try-finally`, `2nd element must be a catch or finally block, it is ${i._type}`), o.flow = [r, i], o) : o;
	}
	return {
		flow: Jt(e.flow),
		_type: e._type,
		...t
	};
}
function toFlowSteps(e) {
	return e.flow.map((e) => {
		if (e$5(e)) return e;
		let t = subflow(e);
		if (t._type === `alt-else` || t._type === `alt-if` || t._type === `alt-when` || t._type === `try-block` || t._type === `try-catch` || t._type === `try-finally`) throw Error(`Unsupported root-level subflow type: ${t._type}`);
		return t;
	});
}
function applyCustomRelationProperties(e, t, n) {
	let r = e.flatMap(flattenGroupRules(R.isCustom)), i = Array.from(n);
	if (r.length === 0 || i.length === 0) return i;
	for (let { customRelation: { expr: e, title: n, description: a, notes: o, multiple: s, ...c } } of r) {
		let r = a ? { description: MarkdownOrString(a) } : {}, s = o ? { notes: MarkdownOrString(o) } : {}, l = t$15(c, e$7), u = kt(e);
		i.forEach((e, a) => {
			let o, c;
			for (let n of t) if (n.id === e.source && (o = n), n.id === e.target && (c = n), o && c) break;
			!o || !c || u({
				source: o,
				target: c,
				...t$12(e, [`kind`, `tags`])
			}) && (i[a] = {
				...e,
				...l,
				...r,
				...s,
				label: n ?? e.label,
				isCustomized: !0
			});
		});
	}
	return i;
}
function findRedundantConnections(e) {
	return t$32([...e], mergeConnections, t$7((e, t, n, r) => {
		let i = findDescendantConnections(r, t), a = union(...i.map(e$3(`relations`))), o = intersection(t.relations, a);
		if (i.length > 0 && (o = union(o, t.directRelations)), findDeepestNestedConnection(r, t.reversed()) && (o = union(o, t.directRelations)), o.size < t.relations.size) {
			let e = r.some(isAnyInOut(t.source)), n = r.some(isAnyInOut(t.target));
			e && (o = union(o, toSet(ifilter(t.relations, isOutgoing(t.source))))), n && (o = union(o, toSet(ifilter(t.relations, isIncoming(t.target)))));
		}
		return o.size > 0 && e.push(t.update(o)), e;
	}, []));
}
var Yt = class StageFinal {
	memory;
	static for(e) {
		return new StageFinal(e);
	}
	constructor(e) {
		this.memory = e;
	}
	step1CleanConnections(e) {
		if (e.connections.length < 2) return e;
		let t = findRedundantConnections(e.connections);
		if (t.length === 0) return e;
		let n = e.stageExclude({});
		return n.excludeConnections(t, !0), n.commit();
	}
	step2ProcessImplicits(e) {
		let t = union(this.memory.elements, e.elements), n = union(e.final, this.memory.explicits), r = new Set(n), isFinalOrHasIncludedDescendant = (e) => {
			for (let t of r) if (e === t || isAncestor(e, t)) return !0;
			return !1;
		};
		return t$32(difference(t, r), toArray(), sortByFqnHierarchically, t$24((e) => {
			let t = [...e.children()].filter(isFinalOrHasIncludedDescendant).length;
			if (t >= 2) {
				n.add(e);
				return;
			}
			t === 1 && isome(e.siblings(), isFinalOrHasIncludedDescendant) && n.add(e);
		})), e.update({ final: n });
	}
	step3ProcessBoundaries(e) {
		let t = /* @__PURE__ */ new Set();
		for (let n of e.connections) n.boundary && n.boundary !== n.source && n.boundary !== n.target && t.add(n.boundary);
		let n = treeFromMemoryState(e, `final`), r = e.stageExclude({}), isRemovable = (r) => !(t.has(r) || e.explicits.has(r) || n.hasInOut(r) || n.root.has(r)), i = t$14([...n.root]);
		i && !e.explicits.has(i) && r.exclude(i);
		for (let t of e.final) {
			let e = t$14(n.children(t));
			e && !n.hasInOut(e) && isRemovable(t) && r.exclude(t);
		}
		return r.isDirty() ? r.commit() : e;
	}
	commit() {
		let e = this.step1CleanConnections(this.memory), t = this.step2ProcessImplicits(e);
		return this.step3ProcessBoundaries(t);
	}
};
var { findConnection: Xt, findConnectionsBetween: Z, findConnectionsWithin: Q } = ve;
function resolveElements(e, t) {
	switch (!0) {
		case L.isElementKindExpr(t): return [...ifilter(e.elements(), (e) => t.isEqual === (e.kind === t.elementKind))];
		case L.isElementTagExpr(t): return [...ifilter(e.elements(), (e) => t.isEqual === e.tags.includes(t.elementTag))];
		case t.selector === `expanded`: {
			let n = e.element(N.flatten(t.ref));
			return [n, ...n.children()];
		}
		case t.selector === `children`:
		case t.selector === `descendants`: {
			let n = e.element(N.flatten(t.ref)), r = t.selector === `children` ? toArray(n.children()) : toArray(n.descendants());
			return r && r.length > 0 ? r : [n];
		}
		case L.isModelRef(t): return [e.element(N.flatten(t.ref))];
		default: nonexhaustive(t);
	}
}
function includeDescendantsFromMemory(e, t) {
	if (t.isEmpty() || e.length === 0) return e;
	let n = t$35(e.map((e) => isDescendantOf(e))), r = toArray(ifilter(t.elements, (t) => !e.includes(t) && n(t)));
	return [...e, ...r];
}
function resolveAndIncludeFromMemory(e, { memory: t, model: n }) {
	let r = resolveElements(n, e);
	return L.isModelRef(e) ? e.selector === `descendants` ? r : includeDescendantsFromMemory(r, t) : r;
}
var Zt = {
	include: ({ expr: e, model: t, stage: n, where: r }) => {
		let i = t.element(N.flatten(e.ref));
		r(i) && (n.addExplicit(i), n.connectWithExisting(i));
		let a = [...i.children()].filter(r), o = [];
		for (let e of a) n.addImplicit(e), n.connectWithExisting(e) && o.push(e);
		return n.addConnections(Q(o)), n;
	},
	exclude: ({ expr: e, model: t, stage: n, filterWhere: r }) => {
		let i = r(resolveElements(t, e));
		return n.exclude(i), n;
	}
};
var Qt = {
	include: ({ expr: e, model: t, stage: n, filterWhere: r }) => {
		let i = r(resolveElements(t, e));
		if (i.length !== 0) return n.addExplicit(i), n.connectWithExisting(i), n.addConnections(Q(i)), n;
	},
	exclude: ({ expr: e, model: t, stage: n, filterWhere: r }) => {
		let i = r(resolveElements(t, e));
		return n.exclude(i), n;
	}
};
var $t = {
	include: ({ expr: e, model: t, stage: n, filterWhere: r }) => {
		let i = r(resolveElements(t, e));
		if (i.length !== 0) return n.addExplicit(i), n.connectWithExisting(i), n.addConnections(Q(i)), n;
	},
	exclude: ({ expr: e, model: t, stage: n, filterWhere: r }) => {
		let i = r(resolveElements(t, e));
		return n.exclude(i), n;
	}
};
var NoWhere = () => !0;
var NoFilter = (e) => e;
var hashEdgeId = (e, t, n) => stringHash(`model:${e}:${t}:${n}`);
function toComputedEdges(e, t) {
	return e.flatMap((e) => {
		let r = [...e.relations];
		invariant(t$30(r, 1), `Edge must have at least one relation`);
		let i = e.source.$model.$styles.defaults, a = e.source.id, o = e.target.id, s = n$27(r, (e) => e.source.id === a && e.target.id === o);
		if (t && r.length > 1) {
			let [n, s] = t$32(r, t$13((n) => t(n, e)));
			if (n.length > 0) {
				let t = n.map((t) => {
					let { title: n, color: r = i.relationship.color, line: s = i.relationship.line, head: c = i.relationship.arrow, ...l } = mergePropsFromRelationships([t.$relationship], t.$relationship);
					return {
						id: hashEdgeId(a, o, t.id),
						parent: e.boundary?.id ?? null,
						source: a,
						target: o,
						label: n ?? null,
						relations: [t.id],
						color: r,
						line: s,
						head: c,
						...l
					};
				});
				if (s.length > 0) {
					let { title: n, color: r = i.relationship.color, line: c = i.relationship.line, head: l = i.relationship.arrow, ...u } = mergePropsFromRelationships(s.map((e) => e.$relationship), t$14(n$27(s, (e) => e.source.id === a && e.target.id === o))?.$relationship);
					t.push({
						id: e.id,
						parent: e.boundary?.id ?? null,
						source: a,
						target: o,
						label: n ?? null,
						relations: s.map((e) => e.id),
						color: r,
						line: c,
						head: l,
						...u
					});
				}
				return t;
			}
		}
		let { title: c, color: l = i.relationship.color, line: u = i.relationship.line, head: d = i.relationship.arrow, ...p } = mergePropsFromRelationships(r.map((e) => e.$relationship), t$14(s)?.$relationship);
		return [exact({
			id: e.id,
			parent: e.boundary?.id ?? null,
			source: a,
			target: o,
			label: c ?? null,
			relations: r.map((e) => e.id),
			color: l,
			line: u,
			head: d,
			...p
		})];
	});
}
function buildNodes(e, t) {
	return buildComputedNodes(e.$styles, [...t.final].map(elementModelToNodeSource), t.groups);
}
var $ = L.isWildcard;
var isAncestorOrDescendantOf = (e, t) => e.isAncestorOf(t) || e.isDescendantOf(t);
var en = {
	include: ({ expr: { source: e, target: t, isBidirectional: r = !1 }, memory: i, model: a, stage: o, where: c, filterWhere: l }) => {
		let u = $(e), f = $(t), p = [];
		switch (!0) {
			case u && f && i.isEmpty() && c !== NoWhere: {
				let e = t$32(a.relationships(), ifilter(c), toArray(), t$18((e) => new $$1(e.source, e.target, /* @__PURE__ */ new Set([e]))));
				return o.addConnections(e), o;
			}
			case u && f && !i.isEmpty() && c !== NoWhere:
				p.push(...Q(i.elements));
				break;
			case u && f:
				p.push(...Q(a.roots()));
				break;
			case !u && f && c !== NoWhere: {
				let t = t$32(resolveElements(a, e), t$25((e) => t$32(e, e$2(e$20(r === !0), {
					onTrue: (e) => union(e.allIncoming, e.allOutgoing),
					onFalse: (e) => e.allOutgoing
				}), ifilter(c), iunique(), toArray(), t$13((t) => t.source === e), ([t, n]) => t$34(t$32(t, n$27((t) => !isAncestorOrDescendantOf(e, t.target)), t$18((t) => new $$1(e, t.target, /* @__PURE__ */ new Set([t])))), t$32(n, n$27((t) => !isAncestorOrDescendantOf(e, t.source)), t$18((t) => new $$1(t.source, e, /* @__PURE__ */ new Set([t]))))))));
				return o.addConnections(t), o;
			}
			case !u && f: {
				let [t, n] = resolveWildcard(e, {
					memory: i,
					model: a
				}), o = r ? `both` : `directed`;
				for (let e of t) p.push(...Z(e, n, o));
				break;
			}
			case u && !f && c !== NoWhere: {
				let e = t$32(resolveElements(a, t), t$25((e) => t$32(e, e$2(e$20(r === !0), {
					onTrue: (e) => union(e.allIncoming, e.allOutgoing),
					onFalse: (e) => e.allIncoming
				}), ifilter(c), toArray(), t$13((t) => t.target === e), ([t, n]) => t$34(t$32(n, n$27((t) => !isAncestorOrDescendantOf(e, t.target)), t$18((t) => new $$1(e, t.target, /* @__PURE__ */ new Set([t])))), t$32(t, n$27((t) => !isAncestorOrDescendantOf(e, t.source)), t$18((t) => new $$1(t.source, e, /* @__PURE__ */ new Set([t]))))))));
				return o.addConnections(e), o;
			}
			case u && !f: {
				let [e, n] = resolveWildcard(t, {
					memory: i,
					model: a
				}), o = r ? `both` : `directed`;
				for (let t of n) p.push(...Z(t, e, o));
				break;
			}
			default: {
				invariant(!$(e), `Inference failed - source must be not a wildcard`), invariant(!$(t), `Inference failed - target must be not a wildcard`);
				let o = resolveAndIncludeFromMemory(e, {
					memory: i,
					model: a
				}), s = resolveAndIncludeFromMemory(t, {
					memory: i,
					model: a
				}), c = r ? `both` : `directed`;
				for (let e of o) p.push(...Z(e, s, c));
			}
		}
		return o.addConnections(l(p)), o;
	},
	exclude: ({ expr: { source: e, target: t, isBidirectional: r }, model: i, memory: a, stage: o, where: s }) => {
		let c = $(e), l = $(t), u;
		switch (!0) {
			case c && l:
				u = t$32(a.connections, t$25(t$10(e$3(`relations`), ifilter(s), toArray())), toSet());
				break;
			case !c && l:
				u = t$32(resolveElements(i, e), t$25((e) => t$32(e, e$2(e$20(r === !0), {
					onTrue: (e) => union(e.allIncoming, e.allOutgoing),
					onFalse: (e) => e.allOutgoing
				}), ifilter(s), toArray())), toSet());
				break;
			case c && !l:
				u = t$32(resolveElements(i, t), t$25((e) => t$32(e, e$2(e$20(r === !0), {
					onTrue: (e) => union(e.allIncoming, e.allOutgoing),
					onFalse: (e) => e.allIncoming
				}), ifilter(s), toArray())), toSet());
				break;
			default: {
				invariant(!$(e), `Inference failed - source must be not a wildcard`), invariant(!$(t), `Inference failed - target must be not a wildcard`);
				let a = resolveElements(i, e), o = resolveElements(i, t), c = /* @__PURE__ */ new Set();
				for (let e of a) for (let t of o) isSameHierarchy(e, t) || (c = union(c, intersection(e.allOutgoing, t.allIncoming), r ? intersection(t.allOutgoing, e.allIncoming) : /* @__PURE__ */ new Set()));
				u = toSet(ifilter(c, s));
			}
		}
		return o.excludeRelations(u), o;
	}
};
function resolveWildcard(e, { memory: n, model: r }) {
	let i = resolveElements(r, e);
	if (!t$30(i, 1)) return [[], []];
	if (L.isModelRef(e)) {
		let t = toArray(r.element(N.flatten(e.ref)).ascendingSiblings());
		return [includeDescendantsFromMemory(i, n), includeDescendantsFromMemory(t, n)];
	}
	return [i, t$32(i, t$18((e) => e.ascendingSiblings()), iflat(), iunique(), toArray(), (e) => includeDescendantsFromMemory(e, n))];
}
var tn = {
	include: ({ expr: e, scope: t, model: n, memory: r, stage: i, filterWhere: a }) => {
		let o = e.incoming, s = [];
		if (L.isWildcard(o)) {
			if (!t) return;
			for (let e of t.ascendingSiblings()) s.push(...Xt(e, t, `directed`));
		} else {
			let e = resolveAndIncludeFromMemory(o, {
				memory: r,
				model: n
			}), t = [...r.elements];
			t.length === 0 && t.push(...n$1(e.flatMap((e) => [...e.ascendingSiblings()])));
			let i = incomingConnectionPredicate(n, o);
			for (let n of t) s.push(...Z(n, e, `directed`).filter(i));
		}
		return i.addConnections(a(s)), i;
	},
	exclude: ({ expr: { incoming: e }, model: t, scope: n, stage: r, where: i }) => {
		let a = [];
		if (L.isWildcard(e)) {
			if (!n) return;
			a.push(...n.allIncoming);
		} else {
			let n = resolveElements(t, e);
			a.push(...n.flatMap((e) => [...e.allIncoming]));
		}
		return r.excludeRelations(new Set(a.filter(i))), r;
	}
};
function incomingConnectionPredicate(e, t) {
	switch (!0) {
		case L.isElementKindExpr(t):
		case L.isElementTagExpr(t): {
			let e = elementExprToPredicate(t);
			return (t) => e(t.target);
		}
		case L.isModelRef(t) && t.selector === `children`: {
			let n = N.flatten(t.ref);
			return t$35([...e.children(n)].map((e) => X$1.isIncoming(e.id)));
		}
		case L.isModelRef(t) && t.selector === `descendants`: {
			let n = N.flatten(t.ref);
			return t$35([X$1.isInside(n), ...[...e.children(n)].map((e) => X$1.isIncoming(e.id))]);
		}
		case L.isModelRef(t) && t.selector === `expanded`: {
			let e = N.flatten(t.ref);
			return t$35([X$1.isIncoming(e), X$1.isInside(e)]);
		}
		case L.isModelRef(t): {
			let e = N.flatten(t.ref);
			return X$1.isIncoming(e);
		}
		default: nonexhaustive(t);
	}
}
var nn = {
	include: ({ expr: { inout: e }, scope: t, model: n, memory: r, stage: i, filterWhere: a }) => {
		let o = [];
		if (L.isWildcard(e)) {
			if (!t) return;
			o.push(...Z(t, t.ascendingSiblings()));
		} else {
			let t = resolveAndIncludeFromMemory(e, {
				memory: r,
				model: n
			}), i = [...r.elements];
			i.length === 0 && (i = n$1(t.flatMap((e) => toArray(e.ascendingSiblings()))));
			for (let e of t) o.push(...Z(e, i));
		}
		return i.addConnections(a(o)), i;
	},
	exclude: ({ expr: { inout: e }, model: t, scope: n, stage: r, where: i }) => {
		let a = [];
		if (L.isWildcard(e)) {
			if (!n) return;
			a.push(...n.allOutgoing), a.push(...n.allIncoming);
		} else {
			let n = resolveElements(t, e);
			a.push(...n.flatMap((e) => [...e.allOutgoing, ...e.allIncoming]));
		}
		return r.excludeRelations(toSet(a.filter(i))), r;
	}
};
var rn = {
	include: ({ expr: e, scope: t, model: n, memory: r, stage: i, filterWhere: a }) => {
		let o = e.outgoing, s = [];
		if (L.isWildcard(o)) {
			if (!t) return;
			s.push(...Z(t, t.ascendingSiblings(), `directed`));
		} else {
			let e = resolveAndIncludeFromMemory(o, {
				memory: r,
				model: n
			}), t = [...r.elements];
			t.length === 0 && t.push(...n$1(e.flatMap((e) => [...e.ascendingSiblings()])));
			let i = outgoingConnectionPredicate(n, o);
			for (let n of e) s.push(...Z(n, t, `directed`).filter(i));
		}
		return i.addConnections(a(s)), i;
	},
	exclude: ({ expr: { outgoing: e }, model: t, scope: n, stage: r, where: i }) => {
		let a = [];
		if (L.isWildcard(e)) {
			if (!n) return;
			a.push(...n.allOutgoing);
		} else {
			let n = resolveElements(t, e);
			a.push(...n.flatMap((e) => [...e.allOutgoing]));
		}
		return r.excludeRelations(toSet(a.filter(i))), r;
	}
};
function outgoingConnectionPredicate(e, t) {
	switch (!0) {
		case L.isElementKindExpr(t):
		case L.isElementTagExpr(t): {
			let e = elementExprToPredicate(t);
			return (t) => e(t.source);
		}
		case L.isModelRef(t) && t.selector === `children`: {
			let n = N.flatten(t.ref);
			return t$35([...e.children(n)].map((e) => X$1.isOutgoing(e.id)));
		}
		case L.isModelRef(t) && t.selector === `descendants`: {
			let n = N.flatten(t.ref);
			return t$35([X$1.isInside(n), ...[...e.children(n)].map((e) => X$1.isOutgoing(e.id))]);
		}
		case L.isModelRef(t) && t.selector === `expanded`: {
			let e = N.flatten(t.ref);
			return t$35([X$1.isOutgoing(e), X$1.isInside(e)]);
		}
		case L.isModelRef(t): {
			let e = N.flatten(t.ref);
			return X$1.isOutgoing(e);
		}
		default: nonexhaustive(t);
	}
}
var an = {
	include: ({ scope: e, model: t, stage: n, memory: r, where: i }) => {
		if (!e) {
			let e = i === NoWhere ? [...t.roots()] : toArray(ifilter(t.elements(), i));
			if (e.length === 0) return;
			let [r, a] = t$13(e, (e) => !e.imported);
			return n.addExplicit(r), n.addImplicit(a), n.addConnections(Q(e)), n.connectWithExisting(e), n;
		}
		let a = toArray(ifilter(e.children(), i)), o = a.length > 0;
		if (!o) {
			let t = Z(e, e.siblings()), r = e.parent;
			t.length === 0 && r && n.addExplicit(r), a.push(e);
		}
		n.addExplicit(e);
		let s = toSet([...r.elements, ...e.descendingSiblings()]);
		for (let e of s) n.addConnections(Z(e, a, `directed`));
		o && (n.addConnections(Q(a)), n.addExplicit(a));
		for (let e of a) n.addConnections(Z(e, s, `directed`));
		return n;
	},
	exclude: ({ scope: e, memory: t, stage: n, where: r }) => r === NoWhere ? e ? (n.exclude([e, ...e.descendants()]), n) : Nt.empty(t.scope).stageExclude(n.expression) : (n.exclude(n$27([...t.elements], r)), n)
};
function processElementPredicate(e, t, n) {
	switch (!0) {
		case L.isCustom(e): return t === `include` ? processElementPredicate(e.custom.expr, t, n) : n.stage;
		case L.isWhere(e): {
			let r = whereOperatorAsPredicate(e.where.condition), filterWhere = (e) => n$27(e, r);
			return processElementPredicate(e.where.expr, t, {
				...n,
				where: r,
				filterWhere
			});
		}
		case L.isModelRef(e) && e.selector === `expanded`: return Zt[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case L.isWildcard(e): return an[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case L.isElementKindExpr(e):
		case L.isElementTagExpr(e): return Qt[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case L.isModelRef(e): return $t[t]({
			...n,
			expr: e
		}) ?? n.stage;
		default: nonexhaustive(e);
	}
}
function processRelationtPredicate(e, t, n) {
	switch (!0) {
		case R.isCustom(e): return t === `include` ? processRelationtPredicate(e.customRelation.expr, t, n) : n.stage;
		case R.isWhere(e): {
			let r = whereOperatorAsPredicate(e.where.condition), filterRelations = (e) => new Set(n$27([...e], r)), filterWhere = (e) => t$32(e, t$18((e) => new $$1(e.source, e.target, filterRelations(e.relations))), n$27((e) => e.nonEmpty()));
			return processRelationtPredicate(e.where.expr, t, {
				...n,
				where: r,
				filterWhere
			});
		}
		case R.isInOut(e): return nn[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case R.isDirect(e): return en[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case R.isOutgoing(e): return rn[t]({
			...n,
			expr: e
		}) ?? n.stage;
		case R.isIncoming(e): return tn[t]({
			...n,
			expr: e
		}) ?? n.stage;
		default: nonexhaustive(e);
	}
}
function process_predicates(e, t, r) {
	let i = {
		model: e,
		scope: t.scope,
		where: NoWhere,
		filterWhere: NoFilter
	};
	for (let a of r) {
		if (isViewRuleGroup(a)) {
			t = process_predicates(e, Pt.enter(t, a), a.groupRules), invariant(t instanceof Pt, `processPredicates must return ActiveGroupMemory`), t = t.leave();
			continue;
		}
		if (isViewRulePredicate(a)) {
			let e = `include` in a ? `include` : `exclude`, n = a.include ?? a.exclude;
			for (let r of n) {
				let n = e === `include` ? t.stageInclude(r) : t.stageExclude(r);
				switch (!0) {
					case z.isFqnExpr(r):
						n = processElementPredicate(r, e, {
							...i,
							stage: n,
							memory: t
						}) ?? n;
						break;
					case z.isRelationExpr(r):
						n = processRelationtPredicate(r, e, {
							...i,
							stage: n,
							memory: t
						}) ?? n;
						break;
					default: nonexhaustive(r);
				}
				t = n.commit();
			}
		}
	}
	return t;
}
function processPredicates(e, t, n) {
	return t = process_predicates(e, t, n), Yt.for(t).commit();
}
function buildExpandPredicate(e, t) {
	let n = t.flatMap(flattenGroupRules(R.isCustom)).filter((e) => e.customRelation.multiple === !0), r = t.flatMap(flattenGroupRules(R.isCustom)).filter((e) => e.customRelation.multiple === !1), i = /* @__PURE__ */ new Set(), a = e.specification.relationships;
	for (let e of Object.keys(a)) {
		let t = a[e];
		(t?.style ?? t)?.multiple && i.add(e);
	}
	if (n.length === 0 && r.length === 0 && i.size === 0) return;
	let o = n.map((e) => ({ pred: kt(e.customRelation.expr) })), s = r.map((e) => ({ pred: kt(e.customRelation.expr) }));
	return (e, t) => {
		let n = t?.source ?? e.source, r = t?.target ?? e.target;
		if (s.length > 0) {
			for (let { pred: t } of s) if (t({
				source: n,
				target: r,
				kind: e.kind,
				tags: e.tags,
				metadata: e.metadata
			})) return !1;
		}
		if (i.size > 0) {
			let t = e.kind;
			if (t && i.has(t)) return !0;
		}
		if (o.length > 0) {
			for (let { pred: t } of o) if (t({
				source: n,
				target: r,
				kind: e.kind,
				tags: e.tags,
				metadata: e.metadata
			})) return !0;
		}
		return !1;
	};
}
function computeElementView(e, { docUri: t, rules: n, ...r }) {
	let i = resolveGlobalRulesInElementView(n, e.globals), a = r.viewOf ? e.asComputed.element(r.viewOf) : null, o = processPredicates(e, Nt.empty(a), i);
	o.isEmpty() && a && (o = o.update({ final: /* @__PURE__ */ new Set([a]) })), o = assignElementsToGroups(o);
	let s = buildNodes(e, o), c = buildExpandPredicate(e, i), l = toComputedEdges(o.connections, c);
	linkNodesWithEdges(s, l);
	let u = topologicalSort({
		nodes: s,
		edges: l
	}), d = applyCustomElementProperties(i, applyViewRuleStyles(i, u.nodes)), f = t$26(i, isViewRuleAutoLayout), p = buildElementNotations(d), m = collectRankConstraints(i, d);
	return calcViewLayoutHash({
		...r,
		_stage: `computed`,
		autoLayout: {
			direction: f?.direction ?? `TB`,
			...f?.nodeSep && { nodeSep: f.nodeSep },
			...f?.rankSep && { rankSep: f.rankSep }
		},
		edges: applyCustomRelationProperties(i, d, u.edges),
		nodes: t$18(d, (e) => (e.icon === `none` && delete e.icon, e)),
		...p.length > 0 && { notation: { nodes: p } },
		...m.length > 0 && { ranks: m }
	});
}
function collectRankConstraints(e, t) {
	let n = [];
	for (let r of e) {
		if (!isViewRuleRank(r) || r.targets.length === 0) continue;
		let e = t$32(t, n$27(t$35(r.targets.map(elementExprToPredicate))), t$18((e) => e.id));
		t$30(e, 1) && n.push({
			type: r.rank,
			nodes: e
		});
	}
	return n;
}
function assignElementsToGroups(e) {
	if (e.groups.length === 0) return e;
	let t = new g.default(() => /* @__PURE__ */ new Set()), n = /* @__PURE__ */ new Map(), isAncestorAssigned = (e) => {
		for (let r of e.ancestors()) {
			let i = n.get(r);
			if (i) return n.set(e, i), t.get(i).add(e), !0;
		}
		return !1;
	}, isDescendantAssigned = (e) => {
		for (let r of e.descendants(`asc`)) {
			let i = n.get(r);
			if (i) return n.set(e, i), t.get(i).add(e), !0;
		}
		return !1;
	};
	if (t$32(sortParentsFirst([...e.explicitFirstSeenIn.keys()]), t$24((r) => {
		if (!isAncestorAssigned(r)) {
			let i = nonNullable(e.explicitFirstSeenIn.get(r));
			n.set(r, i), t.get(i).add(r);
		}
	})), t$32(sortParentsFirst([...e.lastSeenIn.keys()]), n$27((e) => !n.has(e)), t$24((r) => {
		if (isAncestorAssigned(r) || isDescendantAssigned(r)) return;
		let i = nonNullable(e.lastSeenIn.get(r));
		n.set(r, i), t.get(i).add(r);
	})), t.size === 0) return e;
	let r = e.groups.map((e) => {
		let n = t.get(e.id);
		return n ? e.update(n) : e;
	});
	return e.update({ groups: r });
}
function unsafeComputeView(e, t) {
	switch (!0) {
		case isElementView(e): return computeElementView(t, e);
		case isDeploymentView(e): return computeDeploymentView(t, e);
		case isDynamicView(e): return computeDynamicView(t, e);
		default: nonexhaustive(e);
	}
}
function computeView(e, t) {
	try {
		return {
			isSuccess: !0,
			view: unsafeComputeView(e, t)
		};
	} catch (e) {
		return {
			isSuccess: !1,
			error: e instanceof Error ? e : Error(`Unknown error: ${e}`),
			view: void 0
		};
	}
}
function computeParsedModelData(e) {
	let t = Y$1.create(e), { views: n, _stage: r, ...i } = e, o = t$16(n, (e) => unsafeComputeView(e, t));
	return {
		[k]: `computed`,
		...i,
		views: o
	};
}
function computeLikeC4Model(e) {
	return Y$1.create(computeParsedModelData(e));
}
var asTypedExpr = (e) => e;
function $expr(e) {
	if (!e$5(e)) return e;
	if (e === `*`) return asTypedExpr({ wildcard: !0 });
	if (e.startsWith(`->`)) return e.endsWith(`->`) ? asTypedExpr({ inout: $expr(e.replace(/->/g, ``).trim()) }) : asTypedExpr({ incoming: $expr(e.replace(`-> `, ``)) });
	if (e.endsWith(` ->`)) return asTypedExpr({ outgoing: $expr(e.replace(` ->`, ``)) });
	if (e.includes(` <-> `)) {
		let [t, n] = e.split(` <-> `);
		return asTypedExpr({
			source: $expr(t),
			target: $expr(n),
			isBidirectional: !0
		});
	}
	if (e.includes(` -> `)) {
		let [t, n] = e.split(` -> `);
		return asTypedExpr({
			source: $expr(t),
			target: $expr(n)
		});
	}
	return e.endsWith(`._`) ? asTypedExpr({
		ref: { model: e.replace(`._`, ``) },
		selector: `expanded`
	}) : e.endsWith(`.**`) ? asTypedExpr({
		ref: { model: e.replace(`.**`, ``) },
		selector: `descendants`
	}) : e.endsWith(`.*`) ? asTypedExpr({
		ref: { model: e.replace(`.*`, ``) },
		selector: `children`
	}) : asTypedExpr({ ref: { model: e } });
}
//#endregion
//#region node_modules/@likec4/core/dist/compute-view/index.mjs
function r(...n) {
	return t$37(i, n, a);
}
var i = (n, o) => o < 0 ? [...n] : n.slice(o);
function a(n) {
	if (n <= 0) return m$1;
	let o = n;
	return (n) => o > 0 ? (--o, f$1) : {
		done: !1,
		hasNext: !0,
		next: n
	};
}
function t(...n) {
	return t$37(Object.keys, n);
}
function e(n) {
	let o = !1, s;
	return () => (o ||= (s = n(), !0), s);
}
function resolveRulesExtendedViews(n) {
	let o = new m({ type: `directed` }), s = [];
	for (let c of t$2$1(n)) o.addNode(c.id, { view: c }), isExtendsElementView(c) && s.push(c);
	if (s.length === 0) return n;
	for (let n of s) {
		if (!o.hasNode(n.extends)) {
			console.warn(`View "${n.id}" extends from "${n.extends}" which does not exist`);
			continue;
		}
		if ((0, a$1.default)(o, n.id, n.extends)) {
			console.warn(`View "${n.id}" extends from "${n.extends}" which creates a cycle`);
			continue;
		}
		o.addDirectedEdge(n.id, n.extends);
	}
	return (0, i$1.topologicalSort)(o).reverse().reduce((n, s) => {
		let c = o.getNodeAttribute(s, `view`);
		if (!isExtendsElementView(c)) return n[c.id] = c, n;
		let l = n[c.extends];
		if (!l || !isElementView(l)) return n;
		let u = n$1([...l.tags ?? [], ...c.tags ?? []]), f = [...l.links ?? [], ...c.links ?? []];
		return n[c.id] = {
			...l,
			...c,
			title: c.title ?? l.title ?? null,
			description: c.description ?? l.description ?? null,
			tags: u,
			links: isNonEmptyArray(f) ? f : null,
			rules: [...l.rules, ...c.rules]
		}, n;
	}, {});
}
function withReadableEdges({ edges: n, nodes: o, ...s }, c = `:`) {
	let l = t$17(n, (n) => [n.id, `${n.source}${c}${n.target}`]);
	return {
		...s,
		edges: n.map((n) => ({
			...n,
			id: l[n.id]
		})),
		nodes: o.map((n) => ({
			...n,
			inEdges: t$18(n.inEdges, (n) => l[n]),
			outEdges: t$18(n.outEdges, (n) => l[n])
		})),
		nodeIds: o.map((n) => n.id),
		edgeIds: n.map((n) => l[n.id])
	};
}
function viewsWithReadableEdges({ views: n, ...o }) {
	return {
		...o,
		views: t$16(n, (n) => n$14(withReadableEdges(n), [`nodeIds`, `edgeIds`]))
	};
}
var keysCount = (n) => t(n).length;
var nodeId = (n) => stringHash(n);
function computeProjectsView(o) {
	let l = buildNodesForEachModel(o), key = (n, o) => `${n}->${o}`, d = new g.default((n) => {
		let [o, s] = n.split(`->`);
		return {
			from: o,
			to: s,
			relationships: /* @__PURE__ */ new Set()
		};
	}), relationshipsFromTo = (n, o) => d.get(key(n, o)).relationships, processImportedElement = (o, s) => {
		let c = s.projectId;
		invariant(c !== o.projectId, `Imported element must have a different project id`);
		let l = relationshipsFromTo(o.projectId, c);
		for (let n of s.incoming(`direct`)) n.source.projectId === o.projectId && l.add(n);
		let u = relationshipsFromTo(c, o.projectId);
		for (let n of s.outgoing(`direct`)) n.target.projectId === o.projectId && u.add(n);
	};
	for (let n of o) if (!e$12(n.$data.imports)) for (let [o, s] of t$27(n.$data.imports)) for (let c of s) {
		let s = GlobalFqn(o, c.id);
		processImportedElement(n, nonNullable(n.findElement(s), `Element ${c.id} from project ${o} not found in model ${n.projectId}`));
	}
	let f = Array.from(d.entries()).filter(([n, { relationships: o }]) => o.size > 0).map(([o, { from: s, to: l, relationships: d }]) => {
		let f = [...d];
		invariant(t$30(f, 1), `Relationships set must have at least one relationship`);
		let p = {
			id: stringHash(o),
			source: nodeId(s),
			target: nodeId(l),
			projectId: s,
			relations: t$18(f, e$3(`id`)),
			label: null,
			color: `gray`,
			parent: null,
			line: `solid`,
			tags: n$1(t$25(f, e$3(`tags`)))
		}, m = t$14(f);
		return m && (p.label = m.title, p.description = preferSummary(m.$relationship) ?? null, p.technology = m.technology, p.color = m.color, p.line = m.line, m.kind && (p.kind = m.kind), m.$relationship.navigateTo && (p.navigateTo = m.$relationship.navigateTo)), p;
	});
	linkNodesWithEdges(l, f);
	let p = topologicalSort({
		nodes: l,
		edges: f
	});
	return {
		id: `projects-view`,
		[Re]: `projects`,
		[k]: `computed`,
		title: `Projects`,
		description: { txt: `Overview of all projects and their relationships` },
		autoLayout: { direction: `TB` },
		...p
	};
}
function buildNodesForEachModel(n) {
	let o = /* @__PURE__ */ new Map();
	for (let s of n) {
		let n = ProjectId(s.projectId), c = {
			id: nodeId(n),
			kind: `@project`,
			parent: null,
			projectId: n,
			title: s.project.title ?? s.project.id,
			description: { txt: [
				`Elements: ${keysCount(s.$data.elements)}`,
				`Relationships: ${keysCount(s.$data.relations)}`,
				`Views: ${keysCount(s.$data.views)}`
			].join(`
`) },
			shape: `rectangle`,
			children: [],
			inEdges: [],
			outEdges: [],
			color: `primary`,
			level: 0,
			style: {},
			tags: []
		};
		o.set(c.id, c);
	}
	return o;
}
function treeFromElements(n) {
	let s = sortParentsFirst([...n]), c = new Set(s), l = /* @__PURE__ */ new Map(), u = new g.default(() => null), d = s.reduce((n, o, s, d) => (l.set(o.id, o), n.set(o.id, t$32(d, r(s + 1), n$27(isDescendantOf(o)), t$7((n, s) => (c.delete(s), n.some(isAncestor(s)) || (n.push(s), u.set(s.id, o)), n), []))), n), new g.default(() => []));
	return {
		sorted: s,
		byId: (n) => nonNullable(l.get(n), `Element not found by id: ${n}`),
		root: c,
		parent: (n) => u.get(typeof n == `string` ? n : n.id),
		children: (n) => d.get(typeof n == `string` ? n : n.id),
		flatten: e(() => /* @__PURE__ */ new Set([...c, ...s.reduce((n, o) => {
			let s = d.get(o.id);
			if (s.length === 0) {
				n.push(o);
				let s = u.get(o.id);
				return s && n.push(s), n;
			}
			return s.length > 1 && n.push(...s), n;
		}, [])]))
	};
}
var finalize = (n, s) => n.size > 2 && s.size !== n.size ? new Set(sortParentsFirst([...treeFromElements(n).flatten(), ...s])) : n.size > 1 ? new Set(sortParentsFirst([...n])) : n;
function computeRelationships(n, o, s) {
	let isOther = (n) => o.has(n), c = /* @__PURE__ */ new Set([n]), l = {
		incomers: /* @__PURE__ */ new Set(),
		subjects: /* @__PURE__ */ new Set([n]),
		outgoers: /* @__PURE__ */ new Set()
	}, u = new Set(s.incoming.flatMap((o) => {
		if (l.subjects.add(o.target), l.incomers.add(o.source), c.add(o.target), o.target !== n) {
			let s = o.target.parent;
			for (; s && s !== n;) c.add(s), s = s.parent;
		}
		let s = o.source, u = [];
		for (; u.push(s), !(isOther(s) || !s.parent);) s = s.parent;
		return u;
	})), d = new Set(s.outgoing.flatMap((o) => {
		if (l.subjects.add(o.source), l.outgoers.add(o.target), c.add(o.source), o.source !== n) {
			let s = o.source.parent;
			for (; s && s !== n;) c.add(s), s = s.parent;
		}
		let s = o.target, u = [];
		for (; u.push(s), !(isOther(s) || !s.parent);) s = s.parent;
		return u;
	}));
	return {
		incomers: finalize(u, l.incomers),
		incoming: new Set(s.incoming),
		subjects: finalize(c, l.subjects),
		outgoing: new Set(s.outgoing),
		outgoers: finalize(d, l.outgoers)
	};
}
function computeRelationshipsView(o, s, c, l = `global`) {
	let u = c ? s.findView(c) : null;
	if (l === `view`) return invariant(u, `Scope view id is required when scope is "view"`), computeScopedRelationshipsView(o, u, s);
	let d = s.element(o);
	return computeRelationships(d, toSet(d.ascendingSiblings()), {
		incoming: [...d.incoming()],
		outgoing: [...d.outgoing()]
	});
}
function computeScopedRelationshipsView(n, o, s) {
	let c = s.element(n), u = {
		incoming: toArray(ifilter(c.incoming(), (n) => o.includesRelation(n.id))),
		outgoing: toArray(ifilter(c.outgoing(), (n) => o.includesRelation(n.id)))
	}, d = isDescendantOf(c);
	return computeRelationships(c, /* @__PURE__ */ new Set([...c.ascendingSiblings(), ...t$32(o.elements(), imap((n) => n.element), ifilter((n) => n !== c && d(n)))]), u);
}
function computeAdhocView(n, o) {
	return {
		...computeElementView(n, {
			id: ViewId(`adhoc`),
			_stage: `parsed`,
			_type: `element`,
			rules: o,
			title: null,
			description: null
		}),
		_type: `adhoc`,
		_stage: `computed`
	};
}
var te = class AdhocView {
	model;
	static use(n) {
		return new AdhocView(n);
	}
	#e = [];
	constructor(n) {
		this.model = n;
	}
	Expr;
	include(...n) {
		return this.#e.push({ include: n.map((n) => $expr(n)) }), this;
	}
	exclude(...n) {
		return this.#e.push({ exclude: n.map((n) => $expr(n)) }), this;
	}
	compute() {
		return computeAdhocView(this.model, this.#e);
	}
};
function computeFlow(n) {
	let { view: o, subflows: s = [] } = n;
	if (!o.flow) throw Error(`view '${o.id}' is outdated and has no flow. Please refresh the view.`);
	let c = new Set(s), l = /* @__PURE__ */ new Set(), hasRequestedDescendant = (n) => {
		let o = `${n}.`;
		for (let n of c) if (n.startsWith(o)) return !0;
		return !1;
	}, walk = (n) => {
		for (let o of n) {
			if (e$5(o)) {
				l.add(o);
				continue;
			}
			o._type === `alt` || o._type === `try` ? walkBranches(o) : walk(o.flow);
		}
	}, walkBranches = (n) => {
		let o = hasRequestedDescendant(n.id);
		for (let s of n.flow) (!o || c.has(s.id) || hasRequestedDescendant(s.id)) && walk(s.flow);
	};
	if (walk(o.flow), l.size === o.edges.length) return {
		edges: o.edges,
		nodes: o.nodes,
		subflows: s
	};
	let u = n$27(o.edges, (n) => l.has(n.id)), d = /* @__PURE__ */ new Set(), f = new Set(t$18(u, (n) => (d.add(n.source), d.add(n.target), n.id))), p = new Map(o.nodes.map((n) => [n.id, n])), m = /* @__PURE__ */ new Set();
	for (let n of d) {
		let o = n;
		for (; o && !m.has(o);) m.add(o), o = p.get(o)?.parent ?? null;
	}
	return {
		subflows: s,
		nodes: t$32(o.nodes, n$27((n) => m.has(n.id)), t$18((n) => ({
			...n,
			children: n.children.filter((n) => m.has(n)),
			inEdges: n.inEdges.filter((n) => f.has(n)),
			outEdges: n.outEdges.filter((n) => f.has(n))
		}))),
		edges: u
	};
}
//#endregion
export { resolveRulesExtendedViews as a, viewsWithReadableEdges as c, computeParsedModelData as d, computeView as f, computeRelationshipsView as i, withReadableEdges as l, computeFlow as n, te as o, unsafeComputeView as p, computeProjectsView as r, treeFromElements as s, computeAdhocView as t, computeLikeC4Model as u };
